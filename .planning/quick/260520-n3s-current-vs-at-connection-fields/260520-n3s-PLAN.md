---
phase: quick-260520-n3s
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/contacts/import/route.ts
  - src/features/job-leads/components/recommendation-card.tsx
  - src/features/job-leads/components/recommendation-list.tsx
  - scripts/backfill-enrichment-reset.mjs
autonomous: true
requirements: [QUICK-CURRENT-VS-AT-CONNECTION]

must_haves:
  truths:
    - "New CSV imports seed currentCompany/title from CSV only and never populate at-connection fields"
    - "Every CSV-imported row lands as enrichmentStatus='unenriched', feeding the enrichment queue"
    - "The triage recommendation card shows two distinguishable lines: current role/company and at-connection role/company, each only when present"
    - "A dry-run backfill script reports how many existing rows have at-connection == current without writing anything"
  artifacts:
    - path: "src/app/api/contacts/import/route.ts"
      provides: "Import seeding at-connection null + unenriched"
      contains: "companyAtConnection: null"
    - path: "src/features/job-leads/components/recommendation-card.tsx"
      provides: "Two-line current vs at-connection display"
      contains: "currentCompany"
    - path: "src/features/job-leads/components/recommendation-list.tsx"
      provides: "Passes current title/company into card"
    - path: "scripts/backfill-enrichment-reset.mjs"
      provides: "Guarded idempotent dry-run/apply reset script"
      contains: "IS NOT DISTINCT FROM"
  key_links:
    - from: "src/features/job-leads/components/recommendation-list.tsx"
      to: "src/features/job-leads/components/recommendation-card.tsx"
      via: "currentRole/currentCompany props"
      pattern: "currentCompany=\\{rec.contact.currentCompany\\}"
---

<objective>
Correct the current-vs-at-connection contact data model surfaced after phase 10.

The LinkedIn connections-export `Company`/`Position` columns are the person's CURRENT role as of export, NOT their role at time of connection. The import route currently mis-seeds the at-connection fields from those same CSV columns and marks rows 'enriched', which permanently empties the enrichment queue. The triage card only renders one (mislabeled) line. ~1500 already-imported rows carry at-connection == current and are wrongly 'enriched'.

This plan: (1) fixes import seeding, (2) shows both lines in the triage card, (3) wires the current fields through the list, (4) ships a guarded dry-run backfill to reset the legacy rows.

Purpose: Restore the enrichment pipeline (queue must be populated) and give triage accurate now/at-connection context.
Output: Corrected import route, two-line card, wired list, idempotent backfill script.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

<interfaces>
<!-- Already-available facts the executor must build against. No exploration needed. -->

drizzle/schema/contacts.ts — relevant columns:
  title (current role), currentCompany (current company),
  companyAtConnection, roleAtConnection (nullable text),
  enrichmentStatus (default 'unenriched')

src/lib/domain/types.ts:
  export type Contact = typeof contacts.$inferSelect   // rec.contact is this full type
  export const contactEnrichmentStatusValues = [...]   // includes 'unenriched', 'enriched'

src/features/job-leads/lib/prioritization.ts:
  export type PrioritizedRecommendation = { contact: Contact; prospects: [...]; score: number }
  // rec.contact.title and rec.contact.currentCompany are ALREADY present — no route change.

src/lib/db/index.ts pattern (for the backfill script):
  import { neon } from '@neondatabase/serverless'
  const sql = neon(process.env.DATABASE_URL!)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix import route — CSV seeds current only, at-connection null, unenriched</name>
  <files>src/app/api/contacts/import/route.ts</files>
  <action>
    Per locked decision "CSV = current only": treat the CSV `Company`/`Position` columns as the contact's CURRENT role.
    - In the `candidates.push({...})` block (around lines 93-104): keep `currentCompany: company` and `title: position`; change `companyAtConnection` and `roleAtConnection` to literal `null` (do NOT seed them from `company`/`position`). The `Candidate` type already declares both as `string | null`.
    - In the bulk-insert `.values(...)` map (around lines 147-165): set `companyAtConnection: null`, `roleAtConnection: null`, and `enrichmentStatus: 'unenriched' as (typeof contactEnrichmentStatusValues)[number]` for every row (no conditional). Remove the now-stale comment about CSV providing the at-connection baseline.
    - Leave UNCHANGED: name+company dedup (STEP 1-3), the `key` composition, the `onConflictDoNothing` URL block (STEP 4), and the `logTimeline` event (STEP 5).
    - Keep `linkedinConnectionDate` seeding from `Connected On` unchanged — only the at-connection role/company and enrichment status change.
    Follow project conventions: single quotes, 2-space indent, no trailing commas, no server actions, `{ success, data, error, meta }` envelope already in place via `success(...)`.
  </action>
  <verify>
    <automated>grep -n "companyAtConnection: null" src/app/api/contacts/import/route.ts && grep -n "roleAtConnection: null" src/app/api/contacts/import/route.ts && grep -v '^[[:space:]]*//' src/app/api/contacts/import/route.ts | grep -c "enrichmentStatus: 'unenriched'"</automated>
  </verify>
  <done>candidates set at-connection fields to null; insert map sets both at-connection fields null and enrichmentStatus='unenriched' unconditionally; dedup/ON CONFLICT/timeline blocks unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Recommendation card — two labeled lines (Now / At connection)</name>
  <files>src/features/job-leads/components/recommendation-card.tsx</files>
  <action>
    Add the contact's CURRENT role/company display alongside the existing at-connection display.
    - Extend `RecommendationCardProps` with `currentRole?: string | null` and `currentCompany?: string | null`.
    - Destructure both new props in the component signature.
    - Replace the single combined at-connection `<p>` (currently lines 69-73) with TWO separate `<p>` elements using the SAME muted styling (`text-muted-foreground mt-0.5 text-xs`):
      1. Current line — render only when `currentRole || currentCompany` is truthy; prefix with a label `Now: ` then `[currentRole, currentCompany].filter(Boolean).join(' @ ')`.
      2. At-connection line — render only when `companyAtConnection || roleAtConnection` is truthy; prefix with a label `At connection: ` then `[roleAtConnection, companyAtConnection].filter(Boolean).join(' @ ')`.
    - Labels go inside the `<p>` so the two lines are visually distinguishable. Keep the `lastContactDate` line above them unchanged.
    Conventions: PascalCase component, named export, single quotes, no trailing commas.
  </action>
  <verify>
    <automated>grep -n "currentRole" src/features/job-leads/components/recommendation-card.tsx && grep -n "Now:" src/features/job-leads/components/recommendation-card.tsx && grep -n "At connection:" src/features/job-leads/components/recommendation-card.tsx</automated>
  </verify>
  <done>Card props include currentRole/currentCompany; two conditionally-rendered muted lines labeled "Now:" and "At connection:" exist; each renders only when its values are present.</done>
</task>

<task type="auto">
  <name>Task 3: Wire current fields through recommendation list</name>
  <files>src/features/job-leads/components/recommendation-list.tsx</files>
  <action>
    Pass the contact's current role/company into the new card props. In the `<RecommendationCard>` JSX (around lines 75-90), add `currentRole={rec.contact.title}` and `currentCompany={rec.contact.currentCompany}` alongside the existing `companyAtConnection`/`roleAtConnection` props. `rec.contact` is the full `Contact` type, so both fields are already available — no route or prioritization change. Leave all other props and the `handleRequestIntro` logic unchanged.
  </action>
  <verify>
    <automated>grep -n "currentRole={rec.contact.title}" src/features/job-leads/components/recommendation-list.tsx && grep -n "currentCompany={rec.contact.currentCompany}" src/features/job-leads/components/recommendation-list.tsx</automated>
  </verify>
  <done>List passes rec.contact.title as currentRole and rec.contact.currentCompany as currentCompany into the card.</done>
</task>

<task type="auto">
  <name>Task 4: Guarded idempotent backfill script (dry-run by default)</name>
  <files>scripts/backfill-enrichment-reset.mjs</files>
  <action>
    Per locked decision "Reset to unenriched": create a one-time Node ESM script that resets legacy rows where at-connection == current.
    - Load `DATABASE_URL` from `.env.local` (read the file, parse the `DATABASE_URL=` line; do NOT add a new dependency — a minimal manual parse is fine, or use `process.env.DATABASE_URL` if already set). Exit with a clear error if unset.
    - Connect via `import { neon } from '@neondatabase/serverless'` (already a project dependency) — `const sql = neon(DATABASE_URL)`.
    - Predicate (use exactly): rows WHERE `archived_at IS NULL AND company_at_connection IS NOT DISTINCT FROM current_company AND role_at_connection IS NOT DISTINCT FROM title`.
    - Parse argv for `--apply`. DEFAULT (no flag) = DRY-RUN: run a `SELECT count(*)` against the predicate, print the affected count and a message that no rows were written, then exit 0. NEVER write in dry-run.
    - With `--apply`: run an `UPDATE contacts SET company_at_connection = NULL, role_at_connection = NULL, enrichment_status = 'unenriched', updated_at = now()` against the SAME predicate. Idempotent — re-running matches fewer/zero rows because reset rows no longer satisfy `IS NOT DISTINCT FROM current` (NULL vs non-null current differs). Print the number of rows updated. NEVER hard-delete.
    - Add a top-of-file comment block documenting: dry-run command `node scripts/backfill-enrichment-reset.mjs`, apply command `node scripts/backfill-enrichment-reset.mjs --apply`, and that the EXECUTOR MUST NOT run --apply against the live DB — the user runs it after review.
    Conventions: ESM, single quotes, 2-space indent, no trailing commas. Do NOT execute the script during this task.
  </action>
  <verify>
    <automated>node --check scripts/backfill-enrichment-reset.mjs && grep -n "IS NOT DISTINCT FROM" scripts/backfill-enrichment-reset.mjs && grep -n -- "--apply" scripts/backfill-enrichment-reset.mjs && grep -n "enrichment_status" scripts/backfill-enrichment-reset.mjs</automated>
  </verify>
  <done>Script parses syntactically; dry-run prints affected count without writing; --apply performs the guarded reset UPDATE; predicate uses IS NOT DISTINCT FROM; no hard-delete; run commands documented in header. Executor does NOT run --apply.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSV upload → import route | Untrusted file content parsed and inserted |
| backfill script → Neon prod DB | Bulk mutation against live data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-n3s-01 | Tampering | backfill-enrichment-reset.mjs | mitigate | Dry-run by default; writes only behind explicit `--apply`; executor forbidden from running `--apply`; user runs after review. Predicate scoped to `archived_at IS NULL` + at-connection==current only; never hard-deletes. |
| T-n3s-02 | Denial of Service | contacts/import/route.ts | accept | Existing bulk-insert path unchanged; only seeded values change. No new attack surface introduced. |
| T-n3s-03 | Information Disclosure | recommendation-card.tsx | accept | Single-user app (Clerk lock on steve@bronstein.org); displaying contact role/company to the sole owner is in-scope. |

No package installs in this plan — no supply-chain (T-*-SC) checkpoint required.
</threat_model>

<verification>
Run `npm run build` — must succeed (TypeScript strict mode, new card props typed correctly).
Per-task grep gates above confirm the specific edits landed.
The backfill script is verified by `node --check` only — it is NOT executed against any DB during the phase.
</verification>

<success_criteria>
- `npm run build` passes.
- Import route seeds at-connection fields null and enrichmentStatus 'unenriched' for all CSV rows; dedup/ON CONFLICT/timeline unchanged.
- Triage card renders two distinguishable conditional lines ("Now:" and "At connection:").
- List wires rec.contact.title and rec.contact.currentCompany into the card.
- Backfill script: dry-run reports count without writing; --apply guarded; idempotent; documented; never hard-deletes; not executed by the executor.
</success_criteria>

<output>
Create `.planning/quick/260520-n3s-current-vs-at-connection-fields/260520-n3s-SUMMARY.md` when done. In the SUMMARY, document the exact backfill run commands (dry-run and --apply) and the explicit note that the user — not the executor — runs `--apply` against the live DB after reviewing the dry-run count.
</output>
