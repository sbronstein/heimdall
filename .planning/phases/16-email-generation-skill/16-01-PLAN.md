---
phase: 16-email-generation-skill
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/outreach-campaigns/[id]/emails/[emailId]/generation/route.ts
  - src/app/api/outreach-campaigns/[id]/emails/[emailId]/generation/route.test.ts
autonomous: true
requirements: [GEN-01]
must_haves:
  truths:
    - "PATCH .../generation advances a pending email to status='generated' in the same UPDATE that writes content, gated by canEmailTransition (D-02)"
    - "A source status that the state machine does not allow → generated is rejected 400 and the email is NOT advanced"
    - "Exactly one timeline event is emitted per write-back"
  artifacts:
    - path: "src/app/api/outreach-campaigns/[id]/emails/[emailId]/generation/route.ts"
      provides: "Guarded status='generated' write-back (content + status + timestamps in one UPDATE)"
      contains: "canEmailTransition"
    - path: "src/app/api/outreach-campaigns/[id]/emails/[emailId]/generation/route.test.ts"
      provides: "PGlite route test: pending→generated 200; invalid-source→400"
  key_links:
    - from: "src/app/api/outreach-campaigns/[id]/emails/[emailId]/generation/route.ts"
      to: "src/features/outreach/lib/email-status.ts"
      via: "import canEmailTransition"
      pattern: "canEmailTransition\\("
---

<objective>
Close the D-02 gap: the `/generation` write-back route currently writes `generatedSubject/generatedBody/generatedAt/updatedAt` but leaves `status='pending'`, so generated emails never advance — contradicting ROADMAP success criterion #1. Edit the route to also set `status='generated'`, gated through `canEmailTransition(email.status, 'generated')`, in the same UPDATE (one call, one timeline event). This is the only application-code change in Phase 16.

Purpose: success criterion #1 ("running the skill advances all pending emails to generated") only holds once the route flips status. The skill (Plan 16-03) makes a single `PATCH .../generation` call per email and relies on this behavior.
Output: edited route + a PGlite transition test pinning the guard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/16-email-generation-skill/16-CONTEXT.md

<interfaces>
From src/features/outreach/lib/email-status.ts:
```typescript
export function canEmailTransition(from: string, to: string): boolean;
// validEmailTransitions.pending = ['generated', 'failed']  -> pending→generated is allowed
// 'approved' / 'drafted' / 'generated' do NOT allow → 'generated' (these are the 400 cases)
```

Current route (src/app/api/outreach-campaigns/[id]/emails/[emailId]/generation/route.ts):
- Zod: { generatedSubject: string().min(1).max(500), generatedBody: string().min(1) }
- Does an UPDATE...returning directly (no prior SELECT), sets generated* + generatedAt + updatedAt, NOT status
- Emits one timeline event: eventType 'outreach_email_generated'

Closest analog for the guard pattern — src/app/api/outreach-campaigns/[id]/emails/[emailId]/status/route.ts:
- SELECT email campaign-scoped → notFound('Email') if missing
- if (!canEmailTransition(email.status, newStatus)) return validationError(`Invalid transition: ${email.status} -> ${newStatus}`)
- then UPDATE...set({ status, updatedAt: new Date() })...where(and(eq(id), eq(campaignId)))

Route test harness — src/app/api/job-leads/[id]/status/route.test.ts:
- createTestDb() from @/test-utils/pglite, callRoute from @/test-utils/call-route
- vi.hoisted + Proxy db-mock pattern; params: { id, emailId }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gate the /generation write-back to also set status='generated'</name>
  <files>src/app/api/outreach-campaigns/[id]/emails/[emailId]/generation/route.ts</files>
  <read_first>
    - src/app/api/outreach-campaigns/[id]/emails/[emailId]/generation/route.ts (the file being edited)
    - src/app/api/outreach-campaigns/[id]/emails/[emailId]/status/route.ts (the SELECT→guard→UPDATE analog)
    - src/features/outreach/lib/email-status.ts (canEmailTransition + validEmailTransitions)
  </read_first>
  <action>
    Restructure the PATCH handler to mirror the SELECT→guard→UPDATE shape of the sibling `status/route.ts`, keeping the existing Zod schema (`generatedSubject`, `generatedBody`) unchanged. (1) Add a campaign-scoped SELECT of the email row by `(emailId, campaignId)` before the UPDATE; return `notFound('Email')` if absent. (2) Import `canEmailTransition` from `@/features/outreach/lib/email-status` and guard: if `!canEmailTransition(email.status, 'generated')` return `validationError(\`Invalid transition: ${email.status} -> generated\`)`. (3) In the single UPDATE `.set({...})`, ADD `status: 'generated'` alongside the existing `generatedSubject`, `generatedBody`, `generatedAt: new Date()`, and `updatedAt: new Date()` — one UPDATE, no second status call. (4) Keep the existing single `logTimeline({ eventType: 'outreach_email_generated', ... })` call exactly as one event. Preserve the existing ZodError → validationError and serverError catch arms. Do NOT reimplement the transition table; import it. The guard intentionally only permits the write-back when the email is in a `pending` state (the only state the machine allows → generated), which matches the skill's pending-only drain (Phase 12 D-05/D-06).
  </action>
  <acceptance_criteria>
    - `grep -c "canEmailTransition(" src/app/api/outreach-campaigns/[id]/emails/[emailId]/generation/route.ts` returns >= 1
    - `grep -c "status: 'generated'" src/app/api/outreach-campaigns/[id]/emails/[emailId]/generation/route.ts` returns >= 1
    - `grep -c "logTimeline(" src/app/api/outreach-campaigns/[id]/emails/[emailId]/generation/route.ts` returns exactly 1 (single timeline event)
    - `grep -c "from '@/features/outreach/lib/email-status'" src/app/api/outreach-campaigns/[id]/emails/[emailId]/generation/route.ts` returns 1
    - The Zod schema still validates `generatedSubject` and `generatedBody` only (no new request fields added)
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -c "generation/route" | grep -q '^0$' && echo "typecheck clean for route"</automated>
  </verify>
  <done>The route sets status='generated' via a canEmailTransition guard in the same UPDATE that writes content, returns 400 on a disallowed source state, 404 on missing email, and emits exactly one timeline event.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PGlite transition test for the /generation write-back</name>
  <files>src/app/api/outreach-campaigns/[id]/emails/[emailId]/generation/route.test.ts</files>
  <read_first>
    - src/app/api/job-leads/[id]/status/route.test.ts (PGlite + callRoute + vi.hoisted Proxy harness to copy)
    - src/app/api/outreach-campaigns/[id]/emails/[emailId]/generation/route.ts (the route under test, as edited in Task 1)
    - drizzle/schema/outreach-emails.ts and drizzle/schema/outreach-campaigns.ts (columns + FK shape for fixtures)
  </read_first>
  <behavior>
    - Test 1: a `pending` email PATCHed with `{ generatedSubject, generatedBody }` returns 200 and the returned row has `status: 'generated'`, non-null `generatedAt`, and the written subject/body.
    - Test 2: an email seeded in `approved` status PATCHed with valid content returns 400 with an "Invalid transition" error and its status is unchanged (still `approved`).
    - Test 3 (optional, if cheap): a non-existent emailId returns 404.
  </behavior>
  <action>
    Create the test mirroring `job-leads/[id]/status/route.test.ts`: `createTestDb()` from `@/test-utils/pglite`, `callRoute` from `@/test-utils/call-route`, and the `vi.hoisted` + Proxy `@/lib/db` mock. In `beforeEach`, seed one `outreach_campaigns` row (with a `goalInstruction`) and a linked `contacts` row, then insert `outreach_emails` fixtures. Import the route via `await import('@/app/api/outreach-campaigns/[id]/emails/[emailId]/generation/route')` and invoke `PATCH` with `params: { id: campaignId, emailId }`. Assert the three behaviors above against the `{ success, data }` envelope. Use the exact column names from `drizzle/schema/outreach-emails.ts`.
  </action>
  <acceptance_criteria>
    - `npm test -- src/app/api/outreach-campaigns` exits 0 with the new test file passing
    - The test asserts `status === 'generated'` on the pending→generated path
    - The test asserts a 400 / "Invalid transition" on the approved-source path and that status stays 'approved'
  </acceptance_criteria>
  <verify>
    <automated>npm test -- src/app/api/outreach-campaigns/\[id\]/emails/\[emailId\]/generation/route.test.ts</automated>
  </verify>
  <done>The route test passes: pending→generated returns 200 with status='generated'; a disallowed source state returns 400 and does not advance.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| skill/CLI → API | The skill sends untrusted generated content + a status transition across the REST boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Tampering | PATCH .../generation status write | mitigate | Status only moves via `canEmailTransition(email.status,'generated')`; disallowed source states return 400 — no unguarded status writes |
| T-16-02 | Elevation | /api/* auth | accept | Bearer-token + single-user gate in `src/proxy.ts` already covers every `/api/*` route; this edit adds no new auth surface |
| T-16-03 | Spoofing | campaign/email scoping | mitigate | UPDATE and SELECT both scoped to `(emailId, campaignId)` — an email cannot be advanced under the wrong campaign |
| T-16-SC | Tampering | npm/pip/cargo installs | accept | No package installs in Phase 16 — no new dependencies added |
</threat_model>

<verification>
- `npm test -- src/app/api/outreach-campaigns` passes (transition test green).
- `npx tsc --noEmit` reports no new errors in the edited route.
- The route imports (not reimplements) `canEmailTransition`.
</verification>

<success_criteria>
PATCH .../generation advances a pending email to `generated` with content written, rejects disallowed source states with 400, emits exactly one timeline event, and is pinned by a passing PGlite test.
</success_criteria>

<output>
Create `.planning/phases/16-email-generation-skill/16-01-SUMMARY.md` when done.
</output>
