---
phase: 01-critical-bug-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/user-avatar-profile.tsx
  - src/components/layout/app-sidebar.tsx
  - src/components/layout/user-nav.tsx
autonomous: false
requirements: [BUG-01, BUG-02]
tags: [bug-fix, hydration, clerk, react]

must_haves:
  decisions_addressed:
    - "D-01: Fix only the two known BUG-02 sites — no codebase-wide sweep (applied in Tasks 2, 3)"
    - "D-02: No grep/fix of other unsafe Clerk property chains (firstName, primaryEmailAddress, etc.) — out of scope this phase (Tasks 2, 3 action text explicit)"
    - "D-03: Verification is dev-server smoke only, not a 1500-contact import repro (Task 5 implements this exact protocol)"
    - "D-04: Do not synthesize a no-email Clerk user state to repro BUG-02 (no such task in plan; honored by omission)"
    - "D-05: No regression test in this phase — TEST-A3 owned by Phase 2 (no test task in plan; honored by omission)"
    - "D-06: Working tree contains partial BUG-01 fixes already (acknowledged in <interfaces> block; Task 1 audits them)"
    - "D-07: Audit-then-accept strategy for pre-existing edits (Task 1 is dedicated audit with concrete grep assertions)"
    - "D-08: Do NOT git restore the working-tree edits (Task 1 action and Task 5 resume-signal both call this out explicitly)"
    - "D-09: Surgical git add per file — only the three BUG files (Tasks 6, 7 use file-specific git add)"
    - "D-10: Leave Job Leads / package.json / docs/summary.md / .planning/config.json dirty and untouched (Tasks 6, 7 acceptance criteria verify with negative grep)"
    - "D-11: Do not stash or commit unrelated dirty changes (Tasks 6, 7 acceptance criteria assert git status still shows them dirty post-commit)"
  truths:
    - "Sidebar nav links route correctly on every dashboard page after a hard refresh"
    - "Loading /dashboard/overview produces no React hydration warnings in the browser console"
    - "Loading /dashboard/networking produces no React hydration warnings in the browser console"
    - "UserAvatarProfile renders inside SidebarMenuButton without {user && ...} guards"
    - "user.emailAddresses[0]?.emailAddress is the access pattern at every patched site (no unguarded [0] dereference)"
    - "Phase 1 commits touch only the three BUG files — Job Leads, package.json, docs/summary.md, .planning/config.json are untouched in this phase's git history"
  artifacts:
    - path: "src/components/user-avatar-profile.tsx"
      provides: "Span-based avatar profile with optional-chain guard on emailAddresses[0]"
      contains: "user?.emailAddresses[0]?.emailAddress"
    - path: "src/components/layout/app-sidebar.tsx"
      provides: "Unconditional UserAvatarProfile render at lines ~148 and ~164"
      contains: "user={user ?? null}"
    - path: "src/components/layout/user-nav.tsx"
      provides: "Optional-chain guard on emailAddresses[0]"
      contains: "user?.emailAddresses[0]?.emailAddress"
  key_links:
    - from: "src/components/layout/app-sidebar.tsx"
      to: "src/components/user-avatar-profile.tsx"
      via: "unconditional render with user={user ?? null}"
      pattern: "user=\\{user \\?\\? null\\}"
    - from: "src/components/user-avatar-profile.tsx"
      to: "browser DOM"
      via: "root <span> + inner showInfo <span> (valid inside <button>)"
      pattern: "<span className='flex items-center gap-2'>"
---

<objective>
Eliminate the React hydration crash (BUG-01) that breaks sidebar navigation after large LinkedIn imports, and guard the unsafe `emailAddresses[0]` array access (BUG-02) that can crash on edge-case Clerk users.

Per CONTEXT.md decisions D-06 and D-07, the working tree already contains partial fixes for BUG-01 — `user-avatar-profile.tsx` root is `<span>`, inner `showInfo` is `<span>`, and `app-sidebar.tsx` already removed the `{user && ...}` guards at lines 148 and 166, passing `user={user ?? null}` through. This plan **audits** those edits against `bug.md`'s prescription, applies the two remaining BUG-02 optional-chain guards, performs a dev-server smoke verification (D-03), and produces surgical commits scoped to the three BUG files only (D-09, D-10).

Purpose: Restore reliable dashboard navigation. The user is currently blocked from daily use after large LinkedIn imports.
Output: Two atomic commits (one per requirement, per D-12), each touching only the BUG-scoped files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-critical-bug-fix/01-CONTEXT.md
@bug.md
@.planning/codebase/CONCERNS.md
@CLAUDE.md

<interfaces>
<!-- Key contracts the executor needs to know. Extracted from src/components/user-avatar-profile.tsx. -->
<!-- Executor should use these directly — no codebase exploration needed. -->

From src/components/user-avatar-profile.tsx (current working-tree state):

```typescript
interface UserAvatarProfileProps {
  className?: string;
  showInfo?: boolean;
  user: {
    imageUrl?: string;
    fullName?: string | null;
    emailAddresses: Array<{ emailAddress: string }>;
  } | null;
}

export function UserAvatarProfile({
  className,
  showInfo = false,
  user
}: UserAvatarProfileProps);
```

Component **already accepts `user: ... | null`** and **already uses `<span>` at the root and inner `showInfo`**. The only remaining defect inside this file is line 31's `user?.emailAddresses[0].emailAddress` chain — the `[0]` is unguarded.

From src/components/layout/app-sidebar.tsx (current working-tree state):

- Line 52: `const { user } = useUser();` — Clerk hook, `user` is `User | null | undefined`
- Lines 148–152: `<UserAvatarProfile className='h-8 w-8 rounded-lg' showInfo user={user ?? null} />` — already unconditional, already passes `user ?? null`
- Lines 164–168: same — already unconditional, already passes `user ?? null`
- No `{user && ...}` JSX guards remain around `UserAvatarProfile`

From src/components/layout/user-nav.tsx (current working-tree state):

- Line 13: `const { user } = useUser();`
- Line 18: outer `if (user) { return ( ... ) }` guard — kept; this is an early return, not a JSX hydration mismatch
- Line 38: `{user.emailAddresses[0].emailAddress}` — unguarded `[0]` dereference inside the truthy branch; this is the BUG-02 site
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit pre-existing BUG-01 working-tree edits and confirm they match bug.md prescription</name>
  <files>src/components/user-avatar-profile.tsx, src/components/layout/app-sidebar.tsx</files>
  <read_first>
    - src/components/user-avatar-profile.tsx (full file — 38 lines)
    - src/components/layout/app-sidebar.tsx (full file, focus on lines 140–195)
    - bug.md (fix prescription)
    - .planning/phases/01-critical-bug-fix/01-CONTEXT.md (decisions D-06, D-07, D-08)
  </read_first>
  <action>
    Per D-07 (audit-then-accept), verify the working-tree state matches `bug.md`'s fix prescription before touching anything. Do NOT `git restore` (D-08).

    Checks to perform with grep against the current working-tree files (NOT the committed HEAD):

    1. In `src/components/user-avatar-profile.tsx`:
       - Root element at line 19 is `<span className='flex items-center gap-2'>` (NOT `<div>`).
       - Inner `showInfo` wrapper at line 28 is `<span className='grid flex-1 text-left text-sm leading-tight'>` (NOT `<div>`).
       - The closing tag on the final line matches the opening — `</span>` (not `</div>`).
       - `UserAvatarProfileProps.user` is typed as `{...} | null` (component handles null user).

    2. In `src/components/layout/app-sidebar.tsx`:
       - No `{user && <UserAvatarProfile ... />}` JSX pattern exists anywhere in the file (grep should return zero matches for `{user && <UserAvatarProfile`).
       - Both `UserAvatarProfile` render sites (around lines 148–152 inside `SidebarMenuButton` and lines 164–168 inside `DropdownMenuLabel`) pass `user={user ?? null}`.

    If ALL checks pass, mark the audit complete and proceed to the next task — the BUG-01 source edits are accepted as-is. If ANY check fails, fix the divergence in-place (apply the exact change from `bug.md`) before proceeding.

    Do NOT modify any other file. Do NOT `git restore`. Do NOT run `git add` yet.
  </action>
  <verify>
    <automated>grep -c "user && &lt;UserAvatarProfile" src/components/layout/app-sidebar.tsx | grep -q '^0$' && grep -q "&lt;span className='flex items-center gap-2'&gt;" src/components/user-avatar-profile.tsx && grep -q "user={user ?? null}" src/components/layout/app-sidebar.tsx && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '{user && <UserAvatarProfile' src/components/layout/app-sidebar.tsx` returns `0`
    - `grep -c "user={user ?? null}" src/components/layout/app-sidebar.tsx` returns `2` (both render sites)
    - `grep -q "<span className='flex items-center gap-2'>" src/components/user-avatar-profile.tsx` succeeds (root span)
    - `grep -q "<span className='grid flex-1 text-left text-sm leading-tight'>" src/components/user-avatar-profile.tsx` succeeds (inner showInfo span)
    - `grep -c "<div" src/components/user-avatar-profile.tsx` returns `0` (no divs remain in this file)
  </acceptance_criteria>
  <done>The working-tree state of `user-avatar-profile.tsx` and `app-sidebar.tsx` matches `bug.md`'s BUG-01 fix prescription verbatim, with zero divergence. No file has been re-edited unnecessarily.</done>
</task>

<task type="auto">
  <name>Task 2: Apply BUG-02 optional-chain guard at user-avatar-profile.tsx:31</name>
  <files>src/components/user-avatar-profile.tsx</files>
  <read_first>
    - src/components/user-avatar-profile.tsx (line 31 specifically — the `emailAddresses[0].emailAddress` access)
    - .planning/phases/01-critical-bug-fix/01-CONTEXT.md (decision D-01 — fix only the two known sites; do NOT do a codebase sweep per D-02)
  </read_first>
  <action>
    Per D-01, change the single line at `src/components/user-avatar-profile.tsx:31` from:

    `{user?.emailAddresses[0].emailAddress || ''}`

    to:

    `{user?.emailAddresses[0]?.emailAddress ?? ''}`

    Two changes on this one line:
    1. Add `?.` between `[0]` and `.emailAddress` (guards against `emailAddresses` being an empty array).
    2. Change the `||` fallback to `??` (per D-01 spec — nullish-coalescing is the prescribed operator; `||` would also coerce `0`/`false`/`""` to `''`, which is not the intent for an email string but is the prescribed pattern in CONTEXT.md and matches the BUG-02 acceptance criteria).

    Do NOT modify any other line in this file. Do NOT grep for or fix other unsafe Clerk property chains (D-02 — no codebase-wide sweep this phase).
  </action>
  <verify>
    <automated>grep -q "user?.emailAddresses\[0\]?.emailAddress ?? ''" src/components/user-avatar-profile.tsx &amp;&amp; ! grep -q "user?.emailAddresses\[0\]\.emailAddress" src/components/user-avatar-profile.tsx &amp;&amp; echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "user?.emailAddresses\[0\]?.emailAddress ?? ''" src/components/user-avatar-profile.tsx` succeeds
    - `grep -c "user?.emailAddresses\[0\]\.emailAddress" src/components/user-avatar-profile.tsx` returns `0` (the unguarded form is gone)
    - `git diff src/components/user-avatar-profile.tsx` shows exactly one line changed beyond the pre-existing BUG-01 span edits (the line 31 change)
    - No other file is modified by this task
  </acceptance_criteria>
  <done>Line 31 of `user-avatar-profile.tsx` reads `{user?.emailAddresses[0]?.emailAddress ?? ''}`. The change is mechanical and surgical — no other lines or files are touched.</done>
</task>

<task type="auto">
  <name>Task 3: Apply BUG-02 optional-chain guard at user-nav.tsx:38</name>
  <files>src/components/layout/user-nav.tsx</files>
  <read_first>
    - src/components/layout/user-nav.tsx (full file — 59 lines)
    - .planning/phases/01-critical-bug-fix/01-CONTEXT.md (decision D-01)
  </read_first>
  <action>
    Per D-01, change the single line at `src/components/layout/user-nav.tsx:38` from:

    `{user.emailAddresses[0].emailAddress}`

    to:

    `{user?.emailAddresses[0]?.emailAddress ?? ''}`

    Three changes on this one line:
    1. Add `?.` after `user` (defensive — the outer `if (user) { return ... }` at line 18 already guards this branch, but matching the prescribed pattern in CONTEXT.md keeps both BUG-02 sites identical).
    2. Add `?.` between `[0]` and `.emailAddress`.
    3. Add the `?? ''` fallback for the empty-emailAddresses-array case.

    Do NOT modify the outer `if (user) { return ... }` guard at line 18 — that is an early return for the falsy-user case, not a JSX hydration issue. Do NOT touch any other line. Do NOT touch any other file.
  </action>
  <verify>
    <automated>grep -q "user?.emailAddresses\[0\]?.emailAddress ?? ''" src/components/layout/user-nav.tsx &amp;&amp; ! grep -q "user\.emailAddresses\[0\]\.emailAddress" src/components/layout/user-nav.tsx &amp;&amp; echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "user?.emailAddresses\[0\]?.emailAddress ?? ''" src/components/layout/user-nav.tsx` succeeds
    - `grep -cE "user\.emailAddresses\[0\]\.emailAddress" src/components/layout/user-nav.tsx` returns `0` (the unguarded form is gone)
    - `git diff src/components/layout/user-nav.tsx` shows exactly one line changed
    - The `if (user) {` guard on line 18 is intact and unmodified
  </acceptance_criteria>
  <done>Line 38 of `user-nav.tsx` reads `{user?.emailAddresses[0]?.emailAddress ?? ''}`. The outer `if (user)` early return is preserved unchanged.</done>
</task>

<task type="auto">
  <name>Task 4: Build verification — confirm TypeScript compiles cleanly</name>
  <files>(none — verification task)</files>
  <read_first>
    - src/components/user-avatar-profile.tsx (post-edit)
    - src/components/layout/user-nav.tsx (post-edit)
    - src/components/layout/app-sidebar.tsx (working-tree state)
  </read_first>
  <action>
    Run `npx tsc --noEmit` to confirm the three modified files type-check cleanly with no errors.

    This is a sanity check that the optional-chain edits did not break any TypeScript inference (e.g., a stale `as string` assertion somewhere downstream).

    If tsc reports errors specifically in `user-avatar-profile.tsx`, `app-sidebar.tsx`, or `user-nav.tsx` (the three BUG-scoped files), fix them. If tsc reports unrelated errors in Job Leads scrape files or other dirty working-tree files, IGNORE them — those are out of scope per D-10.

    Do NOT run `npm run build` — that would also touch unrelated dirty files and is overkill for this phase. `tsc --noEmit` is sufficient.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&amp;1 | grep -E "(user-avatar-profile|app-sidebar|user-nav)\.tsx" | grep -v "^$" | wc -l | tr -d ' ' | grep -q '^0$' &amp;&amp; echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` produces zero errors referencing `user-avatar-profile.tsx`, `app-sidebar.tsx`, or `user-nav.tsx`
    - Errors in unrelated files (Job Leads scrapers, etc.) are acceptable per D-10 — they do NOT block this task
  </acceptance_criteria>
  <done>The three BUG-scoped files type-check cleanly. Unrelated tsc errors in dirty Job Leads files are recorded but ignored.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Dev-server smoke verification (D-03)</name>
  <files>(none — manual verification task; no file mutations)</files>
  <read_first>
    - .planning/phases/01-critical-bug-fix/01-CONTEXT.md (decision D-03 — dev-server smoke only, not a full 1500-contact repro)
    - bug.md (for the symptom list to scan the console against)
  </read_first>
  <action>
    Per D-03, this is a 2-minute manual smoke check. The executor automates the dev-server launch but the human verifies the result.

    Executor responsibilities (BEFORE handing off to the user):
    1. Start the dev server in a background task: `npm run dev`. (The project's `npm run dev` script binds to port 4000 — confirm by reading the "Ready" line in the server output.)
    2. Wait until the server prints "Ready" / "Local: http://localhost:4000" before printing the verification instructions to the user.
    3. If the dev server fails to start (TypeScript error, port conflict, missing env), surface the error and pause for resolution BEFORE asking the user to verify.

    Then print the verification steps below verbatim and pause for the resume signal.

    The user (steve@bronstein.org) verification steps:

    1. Open a Chromium browser and navigate to `http://localhost:4000/dashboard/overview`.
    2. Open DevTools → Console. **Expected: zero React hydration warnings or errors.** Specifically, no messages containing "Hydration", "did not match", "Text content does not match server-rendered HTML", or "Warning: Expected server HTML to contain..."
    3. Click each sidebar nav link in turn (Overview, Networking, Companies, Contacts, Applications, Pipeline, Tasks, Notes, Job Leads, Recruiters, Metrics, etc. — every link that appears in the sidebar). **Expected: each click changes the URL and renders the destination page without console errors.**
    4. Navigate to `http://localhost:4000/dashboard/networking`. **Expected: same — no hydration warnings, page renders, sidebar still works.**
    5. Click the user avatar in the sidebar footer to open the dropdown. **Expected: dropdown opens, displays full name + email (or empty string if Clerk user has no email), no console errors.**

    If any of the above fails, do NOT type "approved". Capture the console error text and the URL where it occurred, then describe the failure in the resume message. Per D-08, do NOT `git restore` — the diagnostic data is more valuable than rolling back.

    If all steps pass, the user types "approved" to continue. After the resume signal, the executor stops the dev server before proceeding to Task 6.
  </action>
  <what-built>
    BUG-01 hydration mismatch resolved (audited and accepted from working tree): `<span>`-based `UserAvatarProfile` renders unconditionally inside `SidebarMenuButton` with `user={user ?? null}`. BUG-02 optional-chain guards applied at the two known sites.
  </what-built>
  <how-to-verify>
    1. Executor starts `npm run dev` in background; waits for "Ready" on `http://localhost:4000`.
    2. User opens `http://localhost:4000/dashboard/overview`, opens DevTools console, confirms zero hydration warnings.
    3. User clicks every sidebar nav link in turn; confirms each routes correctly.
    4. User opens `http://localhost:4000/dashboard/networking`; same check.
    5. User opens the sidebar-footer user dropdown; confirms it opens cleanly.
  </how-to-verify>
  <verify>
    <automated>echo "MANUAL CHECKPOINT — see &lt;how-to-verify&gt;. Executor must wait for &apos;approved&apos; resume signal from user (steve@bronstein.org) before proceeding to Task 6."</automated>
  </verify>
  <acceptance_criteria>
    - User has loaded `/dashboard/overview` AND `/dashboard/networking` in a real Chromium browser
    - User has confirmed (via "approved" resume signal) that DevTools Console showed zero messages matching "Hydration", "did not match", "Text content does not match server-rendered HTML", or "Warning: Expected server HTML to contain..."
    - User has confirmed every sidebar nav link routes correctly with no console errors
    - User has confirmed the sidebar-footer user dropdown opens cleanly
    - Dev server has been stopped before the executor proceeds to Task 6
  </acceptance_criteria>
  <done>The user has typed "approved" after completing all five verification steps with no hydration warnings or routing failures. The dev server is stopped.</done>
  <resume-signal>Type "approved" if no hydration warnings appear on `/dashboard/overview` and `/dashboard/networking`, all sidebar nav links route correctly, and the user dropdown opens cleanly. Otherwise paste the console error text and the URL that produced it.</resume-signal>
</task>

<task type="auto">
  <name>Task 6: Commit BUG-01 — span migration + unconditional UserAvatarProfile render</name>
  <files>src/components/user-avatar-profile.tsx, src/components/layout/app-sidebar.tsx</files>
  <read_first>
    - .planning/phases/01-critical-bug-fix/01-CONTEXT.md (decisions D-09, D-10, D-11, D-12)
    - `git status` output (verify the dirty-tree boundary)
  </read_first>
  <action>
    Per D-12, produce one commit per requirement for clean revertability. This task handles BUG-01.

    **The complication:** `src/components/user-avatar-profile.tsx` contains BOTH BUG-01 changes (the `<span>` migration on lines 19, 28, 33, 35) AND the BUG-02 change (line 31 optional-chain). To commit BUG-01 in isolation, use `git add -p` (interactive hunk staging) to stage only the `<span>` hunks and skip the line-31 hunk.

    **Executor's discretion (per CONTEXT.md):** if `git add -p` is impractical in this environment, the acceptable fallback is to combine BUG-01 + BUG-02 into a single commit. State the choice explicitly in the commit message footer.

    **Preferred path (two commits):**

    1. Verify the dirty-tree boundary first (D-10). Run `git status` and confirm these files are dirty but will be left untouched:
       - `src/features/job-leads/lib/linkedin-browser.ts`
       - `src/features/job-leads/lib/scrape-connections.ts`
       - `src/features/job-leads/lib/scrape-job-page.ts`
       - `src/app/api/job-leads/[id]/search/route.ts`
       - `package.json`, `package-lock.json`
       - `docs/summary.md`, `.planning/config.json`

       Do NOT `git add` any of these.

    2. `git add -p src/components/user-avatar-profile.tsx`
       - Stage the `<span>` hunks (root `<span>`, inner `showInfo` `<span>`, closing tag fixes).
       - **Skip** the hunk containing the line-31 `?.emailAddress ?? ''` change (that is BUG-02, committed separately in Task 7).

    3. `git add src/components/layout/app-sidebar.tsx`
       - The whole file's diff is BUG-01 (unconditional render + `user ?? null` passthrough); stage in full.

    4. Verify the staged set with `git diff --cached --stat` — exactly two files, no Job Leads / package.json / docs entries.

    5. Commit with message:

       ```
       fix(BUG-01): eliminate hydration crash in app-sidebar UserAvatarProfile

       Remove the `{user && ...}` conditional guard around UserAvatarProfile in
       app-sidebar.tsx (lines 148, 166) — useUser() returns undefined on the
       server and a user object on the client, causing an SSR/CSR HTML mismatch
       that crashes React's hydration and kills sidebar navigation after large
       LinkedIn imports.

       UserAvatarProfile now renders unconditionally with `user={user ?? null}`;
       the component already handles a null user via optional chaining.

       Also migrate UserAvatarProfile's root and inner `showInfo` wrappers from
       <div> to <span> so the component renders valid HTML inside a <button>.

       Refs: BUG-01, .planning/phases/01-critical-bug-fix/01-CONTEXT.md (D-06, D-07).
       ```

    **Fallback path (single combined commit, if `git add -p` fails or is unavailable):**

    - `git add src/components/user-avatar-profile.tsx src/components/layout/app-sidebar.tsx src/components/layout/user-nav.tsx`
    - Commit with a combined message referencing both BUG-01 and BUG-02.
    - **Skip Task 7** in this case.

    Critical: per D-11, do NOT stash, do NOT commit, do NOT touch the unrelated dirty files. Only the BUG-scoped files leave the working tree as committed changes.
  </action>
  <verify>
    <automated>git log -1 --name-only | grep -E "user-avatar-profile|app-sidebar" &amp;&amp; ! git log -1 --name-only | grep -E "job-leads|package|docs/summary|.planning/config" &amp;&amp; echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - The latest commit's file list contains `src/components/user-avatar-profile.tsx` and `src/components/layout/app-sidebar.tsx`
    - The latest commit's file list does NOT contain any path matching `job-leads`, `package.json`, `package-lock.json`, `docs/summary.md`, or `.planning/config.json`
    - `git status` after the commit still shows the Job Leads / package / docs files as dirty (untouched, per D-11)
    - The commit message subject starts with `fix(BUG-01):` (or, in the fallback path, `fix(BUG-01,BUG-02):` and Task 7 is skipped)
  </acceptance_criteria>
  <done>BUG-01 is committed as an isolated change touching only `user-avatar-profile.tsx` (span-migration hunks) and `app-sidebar.tsx`. The dirty-tree boundary is intact.</done>
</task>

<task type="auto">
  <name>Task 7: Commit BUG-02 — optional-chain guards on emailAddresses[0]</name>
  <files>src/components/user-avatar-profile.tsx, src/components/layout/user-nav.tsx</files>
  <read_first>
    - .planning/phases/01-critical-bug-fix/01-CONTEXT.md (decisions D-09, D-12)
    - `git status` output
  </read_first>
  <action>
    Skip this task entirely if Task 6 took the fallback "single combined commit" path (the commit message will then reference both BUG-01 and BUG-02 and BUG-02 is already committed).

    Otherwise, finish the BUG-02 commit:

    1. `git add src/components/user-avatar-profile.tsx` — the remaining unstaged hunk is the line-31 optional-chain change.
    2. `git add src/components/layout/user-nav.tsx` — the entire diff is BUG-02 (line-38 optional-chain change).
    3. Verify the staged set with `git diff --cached --stat` — exactly two files.
    4. Confirm `git status` still shows Job Leads / package / docs files as dirty and uncommitted (D-11).
    5. Commit with message:

       ```
       fix(BUG-02): guard emailAddresses[0] access against empty array

       Clerk users with SSO-only sign-in can have an empty emailAddresses
       array. The existing `user?.emailAddresses[0].emailAddress` pattern
       guards the user object but not the array index, throwing at runtime
       when accessing `.emailAddress` on undefined.

       Replace with `user?.emailAddresses[0]?.emailAddress ?? ''` at:
       - src/components/user-avatar-profile.tsx:31
       - src/components/layout/user-nav.tsx:38

       Refs: BUG-02, .planning/phases/01-critical-bug-fix/01-CONTEXT.md (D-01).
       ```
  </action>
  <verify>
    <automated>git log -1 --name-only | grep -E "user-avatar-profile|user-nav" &amp;&amp; ! git log -1 --name-only | grep -E "job-leads|package|docs/summary|.planning/config" &amp;&amp; echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - The latest commit's file list contains `src/components/user-avatar-profile.tsx` and `src/components/layout/user-nav.tsx`
    - The latest commit's file list does NOT contain `app-sidebar.tsx` (that committed in Task 6) or any Job Leads / package / docs file
    - `git status` after the commit shows zero dirty files among the three BUG-scoped paths
    - `git status` after the commit STILL shows the Job Leads / package / docs files as dirty (untouched, per D-11)
    - The commit message subject starts with `fix(BUG-02):`
  </acceptance_criteria>
  <done>BUG-02 is committed as an isolated change touching only `user-avatar-profile.tsx` (line-31 hunk) and `user-nav.tsx`. The dirty-tree boundary remains intact. The phase is complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Clerk → React tree | Clerk's `useUser()` hook crosses SSR/CSR; user data can be null/undefined |
| Clerk user record → UI render | `emailAddresses` can be an empty array for SSO-only users |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Denial of Service | `app-sidebar.tsx` hydration | mitigate | Remove `{user && ...}` JSX guard so SSR and CSR render identical HTML; the entire app's sidebar navigation depends on this not crashing |
| T-01-02 | Denial of Service | `user-avatar-profile.tsx:31`, `user-nav.tsx:38` | mitigate | Add `?.` between `[0]` and `.emailAddress` to prevent runtime TypeError on empty `emailAddresses` array |
| T-01-03 | Information Disclosure | Component renders email | accept | Email is the signed-in user's own email displayed in their own dashboard; single-user app. No PII exposure to third parties. |
| T-01-04 | Tampering | Working-tree edits committed without audit | mitigate | D-07 audit-then-accept: Task 1 explicitly verifies the pre-existing edits match `bug.md` before they're committed. Surgical `git add` (D-09) prevents accidentally committing unrelated dirty files. |
</threat_model>

<verification>
## Phase-level checks

1. After Task 1 (audit): `grep -c '{user && <UserAvatarProfile' src/components/layout/app-sidebar.tsx` returns `0`.
2. After Task 2: `grep -q "user?.emailAddresses\[0\]?.emailAddress ?? ''" src/components/user-avatar-profile.tsx` succeeds.
3. After Task 3: `grep -q "user?.emailAddresses\[0\]?.emailAddress ?? ''" src/components/layout/user-nav.tsx` succeeds.
4. After Task 4: `npx tsc --noEmit` produces zero errors in the three BUG-scoped files.
5. After Task 5: human-verified — no hydration warnings on `/dashboard/overview` and `/dashboard/networking`; all sidebar nav links route correctly; user dropdown opens cleanly.
6. After Tasks 6 + 7: `git log -2 --name-only` shows exactly the three BUG-scoped files across the last two commits and zero Job Leads / package.json / docs files. `git status` still shows the unrelated dirty files as uncommitted.
</verification>

<success_criteria>
- All three BUG-scoped files modified to match `bug.md`'s prescription and D-01's BUG-02 optional-chain pattern.
- `/dashboard/overview` and `/dashboard/networking` load with zero React hydration warnings in DevTools console (D-03 verification).
- Every sidebar nav link routes correctly after a hard refresh.
- Two atomic commits (or one combined commit, per executor discretion D-12) exist on `main`, each scoped exclusively to the BUG files.
- `git status` shows Job Leads scrape files, `package.json`, `package-lock.json`, `docs/summary.md`, and `.planning/config.json` still dirty and untouched (D-10, D-11).
- ROADMAP.md updated to mark Phase 1 complete (handled by execute-phase, not this plan).
</success_criteria>

<output>
After completion, create `.planning/phases/01-critical-bug-fix/01-01-SUMMARY.md` recording:
- The audit outcome from Task 1 (whether pre-existing edits matched `bug.md` verbatim or required correction).
- The two commit SHAs (or the single combined SHA, if the fallback path was taken).
- The list of dirty files left untouched (Job Leads + package + docs), confirming the D-10 boundary held.
- Confirmation that no hydration warnings appeared during the Task 5 smoke check.
</output>
