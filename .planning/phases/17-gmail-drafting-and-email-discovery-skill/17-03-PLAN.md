---
phase: 17-gmail-drafting-and-email-discovery-skill
plan: 03
type: execute
wave: 2
depends_on: ["17-01", "17-02"]
files_modified:
  - .claude/skills/draft-outreach-emails/SKILL.md
autonomous: true
requirements: [DISC-01, DISC-02, DISC-03, DRFT-01, DRFT-02, DRFT-03, DRFT-04]

must_haves:
  truths:
    - "Running `draft-outreach-emails <campaign-id>` reads the campaign's approved emails (paginated), confirms count up front, discovers missing recipients via Gmail thread search, creates a Gmail draft per recipient'd email, and writes back via the /draft route"
    - "Discovery accepts an address ONLY when the contact was a confirmed direct thread participant matched by name; exactly one distinct address → accept; ≥2 → ambiguous (recipientEmail left unset, listed in summary); none → channel=linkedin_message"
    - "Re-running is idempotent: drafted emails never reappear in the approved queue (skip); an edited-after-draft email returns as approved+stale gmailDraftId and is re-drafted by create+repoint"
    - "The skill contains zero send-family Gmail calls; the allowlist and grep gate are present and verifiable"
  artifacts:
    - path: ".claude/skills/draft-outreach-emails/SKILL.md"
      provides: "Discover-then-draft batch workflow, idempotency, send-safety allowlist + grep gate, run summary"
      min_lines: 200
      contains: "create_draft"
  key_links:
    - from: ".claude/skills/draft-outreach-emails/SKILL.md"
      to: ".claude/skills/draft-outreach-emails/references/heimdall-api.md"
      via: "references the API contract"
      pattern: "heimdall-api"
    - from: ".claude/skills/draft-outreach-emails/SKILL.md"
      to: "PATCH .../draft"
      via: "one call per email after create_draft"
      pattern: "/draft"
---

<objective>
Author `SKILL.md` for `draft-outreach-emails` — the discover-then-draft batch workflow. It reads the campaign's approved emails, gates on an upfront count confirm, discovers missing recipient addresses from Gmail thread history (strict D-03 accept rule), creates Gmail drafts (never sends — D-06), writes back via the Plan-17-01 `/draft` route, flags LinkedIn-only contacts, and prints an end-of-run summary. Mirrors the sibling `generate-outreach-emails/SKILL.md` structure.

Purpose: This is the user-facing deliverable — the skill that turns approved emails into Gmail drafts and discovers missing addresses. It depends on the 17-02 reference contract and assumes the 17-01 `/draft` route behavior (one call sets status + contact + timeline).
Output: `SKILL.md` + its sibling HTML companion + refreshed `_index.html`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-gmail-drafting-and-email-discovery-skill/17-CONTEXT.md
@.planning/phases/17-gmail-drafting-and-email-discovery-skill/17-RESEARCH.md
@.planning/phases/17-gmail-drafting-and-email-discovery-skill/17-PATTERNS.md
@.claude/skills/draft-outreach-emails/references/heimdall-api.md

<interfaces>
<!-- Locked behavior the workflow must encode. Sourced from 17-CONTEXT.md (D-01..D-06) + 17-RESEARCH.md. -->

Invocation (D-05): `draft-outreach-emails <campaign-id>` — batch-only, discover-then-draft in ONE run. NO --discover-only / --draft-only flags.

allowed-tools (D-06 allowlist — Gmail): mcp__gmail__search_threads, mcp__gmail__get_thread,
  mcp__gmail__create_draft, mcp__gmail__list_drafts. Plus Read, Bash. NO send/trash/update/delete tool exists or is allowed.

Discovery accept rule (D-03, LOCKED): query search_threads with the contact's full name → for each thread
  get_thread → walk messages[].payload.headers (From/To/Cc) → parse "Display Name <email>" → strip
  steve@bronstein.org → keep only addresses whose display name case-insensitively matches the contact's
  first+last (Pitfall 3) → dedupe by lowercased address (Pitfall 4). Then:
    exactly 1 distinct → PATCH .../recipient {channel:'email', recipientEmail}; email joins the drafting queue this run.
    ≥2 distinct → AMBIGUOUS: leave recipientEmail UNSET (do NOT force linkedin_message), list contact+candidates in summary, skip drafting.
    0 → PATCH .../recipient {channel:'linkedin_message'} (route nulls recipientEmail); list in linkedin-fallback.

Idempotency (D-02 / DRFT-03): the approved queue NEVER contains status='drafted' (already skipped before the loop).
  Per drafting item: gmailDraftId IS NULL → new draft. gmailDraftId IS NOT NULL (edited-after-draft → returned to
  approved with stale id) → create a FRESH draft + repoint via /draft (no update_draft tool exists; old draft left
  harmless, never sent).

Draft content (Pitfall 2): subject = editedSubject ?? generatedSubject; body = editedBody ?? generatedBody.
  If both null → skip + report (same as archived edge case).

Per-email failure (discretion): approved→failed is ILLEGAL. On a draft failure of an approved email do NOT call
  /status; leave it approved, add to failed list, continue. (Only call /status {failed,lastError} for a
  non-approved email, edge case.)

Archived contact (discretion): contact.archivedAt != null (already in the work-queue item) → skip drafting + report.

Section map to mirror (17-PATTERNS.md lines 175-185): Overview, Setup (+Gmail-MCP check), Step 1 read queue
  (pagination loop), Step 2 confirm gate (N approved / M missing recipient), Step 3 discovery loop, Step 4 drafting
  loop, Step 5 end-of-run summary, Constraints (allowlist + grep gate). 17-PATTERNS.md gives verbatim copy blocks
  for Setup, Confirm gate, Summary, Constraints.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write SKILL.md frontmatter, setup, queue read, and confirm gate</name>
  <files>.claude/skills/draft-outreach-emails/SKILL.md</files>
  <read_first>
    - .claude/skills/generate-outreach-emails/SKILL.md (structural template — frontmatter, Setup verbatim block, confirm-gate pattern, Constraints)
    - .claude/skills/draft-outreach-emails/references/heimdall-api.md (the contract written in 17-02 — work-queue read + endpoints)
    - 17-PATTERNS.md §"SKILL.md" (frontmatter shape, Setup/Confirm-gate verbatim blocks, section map)
    - 17-RESEARCH.md §"Pattern 1: Work-Queue Read with Cursor Pagination" and Pitfall 6
  </read_first>
  <action>
    Create `.claude/skills/draft-outreach-emails/SKILL.md`. Frontmatter: `name: draft-outreach-emails`; `description` = discover missing recipient addresses via Gmail MCP and create Gmail drafts for approved emails, write back via REST, discover-then-draft in one batch run; `argument-hint: '<campaign-id>'`; `allowed-tools` listing ONLY `Read`, `Bash`, `mcp__gmail__search_threads`, `mcp__gmail__get_thread`, `mcp__gmail__create_draft`, `mcp__gmail__list_drafts` (D-06 allowlist — no send/trash/update tool). Then write: `## Overview` (one-paragraph discover-then-draft, REST-only, never-sends summary). `## Setup` — copy the sibling's verification block verbatim (jq, token file, API_TOKEN_HASH, SINGLE_USER_EMAIL, dev server on :4000) and ADD a Gmail-MCP availability check note (the skill requires an interactive Claude Code session; confirm search_threads/create_draft are in scope before proceeding; do NOT auto-fix). `## Step 1: Read the approved queue` — the cursor-pagination loop from 17-RESEARCH Pattern 1 (`?status=approved&limit=100`, loop until `meta.hasMore=false`, thread the cursor); then partition the collected items into DISCOVERY queue (recipientEmail null/empty) and DRAFTING queue (recipientEmail set). `## Step 2: Confirm gate (D-05)` — report `N approved emails / M missing a recipient address`, ask "Proceed with discover-then-draft? (yes/no)", wait for explicit confirmation; if N=0 report "No approved emails — run complete" and exit cleanly.
  </action>
  <verify>
    <automated>test -f .claude/skills/draft-outreach-emails/SKILL.md && grep -q "argument-hint" .claude/skills/draft-outreach-emails/SKILL.md && grep -q "status=approved" .claude/skills/draft-outreach-emails/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - `SKILL.md` exists with YAML frontmatter; `grep -q "name: draft-outreach-emails" SKILL.md`
    - allowed-tools lists EXACTLY the four Gmail tools `search_threads, get_thread, create_draft, list_drafts` plus Read+Bash: `grep -q "mcp__gmail__create_draft" SKILL.md` AND `! grep -qiE "mcp__gmail__(send|trash|delete|update_draft|import|modify)" SKILL.md`
    - `grep -q "limit=100" SKILL.md` and `grep -q "hasMore" SKILL.md` (pagination loop present — Pitfall 6)
    - `grep -qiE "missing.*recipient|missing a recipient" SKILL.md` (confirm gate reports N approved / M missing)
    - `grep -q "cat ~/.heimdall/api-token" SKILL.md` (inline-token convention)
  </acceptance_criteria>
  <done>SKILL.md opens with the correct frontmatter+allowlist, a setup gate (incl. Gmail-MCP check), a paginated approved-queue read partitioned into discovery/drafting queues, and an upfront count-confirm gate.</done>
</task>

<task type="auto">
  <name>Task 2: Write the discovery loop, drafting loop, summary, and send-safety constraints</name>
  <files>.claude/skills/draft-outreach-emails/SKILL.md</files>
  <read_first>
    - .claude/skills/generate-outreach-emails/SKILL.md (mark-failed-and-continue, end-of-run summary, Constraints blocks)
    - .claude/skills/draft-outreach-emails/references/heimdall-api.md (recipient, draft, status endpoint bodies)
    - 17-RESEARCH.md §"Pattern 3" (search_threads/get_thread extraction + header parsing), §"Pattern 4" (create_draft), §"Pattern 5" (idempotency), and Pitfalls 1-5
    - 17-PATTERNS.md §"Failure handling", §"End-of-run summary", §"Constraints" (verbatim blocks + the approved→failed CRITICAL DIFFERENCE)
    - src/features/outreach/lib/review-helpers.ts (finalSubject/finalBody precedence; needsLinkedinMessage)
  </read_first>
  <action>
    Append to `SKILL.md`: `## Step 3: Discovery loop (DISC-01 / D-03 / D-04 / D-04b)` — for each DISCOVERY-queue item: call `mcp__gmail__search_threads` with the contact's full name (note params/response are [ASSUMED] — validate the exact shape at run time per Assumptions A1-A3; cap at ~20 most-recent threads); for each thread `mcp__gmail__get_thread`, walk `messages[].payload.headers` (From/To/Cc), parse `Display Name <email>` (sed angle-bracket extraction, lowercase), strip `steve@bronstein.org`, KEEP only addresses whose display name case-insensitively matches `firstName + ' ' + lastName` (Pitfall 3), dedupe by lowercased address (Pitfall 4). Apply the LOCKED accept rule: 1 distinct → `PATCH .../recipient {channel:'email', recipientEmail}` then move the item into the drafting queue for this run; ≥2 distinct → AMBIGUOUS: leave recipientEmail unset, add `{contact, candidates}` to the ambiguous list, do NOT draft, do NOT force linkedin_message; 0 → `PATCH .../recipient {channel:'linkedin_message'}` and add to linkedin-fallback list. Use `jq -n --arg` for every PATCH body. `## Step 4: Drafting loop (DRFT-01 / D-02 idempotency)` — for each DRAFTING-queue item: skip if `contact.archivedAt != null` (report); resolve `subject = editedSubject ?? generatedSubject`, `body = editedBody ?? generatedBody`, skip+report if both null (Pitfall 2); decide new-vs-redraft from `gmailDraftId` (null → new; non-null → create fresh + repoint, old draft left harmless — D-02); call `mcp__gmail__create_draft {to: recipientEmail, subject, body}` ([ASSUMED] shape — validate per A4, read the returned `id`); then ONE `PATCH .../draft {gmailDraftId:<new id>}` (the 17-01 route sets status/contact/timeline). On a draft failure of an approved email: do NOT call `/status` (approved→failed illegal — Pitfall 1); add to failed list and continue. Each write-back must be durable before the next item so an interrupted run just re-queues the rest. `## Step 5: End-of-run summary` — mirror the sibling summary with counters drafted / skipped-already-drafted / discovered / ambiguous / linkedin-fallback / failed, plus the ambiguous-candidate list (contact + addresses) and the failed list (left as approved). `## Constraints` — copy the sibling REST-only + never-log-token blocks and ADD: Gmail tool allowlist (the four tools), the pre-run grep gate (`grep -ri "send" .claude/skills/draft-outreach-emails/` → confirm zero send-family calls — D-06), never-sends hard invariant, batch-only (D-05), no DB columns for transient state (D-04). Then refresh the sibling HTML companion (`node ~/.claude/scripts/md-to-html.mjs <file>`) and regenerate `_index.html` (`node ~/.claude/scripts/build-index.mjs`).
  </action>
  <verify>
    <automated>grep -q "create_draft" .claude/skills/draft-outreach-emails/SKILL.md && grep -q "linkedin_message" .claude/skills/draft-outreach-emails/SKILL.md && ! grep -riE "mcp__gmail__(send|drafts_send|trash|delete|update_draft)" .claude/skills/draft-outreach-emails/</automated>
  </verify>
  <acceptance_criteria>
    - DISC-01: `grep -q "search_threads" SKILL.md` and `grep -q "get_thread" SKILL.md` (Gmail thread discovery present)
    - D-03 accept rule encoded: `grep -qiE "one distinct|exactly one|1 distinct" SKILL.md` AND `grep -qi "ambiguous" SKILL.md` AND `grep -qi "linkedin_message" SKILL.md`
    - DISC-02: ambiguous path leaves recipient unset and lists candidates — `grep -qiE "leave.*unset|do not.*guess|recipientEmail.*unset" SKILL.md`
    - DRFT-01: `grep -q "create_draft" SKILL.md`; DRFT-03 idempotency: `grep -qiE "repoint|fresh draft|already drafted|skip" SKILL.md`
    - DRFT-02 send-safety: `grep -qi "grep" SKILL.md` (the grep gate is documented) AND `! grep -riE "mcp__gmail__(send|drafts_send|trash|delete|update_draft|import|modify)" .claude/skills/draft-outreach-emails/` returns zero matches across the whole skill dir
    - Pitfall 1: `grep -qiE "approved.*failed|not call.*status|leave.*approved" SKILL.md` (approved→failed avoided)
    - Pitfall 2: `grep -qE "editedSubject \?\? generatedSubject|editedBody \?\? generatedBody|finalSubject|finalBody" SKILL.md`
    - Summary counters present: `grep -qi "ambiguous" SKILL.md` and `grep -qiE "linkedin.?fallback|linkedin fallback" SKILL.md` and `grep -qi "failed" SKILL.md`
    - Sibling `SKILL.html` exists in the skill dir; repo-root `_index.html` regenerated
  </acceptance_criteria>
  <done>SKILL.md fully encodes the strict D-03 discovery accept rule, the ambiguous/LinkedIn branches, idempotent create-and-repoint drafting via one /draft call, the approved→failed avoidance, the run summary, and the D-06 allowlist + grep gate — with zero send-family Gmail tool references anywhere in the skill directory.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Skill (CLI session) → Gmail MCP | Reads thread contents (PII: third-party email addresses) |
| Skill → Heimdall REST | Bearer-authenticated writes (recipient, draft, status) |
| Skill → terminal output / shell history | Token and PII could leak if mishandled |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-07 | Elevation of Privilege / Tampering | accidental send-family Gmail call | mitigate | D-06 allowed-tools allowlist (four read/draft tools only); mandatory pre-run grep gate; connected MCP exposes no send tool; acceptance criterion greps the whole skill dir for send-family tool names → must be zero |
| T-17-08 | Information Disclosure | discovered third-party email addresses (PII) | mitigate | addresses written ONLY via `PATCH .../recipient`; ambiguous addresses live only in the ephemeral run summary (no `candidates` column — D-04); never written to logs or other endpoints |
| T-17-09 | Information Disclosure | wrong-recipient draft (false-positive discovery) | mitigate | strict D-03 accept rule: only a confirmed direct thread participant matched by name; exactly one distinct address; ambiguous → no guess; archived contact skipped |
| T-17-10 | Information Disclosure | bearer token in output / shell history | mitigate | `$(cat ~/.heimdall/api-token)` inline per curl; Constraints "never log the token"; acceptance forbids literal token strings |
| T-17-11 | Tampering | illegal approved→failed transition aborting a run | mitigate | skill does NOT call `/status` for an approved email that fails to draft (Pitfall 1); leaves it approved, reports in summary |
| T-17-SC | Tampering | npm/pip/cargo installs | accept | no package installs in this phase |
</threat_model>

<verification>
- All Task 1 + Task 2 grep acceptance criteria pass.
- `! grep -riE "mcp__gmail__(send|drafts_send|trash|delete|update_draft|import|modify)" .claude/skills/draft-outreach-emails/` returns zero matches (DRFT-02 / D-06, the load-bearing safety gate).
- Section structure mirrors the sibling skill; HTML companion + `_index.html` refreshed.
- Phase-gate manual smoke (human, post-merge): run `draft-outreach-emails <real campaign>` against a campaign with (a) an approved email with a stored recipient → draft appears in Gmail + email goes `drafted` + contact `reached_out`; (b) an approved email on a contact with no email but a known direct Gmail thread → address discovered + drafted; (c) a contact with no threads → channel set linkedin_message + badge appears; (d) a second run creates no duplicate drafts.
</verification>

<success_criteria>
- Running `draft-outreach-emails <campaign-id>` performs discover-then-draft in one batch with an upfront count confirm.
- Discovery uses the strict D-03 accept rule; ambiguous → summary (recipient unset); none → linkedin_message (DISC-01, DISC-02, DISC-03).
- A Gmail draft is created per recipient'd approved email and written back via one `/draft` call; re-runs are idempotent (DRFT-01, DRFT-03, DRFT-04).
- Zero send-family Gmail calls anywhere in the skill directory; allowlist + grep gate documented (DRFT-02).
</success_criteria>

<output>
Create `.planning/phases/17-gmail-drafting-and-email-discovery-skill/17-03-SUMMARY.md` when done.
</output>
