---
phase: 11-schema-enums-and-state-machine
plan: 01
subsystem: database-schema
tags: [drizzle, schema, enums, postgres, migration, outreach]
dependency_graph:
  requires: []
  provides:
    - outreachCampaigns table (drizzle schema)
    - outreachEmails table with unique constraint + 3 indexes (drizzle schema)
    - outreachCampaignStatusEnum, outreachChannelEnum, outreachEmailStatusEnum (pgEnums)
    - 0013_outreach_campaigns.sql migration (generated, committed — pending apply to Neon)
  affects:
    - drizzle/schema/index.ts (barrel exports added)
tech_stack:
  added:
    - outreach_campaign_status Postgres enum type
    - outreach_channel Postgres enum type
    - outreach_email_status Postgres enum type
  patterns:
    - pgTable with third-arg unique + index array (mirrors prospectBridges pattern)
    - FK references with .references(() => table.id).notNull()
    - Failure tracking columns (lastError + lastErrorAt) mirroring job-leads.ts
    - Soft delete via archivedAt (campaigns only; emails omitted per spec)
key_files:
  created:
    - drizzle/schema/outreach-campaigns.ts
    - drizzle/schema/outreach-emails.ts
    - drizzle/migrations/0013_outreach_campaigns.sql
    - drizzle/migrations/meta/0013_snapshot.json
  modified:
    - drizzle/schema/enums.ts
    - drizzle/schema/index.ts
    - drizzle/migrations/meta/_journal.json
decisions:
  - outreach_channel enum landed in this migration (D-08) — not deferred to Phase 17
  - outreach_emails has no archivedAt — emails soft-delete through their parent campaign
  - channel default is 'email'; linkedin_message is a channel value not a status (D-07)
  - UNIQUE (campaign_id, contact_id) backs Phase 12 onConflictDoNothing() bulk add (T-11-01)
  - Migration auto-numbered 0013 by drizzle-kit (not 0011 as stale CONTEXT.md said)
metrics:
  duration: ~8 min
  completed: 2026-06-20
  tasks_completed: 1.5 of 2 (Task 2 migration generated+committed; apply to Neon pending)
  files_changed: 7
---

# Phase 11 Plan 01: Schema Enums and Outreach Tables Summary

**One-liner:** Three outreach pgEnums and two tables (`outreach_campaigns`, `outreach_emails`) added to Drizzle schema with unique constraint, 3 indexes, and migration 0013 generated and committed — pending `db:migrate` apply to live Neon.

## Tasks Completed

| Task | Status | Commit |
|------|--------|--------|
| Task 1: Three enums, two tables, barrel exports | DONE | d6edc05 |
| Task 2: Generate migration 0013_outreach_campaigns.sql | DONE | af92a6b |
| Task 2: Apply migration to live Neon DB (`npm run db:migrate`) | PENDING HUMAN ACTION | — |

## What Was Built

### Schema Files

**`drizzle/schema/enums.ts`** — Appended 3 pgEnums after `contactEnrichmentStatusEnum`:
- `outreachCampaignStatusEnum` → `pgEnum('outreach_campaign_status', ['draft', 'active', 'completed'])`
- `outreachChannelEnum` → `pgEnum('outreach_channel', ['email', 'linkedin_message'])`
- `outreachEmailStatusEnum` → `pgEnum('outreach_email_status', ['pending', 'generated', 'edited', 'approved', 'drafted', 'failed'])`

**`drizzle/schema/outreach-campaigns.ts`** (new) — `outreach_campaigns` table with `id`, `name`, `goal_instruction`, `status` (enum default `'draft'`), `created_at`, `updated_at`, `archived_at`.

**`drizzle/schema/outreach-emails.ts`** (new) — `outreach_emails` table with:
- FKs: `campaign_id → outreach_campaigns.id`, `contact_id → contacts.id`
- Channel: `outreach_channel` enum default `'email'`, NOT NULL
- Content: `generated_subject/body` + `edited_subject/body` (nullable; final = `editedX ?? generatedX`)
- Status: `outreach_email_status` enum default `'pending'`, NOT NULL
- Gmail linkage: `gmail_draft_id` (nullable, Phase 17)
- Failure tracking: `last_error`, `last_error_at` (timezone, mirrors `job-leads.ts:36-38`)
- Lifecycle timestamps: `generated_at`, `approved_at`, `drafted_at` (timezone, nullable)
- `UNIQUE (campaign_id, contact_id)` constraint
- 3 indexes: `outreach_emails_campaign_id_idx`, `outreach_emails_status_idx`, `outreach_emails_contact_id_idx`

**`drizzle/schema/index.ts`** — Appended barrel re-exports after `./job-leads`:
```
export * from './outreach-campaigns';
export * from './outreach-emails';
```

### Migration File

**`drizzle/migrations/0013_outreach_campaigns.sql`** — Generated by drizzle-kit from journal tail idx 12 (`0012_whole_scream`). Contains:
- `CREATE TYPE "public"."outreach_campaign_status"` / `"outreach_channel"` / `"outreach_email_status"` (3 types)
- `CREATE TABLE "outreach_campaigns"` + `CREATE TABLE "outreach_emails"` (2 tables)
- `CONSTRAINT "outreach_emails_campaign_contact_unique" UNIQUE("campaign_id","contact_id")`
- 3 indexes: `outreach_emails_campaign_id_idx`, `outreach_emails_status_idx`, `outreach_emails_contact_id_idx`
- 2 FK constraints on `campaign_id` and `contact_id`
- `_journal.json` updated with idx 13 entry tagged `0013_outreach_campaigns`

### Verification (Task 1)

- `npx tsc --noEmit` exits 0 — no TypeScript errors introduced
- `grep -c` count of 9 required DDL patterns in migration SQL = 9 (all present)

## Pending: Live DB Apply

`npm run db:migrate` was denied by the auto-mode classifier (live production DB write). The migration file and journal entry are committed. To complete Task 2:

```bash
npm run db:migrate
```

Expected: exits 0, Drizzle applies `0013_outreach_campaigns.sql` to the Neon DB.

## Deviations from Plan

### Migration ordinal is 0013, not 0011

The CONTEXT.md references "migration 0011" for the outreach tables — stale reference from before quick tasks `260521-bhf` (migration 0011 = `0011_split_career_closeness`) and `0012_whole_scream`. The PLAN.md explicitly documented this correction: drizzle-kit auto-numbered from the journal tail (idx 12) and emitted `0013_outreach_campaigns.sql`. The migration file was NOT renamed — it carries the journal-assigned ordinal as required by the plan.

### Auto-mode gate on `npm run db:migrate`

The auto-mode classifier denied `npm run db:migrate` (live production DB write). Surfaced as `checkpoint:human-action`. Not a bug or architectural deviation — the migration file is correct and ready to apply.

## Threat Surface Scan

No new network endpoints, auth paths, or file access patterns introduced. Phase 11 is DDL only — no HTTP routes.

The `UNIQUE (campaign_id, contact_id)` constraint (T-11-01) is present in the generated SQL at line 33 — threat mitigation verified in the DDL.

## Known Stubs

None — this plan delivers schema/DDL only. No UI rendering paths or data sources to stub.

## Self-Check

- [x] `drizzle/schema/outreach-campaigns.ts` exists
- [x] `drizzle/schema/outreach-emails.ts` exists
- [x] `drizzle/schema/enums.ts` contains all 3 new enum declarations
- [x] `drizzle/schema/index.ts` exports `./outreach-campaigns` and `./outreach-emails`
- [x] `drizzle/migrations/0013_outreach_campaigns.sql` exists with 9 required DDL patterns
- [x] Task 1 commit `d6edc05` exists
- [x] Task 2 migration commit `af92a6b` exists
- [ ] `npm run db:migrate` applied to live Neon — PENDING HUMAN ACTION
