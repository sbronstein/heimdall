---
phase: 03-security-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/proxy.ts
  - src/middleware.ts
  - src/middleware.test.ts
autonomous: true
requirements:
  - SEC-A1
tags:
  - clerk
  - middleware
  - auth
  - security
must_haves:
  truths:
    - "Next.js auto-loads middleware at `src/middleware.ts` (file rename activates enforcement for the first time per PD-01)"
    - "The protected-route matcher list inside `clerkMiddleware` covers both `/dashboard(.*)` and `/api/(.*)`"
    - "An unauthenticated request to any `/api/*` path returns HTTP 401 with body `{ success: false, error: 'Unauthorized' }` (API-V1 envelope preserved per CD-02)"
    - "An authenticated request whose Clerk session email is not `steve@bronstein.org` is redirected to `/auth/sign-in` (single-user lock applied to both route classes per D-03)"
    - "Public auth pages under `/auth/*` remain reachable without a session (the protected matcher list is `[dashboard, api]` only — D-05)"
    - "Every `route.ts` under `src/app/api/**` has a URL path that matches the protected matcher pattern `/api/(.*)` (route-enumeration assertion — D-07 layer 2)"
    - "`npm run test:run` exits 0 with the new middleware tests passing"
    - "`npm run build` still succeeds after the rename + matcher expansion"
  artifacts:
    - path: "src/middleware.ts"
      provides: "Active Next.js middleware: clerkMiddleware + matcher expansion + explicit /api/* 401 envelope short-circuit"
      contains: "clerkMiddleware"
      exports: ["default", "config"]
    - path: "src/middleware.test.ts"
      provides: "Two layers of verification — middleware behavior unit tests + route-enumeration assertion"
      contains: "describe"
  key_links:
    - from: "src/middleware.ts"
      to: "@clerk/nextjs/server"
      via: "clerkMiddleware, createRouteMatcher, auth.protect"
      pattern: "clerkMiddleware|createRouteMatcher"
    - from: "src/middleware.ts"
      to: "next/server"
      via: "NextResponse.json for explicit /api/* 401"
      pattern: "NextResponse\\.json"
    - from: "src/middleware.test.ts"
      to: "src/app/api/**/route.ts"
      via: "glob enumeration in route-coverage describe block"
      pattern: "src/app/api"
---

<objective>
Activate the dead `clerkMiddleware()` file by renaming `src/proxy.ts` to `src/middleware.ts` (which Next.js auto-loads), expand the protected-route matcher to cover `/api/(.*)` in addition to `/dashboard(.*)`, and add an explicit 401-envelope short-circuit for unauthenticated `/api/*` requests so the API-V1 `{ success, data, error, meta }` contract is preserved for CLI consumers.

Purpose: Close SEC-A1 — every `/api/*` route currently authenticates against zero callers. After this plan, the middleware is the single chokepoint enforcing both Clerk session presence and the `steve@bronstein.org` email lock on every API path. Per D-06, no per-route `auth()` calls are added inside `src/app/api/**/route.ts` — middleware-only enforcement to avoid drift across 32 route files.

Output:
- `src/middleware.ts` (renamed from `src/proxy.ts` via `git mv`, with expanded matcher and explicit `/api/*` envelope short-circuit)
- `src/middleware.test.ts` (new — two-layer verification: middleware behavior + route-enumeration assertion)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-security-hardening/03-CONTEXT.md
@.planning/codebase/CONCERNS.md
@CLAUDE.md
@src/proxy.ts
@src/lib/api/types.ts
@src/lib/api/errors.ts
@vitest.config.ts
@src/components/layout/app-sidebar.ssr.test.tsx

<interfaces>
<!-- Existing source-of-truth excerpts the executor needs. No code authoring required from these — quote when implementing. -->

From `src/proxy.ts` (current shape — moved verbatim then edited):
- `import { clerkMiddleware, createRouteMatcher } from '@clerk/nextjs/server'`
- `import { NextRequest, NextResponse } from 'next/server'`
- `const ALLOWED_EMAIL = 'steve@bronstein.org'`
- `const isProtectedRoute = createRouteMatcher(['/dashboard(.*)'])` — expand to `['/dashboard(.*)', '/api/(.*)']`
- `export default clerkMiddleware(async (auth, req: NextRequest) => { ... })`
- `export const config = { matcher: [ '/((?!_next|[^?]*\\.(?:html?|css|js(?!on)|jpe?g|webp|png|gif|svg|ttf|woff2?|ico|csv|docx?|xlsx?|zip|webmanifest)).*)', '/(api|trpc)(.*)' ] }`

From `src/lib/api/types.ts` (envelope contract to preserve):
- `type ApiResponse<T> = { success: boolean; data?: T; error?: string; meta?: {...} }`
- 401 short-circuit body MUST be `{ success: false, error: 'Unauthorized' }`

From `src/components/layout/app-sidebar.ssr.test.tsx` (Phase 2 mocking pattern to follow):
- `vi.mock('@clerk/nextjs', () => ({ ... }))` — the Phase 2 surface
- For middleware testing the executor should `vi.mock('@clerk/nextjs/server', () => ({ clerkMiddleware: ..., createRouteMatcher: ... }))` — note this is `/server` not the client `@clerk/nextjs` mocked in Phase 2

API route surface to enumerate (32 paths under `src/app/api/`):
- glob target: `src/app/api/**/route.ts`
- every derived URL path MUST start with `/api/` and thus MUST match `/api/(.*)`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rename proxy.ts → middleware.ts, expand matcher, add /api/* 401 envelope short-circuit</name>
  <files>src/proxy.ts (deleted), src/middleware.ts (created via git mv)</files>
  <read_first>
    - `src/proxy.ts` (full file — this is what gets moved and edited)
    - `.planning/phases/03-security-hardening/03-CONTEXT.md` §"Decisions" D-01..D-06 + CD-02
    - `src/lib/api/types.ts` (envelope contract — body shape for the 401)
    - `src/lib/api/errors.ts` (existing 4xx response helpers — for reference; do NOT import inside middleware since middleware runs at edge runtime and these helpers use `Response.json`; use `NextResponse.json` instead)
  </read_first>
  <behavior>
    - Behavior 1: Next.js loads `src/middleware.ts` automatically (no manual config wiring). After rename, requests to `/dashboard/foo` and `/api/foo` BOTH flow through the middleware.
    - Behavior 2: Unauthenticated request to `/api/anything` returns HTTP 401 with JSON body `{ success: false, error: 'Unauthorized' }` (not a redirect, not HTML) — preserves API-V1 envelope for CLI per CD-02.
    - Behavior 3: Unauthenticated request to `/dashboard/anything` continues to be handled by `auth.protect()`'s default redirect-to-sign-in (existing behavior, unchanged by this plan).
    - Behavior 4: Authenticated request whose session email !== 'steve@bronstein.org' is redirected to `/auth/sign-in` for BOTH `/dashboard/*` and `/api/*` (D-03 — email lock applies equally).
    - Behavior 5: Public `/auth/*` paths pass through without invoking `auth.protect()` (the protected matcher list is `[dashboard, api]` only — D-05).
  </behavior>
  <action>
    Use `git mv src/proxy.ts src/middleware.ts` (CD-01 — preserves blame/history). Then edit `src/middleware.ts`:

    1. Expand the `createRouteMatcher` call argument list from `['/dashboard(.*)']` to `['/dashboard(.*)', '/api/(.*)']` (D-02).

    2. Add an explicit `/api/*` envelope short-circuit at the top of the `clerkMiddleware` async callback, BEFORE the existing `auth.protect()` call. Use `req.nextUrl.pathname.startsWith('/api/')` to detect API paths. For an API path without a valid session, return `NextResponse.json({ success: false, error: 'Unauthorized' }, { status: 401 })`. Determine "without a valid session" by calling `await auth()` (without `.protect()`) first and checking if `userId` is falsy — this avoids `auth.protect()`'s default redirect for `/api/*` (CD-02, confirmed via Clerk docs: `auth.protect()` redirects unauthenticated callers to sign-in by default, which yields HTML not JSON 401 — wrong shape for the CLI).

    3. For `/dashboard/*` paths, preserve the existing `auth.protect()` behavior (HTML redirect to sign-in is correct for browser callers).

    4. The email-lock check applies to both classes: after a session is confirmed present (via `userId`), check `sessionClaims?.email` and if non-empty and !== 'steve@bronstein.org', return `NextResponse.redirect(new URL('/auth/sign-in', req.url))` for BOTH path classes (D-03).

    5. Keep the existing `export const config = { matcher: [...] }` block unchanged — the outer matcher already covers both `/api/*` and non-static dashboard paths; the gate logic happens via `isProtectedRoute` inside the callback per D-05.

    Maintain the file's existing imports (`clerkMiddleware`, `createRouteMatcher`, `NextRequest`, `NextResponse`) — no new imports required. Use named identifiers only; no fenced code blocks elsewhere in the codebase need to change. Per D-06, do NOT add `auth()` calls to any `src/app/api/**/route.ts` file.

    Run `npm run build` after the edit to confirm the rename + matcher expansion compiles cleanly (Next.js 16 must successfully detect the new middleware location).
  </action>
  <verify>
    <automated>npm run build 2>&amp;1 | tail -20 &amp;&amp; test -f src/middleware.ts &amp;&amp; test ! -f src/proxy.ts &amp;&amp; grep -q "/api/(.*)" src/middleware.ts &amp;&amp; grep -q "success: false" src/middleware.ts &amp;&amp; grep -q "status: 401" src/middleware.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/middleware.ts` exists; `src/proxy.ts` does not (verified via `test -f` and `test ! -f`)
    - `git log --follow src/middleware.ts` shows history from the prior `src/proxy.ts` (blame preserved per CD-01)
    - `src/middleware.ts` contains the literal substring `/api/(.*)` in the `createRouteMatcher` argument
    - `src/middleware.ts` contains a `NextResponse.json(...)` call with `success: false` and `status: 401`
    - `src/middleware.ts` contains the literal substring `startsWith('/api/')` (path-class detection for the envelope short-circuit)
    - `src/middleware.ts` still contains the existing `ALLOWED_EMAIL = 'steve@bronstein.org'` constant and the redirect-to-`/auth/sign-in` branch for wrong-email sessions
    - `npm run build` exits 0 (Next.js compiles the renamed middleware without warnings about a missing matcher or unrecognized middleware location)
    - No new file is created under `src/app/api/**/route.ts` (D-06: no per-route auth calls — middleware-only)
  </acceptance_criteria>
  <done>The middleware is active for the first time, gates both `/dashboard/*` and `/api/*` with the single-user email lock, and returns a proper `{ success: false, error: 'Unauthorized' }` 401 JSON envelope for unauthenticated `/api/*` callers. `npm run build` is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write middleware unit test + route-enumeration assertion (src/middleware.test.ts)</name>
  <files>src/middleware.test.ts</files>
  <read_first>
    - `src/middleware.ts` (the file just produced by Task 1 — test target)
    - `src/components/layout/app-sidebar.ssr.test.tsx` (Phase 2 vi.mock pattern — Clerk mocking template)
    - `src/components/layout/app-sidebar.hydration.test.tsx` (Phase 2 jsdom test pattern for reference)
    - `vitest.config.ts` (env=node default, glob `src/**/*.test.{ts,tsx}`, `passWithNoTests: true`)
    - `.planning/phases/03-security-hardening/03-CONTEXT.md` §"Decisions" D-07..D-09 (verification strategy)
  </read_first>
  <behavior>
    - Behavior 1 (describe block A — unauthenticated `/api/*` returns 401 envelope): A `NextRequest` to `/api/companies` with no Clerk session produces a `NextResponse` with `status === 401` and JSON body `{ success: false, error: 'Unauthorized' }`. Tests at least two representative API paths (e.g. `/api/companies` and `/api/job-leads/abc/search`) to cover both list and nested-dynamic shapes.
    - Behavior 2 (describe block B — unauthenticated `/dashboard/*` triggers Clerk's protect): A `NextRequest` to `/dashboard/overview` with no Clerk session causes `auth.protect()` to be invoked (the mock records the call); the response is NOT the JSON 401 envelope (it goes down the Clerk-redirect path). The test asserts on the mock-call shape, not on Clerk's actual redirect.
    - Behavior 3 (describe block C — wrong-email session redirected to /auth/sign-in): A `NextRequest` to `/api/companies` OR `/dashboard/overview` with a Clerk session whose `sessionClaims.email === 'attacker@example.com'` produces a `NextResponse.redirect` with location `/auth/sign-in`. The email-lock check fires for BOTH route classes (D-03).
    - Behavior 4 (describe block D — public `/auth/*` passes through): A `NextRequest` to `/auth/sign-in` does NOT invoke `auth.protect()` and does NOT return the envelope short-circuit (mock `auth.protect` is not called; response is undefined / pass-through).
    - Behavior 5 (describe block E — route-enumeration assertion): Glob `src/app/api/**/route.ts` from the test, derive each URL path by stripping `src/app` prefix and removing the trailing `/route.ts` (e.g. `src/app/api/companies/route.ts` → `/api/companies`, `src/app/api/companies/[id]/route.ts` → `/api/companies/[id]`). Assert that for every derived path, the regex `/^\/api\/(.*)/` matches (this proves the protected-matcher pattern `'/api/(.*)'` covers the entire current route surface — catches the failure mode where a route is added that escapes the matcher). Asserts the glob returns >= 32 paths.
  </behavior>
  <action>
    Create `src/middleware.test.ts` (Vitest, node environment — the default in `vitest.config.ts`; no `@vitest-environment` pragma needed). Structure as five `describe` blocks per the behaviors above.

    Mocking approach:
    - `vi.mock('@clerk/nextjs/server', ...)` — replace `clerkMiddleware` with a passthrough that invokes the user callback with a stub `auth` object. The stub `auth()` returns `{ userId, sessionClaims }` configurable per test via a shared variable (e.g. `let mockAuthReturn`). The stub `auth.protect()` is a `vi.fn()` that records calls and returns the same `{ userId, sessionClaims }` shape — sufficient because the middleware reads those fields from its return.
    - `createRouteMatcher` mock — return a real function that does string-prefix matching against the same `['/dashboard(.*)', '/api/(.*)']` patterns; for the test path's `req.nextUrl.pathname`, return true if it starts with `/dashboard` or `/api/`.

    Construct `NextRequest` instances directly: `import { NextRequest } from 'next/server'` then `new NextRequest(new URL('http://localhost/api/companies'))`. The middleware import should be the default export of `src/middleware.ts`.

    For the unauthenticated-`/api/*` 401 test (behavior 1):
    - Set `mockAuthReturn` to `{ userId: null, sessionClaims: null }`.
    - Invoke the middleware default export with the constructed `NextRequest`.
    - Assert returned `Response.status === 401` and `await response.json()` equals `{ success: false, error: 'Unauthorized' }`.

    For the wrong-email test (behavior 3):
    - Set `mockAuthReturn` to `{ userId: 'user_xyz', sessionClaims: { email: 'attacker@example.com' } }`.
    - Invoke middleware for `/api/companies` AND for `/dashboard/overview`.
    - For each, assert returned response is a `NextResponse.redirect` (check `response.status` is 307 or 308 AND `response.headers.get('location')` contains `/auth/sign-in`).

    For the route-enumeration assertion (behavior 5):
    - `import { globSync } from 'node:fs'` if available (Node 22 supports), OR use `fast-glob` if already in deps, OR `import { readdirSync } from 'node:fs'` recursive — Phase 2 already targets Node 22 (`.nvmrc`). Simplest: `readdirSync('src/app/api', { recursive: true, withFileTypes: true })`, filter to `route.ts` files, derive paths.
    - Build the regex once: `const apiMatcher = /^\/api\/(.*)/`.
    - For each derived path, `expect(apiMatcher.test(derivedPath)).toBe(true)`.
    - `expect(derivedPaths.length).toBeGreaterThanOrEqual(32)` (current count per CONTEXT.md is 32 — assertion is `>=` not `===` so future-route additions don't break the test).

    Do NOT attempt per-route HTTP-level sweep tests (D-08 — middleware runs before handlers; would require mocking middleware to reach them, defeats the point).
  </action>
  <verify>
    <automated>npm run test:run -- src/middleware.test.ts 2>&amp;1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `src/middleware.test.ts` exists with five `describe` blocks (one per behavior 1–5)
    - `npm run test:run -- src/middleware.test.ts` exits 0 with all describes passing
    - `npm run test:run` (full suite) exits 0 — no regressions to Phase 2 tests
    - Test file contains a `vi.mock('@clerk/nextjs/server', ...)` call (Phase 2 pattern adapted for the `/server` subpath)
    - Test file contains a glob/readdir traversal of `src/app/api/` filtering to `route.ts` files
    - Test file asserts the glob result length is `>= 32`
    - Test file asserts the regex `/^\/api\/(.*)/` matches every derived API path
    - Test file constructs `NextRequest` instances and invokes the default export of `src/middleware.ts` directly (not via HTTP)
    - Test file does NOT mock the middleware itself (D-08 — middleware is the system under test, not a stub)
    - Test file does NOT add any per-route handler imports (D-06 + D-08 — only the middleware module is imported)
  </acceptance_criteria>
  <done>Two layers of verification per D-07 land in a single test file: (1) middleware behavior tests prove the 401 envelope, wrong-email redirect, and `/auth/*` passthrough; (2) route-enumeration assertion proves every current `/api/*` path falls under the protected matcher. Suite is green; pre-push hook will catch matcher drift on every future commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Anonymous internet → `/api/*` | Untrusted (no session) HTTP callers; previously had ZERO authentication enforcement at any layer per CONCERNS.md §"Security Considerations → No Authentication on Any API Route" |
| Authenticated non-owner Clerk session → `/api/*` | Any Clerk-issued session for an email !== `steve@bronstein.org`; CLI parity assumes single-user, so a second user's session reaching the API surface is a confidentiality breach |
| `/auth/sign-in`, `/auth/sign-up` | Public auth pages — MUST remain reachable without a session to support the sign-in flow |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T1 (T-03-01) | Spoofing / Information Disclosure / Tampering | All 32 `src/app/api/**/route.ts` handlers | mitigate | Task 1 activates `src/middleware.ts` via `git mv` and expands `createRouteMatcher` to include `/api/(.*)`. Every unauthenticated `/api/*` request short-circuits with `NextResponse.json({ success: false, error: 'Unauthorized' }, { status: 401 })` BEFORE reaching the route handler. Per D-06 the middleware is the single chokepoint — no per-route drift. Closes the "0/34 routes authenticate" gap recorded in CONCERNS.md. |
| T2 (T-03-02) | Spoofing / Elevation of Privilege | `src/middleware.ts` email-lock branch | mitigate | The existing `ALLOWED_EMAIL = 'steve@bronstein.org'` check is preserved in the rename and explicitly applies to BOTH `/dashboard/*` and `/api/*` per D-03. Wrong-email Clerk sessions get redirected to `/auth/sign-in` for both classes. Tested in `src/middleware.test.ts` describe block C against both path classes. |
| T3 (T-03-03) | Spoofing / Auth-bypass via matcher gap | `createRouteMatcher` argument list in `src/middleware.ts` | mitigate | Task 2 ships a route-enumeration assertion (D-07 layer 2) that globs `src/app/api/**/route.ts` at test time and asserts every derived path matches the regex `/^\/api\/(.*)/`. If a future route is added under a path that escapes the matcher pattern (e.g. accidentally placed under `src/app/v2-api/`), `npm run test:run` fails — caught at pre-push by the Phase 2 husky hook. |
| T6 (T-03-06) | Information Disclosure | Clerk's default `auth.protect()` redirect on `/api/*` | mitigate | CD-02: Clerk's default behavior on unauthenticated `/api/*` is an HTML redirect to `/auth/sign-in`, which leaks app structure to the CLI consumer and breaks the API-V1 envelope contract. Task 1 adds an explicit `req.nextUrl.pathname.startsWith('/api/')` short-circuit that returns the standard 401 envelope `{ success: false, error: 'Unauthorized' }` instead. Verified by Task 2 describe block A. |

## Residual Risk

- T1: **low** — middleware is auto-loaded by Next.js framework convention (not opt-in config), short-circuit is in test coverage, and the matcher gate is enumeration-tested.
- T2: **low** — email-lock branch is unchanged from existing `src/proxy.ts` behavior and now actually fires (previously was dead code).
- T3: **low** — the route-enumeration test runs on every push via the existing husky pre-push hook (Phase 2 D-04), so matcher drift is caught before remote.
- T6: **low** — envelope short-circuit is explicit + asserted; CLI consumers continue to see uniform `{ success: false, error }` shape.

**Block-on-high count: 0.** ASVS L1 gate clears.
</threat_model>

<verification>
After both tasks complete:

1. `test -f src/middleware.ts && test ! -f src/proxy.ts` — rename succeeded
2. `git log --follow src/middleware.ts` shows the prior `src/proxy.ts` history (blame preserved)
3. `grep -E "(/api/\(\.\*\)|success: false|status: 401|startsWith\('/api/'\))" src/middleware.ts | wc -l` returns >= 4
4. `npm run build` exits 0
5. `npm run test:run -- src/middleware.test.ts` passes all five describes
6. `npm run test:run` (full suite) passes — no regressions to Phase 2 (`pipeline`, `envelope`, `app-sidebar.ssr`, `app-sidebar.hydration`, CSV-parse, bridge-score, status-transition tests)
7. Smoke verify (manual / dev-server, optional sanity check — not blocking): `curl -i http://localhost:4000/api/companies` (no session cookie) returns `HTTP/1.1 401` with body `{"success":false,"error":"Unauthorized"}`. This is informational; the automated tests above are the binding gate.
</verification>

<success_criteria>
SEC-A1 satisfied:
- Middleware is active (rename done; Next.js auto-loads `src/middleware.ts`)
- Every `/api/*` route is gated by Clerk session + `steve@bronstein.org` email lock
- Unauthenticated `/api/*` callers receive `{ success: false, error: 'Unauthorized' }` with HTTP 401 (envelope preserved for CLI per CD-02)
- Two-layer automated verification ships in `src/middleware.test.ts`:
  - Layer 1: middleware behavior unit tests (401 envelope, wrong-email redirect, public `/auth/*` passthrough)
  - Layer 2: route-enumeration assertion (every `src/app/api/**/route.ts` path matches the protected matcher pattern)
- `npm run build` and `npm run test:run` both exit 0
- No per-route `auth()` calls added (D-06 — middleware is the single chokepoint)
</success_criteria>

<output>
After completion, create `.planning/phases/03-security-hardening/03-01-SUMMARY.md` documenting:
- Confirmation that `git mv src/proxy.ts src/middleware.ts` was used (CD-01)
- The exact form of the `/api/*` envelope short-circuit (CD-02 decision applied)
- Current `/api/*` route count from the enumeration test (expected: 32 per CONTEXT.md PD-01)
- Any deviation from D-07 (e.g. if Clerk's `auth()` signature in the installed version required adjustments to the mock)
- Note for follow-on doc updates: `.planning/codebase/ARCHITECTURE.md` §"Entry Points → Clerk Middleware (`src/proxy.ts`)" and `CLAUDE.md` "Single-user Clerk lock in middleware" both reference the old filename and the inaccurate "currently active" claim — flag for Phase 3 wrap-up doc-correction step.
</output>
