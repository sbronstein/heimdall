---
phase: 06-performance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - drizzle/schema/contacts.ts
  - drizzle/schema/companies.ts
  - drizzle/migrations/0008_phase6_indexes.sql
  - drizzle/migrations/meta/_journal.json
  - drizzle/migrations/meta/0008_snapshot.json
  - src/lib/db/__phase6_indexes__.test.ts
autonomous: true
requirements: [PERF-A4, PERF-A5]
tags: [drizzle, postgres, schema, indexes, migration]

must_haves:
  truths:
    - "All 6 hot-path indexes (5 btree + 1 partial UNIQUE) exist on the live DB and on PGlite after createTestDb()"
    - "The UNIQUE partial index on contacts(linkedin_url) WHERE linkedin_url IS NOT NULL AND archived_at IS NULL exists — predicate covers ONLY active rows so re-import of archived contacts creates a fresh row (CONTEXT §Out of scope invariant)"
    - "Schema files compile and `npm run build` still passes against unchanged route code"
    - "Index-presence regression test in src/lib/db/__phase6_indexes__.test.ts asserts all 6 indexes by indexname against pg_indexes AND asserts the partial UNIQUE's WHERE predicate contains both `linkedin_url IS NOT NULL` and `archived_at IS NULL`"
    - "D-12: indexes are declared in the Drizzle schema via `index('idx_name').on(table.column)` in each table file's third callback; plain `CREATE INDEX` (no CONCURRENTLY — incompatible with Drizzle's transactional migration runner; single-user 1500-row table makes the brief lock irrelevant)"
    - "D-13: the 6-index list is fixed: (1) `contacts(archived_at)` btree, (2) `contacts(linkedin_url)` partial UNIQUE WHERE linkedin_url IS NOT NULL AND archived_at IS NULL, (3) `contacts(company_id)` btree, (4) `contacts(linkedin_connection_date)` btree, (5) `companies(name)` btree"
    - "D-14: the migration filename is `drizzle/migrations/0008_*.sql` generated by `npm run db:generate`; a single migration file holds all six DDL statements and is applied via `npm run db:migrate` against the local Neon dev branch (then production at deploy time)"
    - "D-16: Wave 1 = schema edits + `npm run db:generate` + `npm run db:migrate` shipped as a single atomic commit; this plan blocks all of Wave 2 (the D-08 ON CONFLICT in Plan 04 depends on the UNIQUE index existing on the live DB)"
  artifacts:
    - path: "drizzle/schema/contacts.ts"
      provides: "4 index() + 1 uniqueIndex().where(sql`linkedin_url IS NOT NULL AND archived_at IS NULL`) declarations in third-callback array"
      contains: "contacts_archived_at_idx, contacts_linkedin_url_unique_idx, contacts_company_id_idx, contacts_linkedin_connection_date_idx"
    - path: "drizzle/schema/companies.ts"
      provides: "1 index() declaration on name"
      contains: "companies_name_idx"
    - path: "drizzle/migrations/0008_phase6_indexes.sql"
      provides: "Generated migration with 5 CREATE INDEX + 1 CREATE UNIQUE INDEX statements; the UNIQUE has predicate `WHERE \"linkedin_url\" IS NOT NULL AND \"archived_at\" IS NULL`"
      contains: "CREATE INDEX \"contacts_archived_at_idx\", CREATE UNIQUE INDEX \"contacts_linkedin_url_unique_idx\""
    - path: "src/lib/db/__phase6_indexes__.test.ts"
      provides: "Regression test pinning index presence against pg_indexes (D-20) AND pinning the active-rows-only partial predicate"
      contains: "pg_indexes, expect.arrayContaining, archived_at IS NULL"
  key_links:
    - from: "drizzle/schema/contacts.ts"
      to: "drizzle/migrations/0008_phase6_indexes.sql"
      via: "npm run db:generate"
      pattern: "CREATE INDEX.*contacts_.*_idx"
    - from: "drizzle/migrations/0008_phase6_indexes.sql"
      to: "src/lib/db/__phase6_indexes__.test.ts"
      via: "createTestDb() replays all migrations including 0008"
      pattern: "indexname.*contacts_linkedin_url_unique_idx"
---

<objective>
Add hot-path index declarations to the Drizzle schema, generate the corresponding migration (0008), apply it to the local Neon dev branch, and lock the result with a pg_indexes-backed regression test. This is the foundation Wave 2 depends on — Plan 4's `ON CONFLICT DO NOTHING` on `contacts.linkedinUrl` only works once the partial UNIQUE index exists, and Plans 2/3/4/5 all benefit from the btree indexes added here.

The partial UNIQUE index's predicate is `WHERE linkedin_url IS NOT NULL AND archived_at IS NULL` — this preserves the existing dedup-only-against-active-contacts invariant (CONTEXT §Out of scope: "the existing behavior — dedup only against active contacts, allowing re-import of archived ones — is the documented intent").

Purpose: PERF-A4 (the 5-index list locked in ROADMAP SC #3) plus the partial UNIQUE index needed for PERF-A5 (D-08).
Output: 2 schema files modified, 1 SQL migration generated, 1 regression test added, migration applied to the dev branch.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-performance/06-CONTEXT.md
@.planning/phases/06-performance/06-PATTERNS.md
@CLAUDE.md
@drizzle/schema/contacts.ts
@drizzle/schema/companies.ts
@drizzle/schema/job-leads.ts
@drizzle/migrations/0007_add_queued_failed_status_and_error_columns.sql
@src/test-utils/pglite.ts

<interfaces>
<!-- Drizzle pg-core surface this plan introduces. No prior `index()` / `uniqueIndex()` usage exists in the repo (see 06-PATTERNS.md §"No Analog Found"). -->

From drizzle-orm/pg-core (planner-introduced API):
```typescript
// Both functions take an index name (required for the regression test in D-20)
// and chain .on(...columns) to declare the indexed columns.
// uniqueIndex additionally supports .where(sql`...`) for partial indexes.
export function index(name: string): IndexBuilderOn;
export function uniqueIndex(name: string): UniqueIndexBuilderOn;
// IndexBuilderOn.on(...columns) -> IndexBuilder
// UniqueIndexBuilderOn.on(...columns) -> UniqueIndexBuilder
// UniqueIndexBuilder.where(sql`<predicate>`) -> UniqueIndexBuilder  (D-08 partial)
```

From drizzle/schema/job-leads.ts (line 85) — the ONLY existing third-callback table-constraint example in the repo, shape reference:
```typescript
(table) => [unique('prospect_bridge_unique').on(table.prospectId, table.contactId)]
```

From src/test-utils/pglite.ts (createTestDb):
```typescript
export async function createTestDb(): Promise<DrizzleClient>;
// Replays all drizzle/migrations/*.sql into a fresh PGlite instance.
// New migrations are picked up automatically by the readdir sort.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add index() and uniqueIndex() declarations to contacts and companies schema</name>
  <files>drizzle/schema/contacts.ts, drizzle/schema/companies.ts</files>
  <read_first>
    - drizzle/schema/contacts.ts (full file, 55 lines) — current pgTable definition, second-arg column block is correct as-is; only the third-callback array is being added.
    - drizzle/schema/companies.ts (full file, 56 lines) — same shape.
    - drizzle/schema/job-leads.ts lines 66-86 — the ONLY existing third-callback constraint usage in the repo (`unique('prospect_bridge_unique').on(table.prospectId, table.contactId)`). The new index/uniqueIndex calls follow the same `(table) => [...]` arrow-callback shape.
    - .planning/phases/06-performance/06-PATTERNS.md §"drizzle/schema/contacts.ts" — full reference pattern with the `import { index, uniqueIndex } from 'drizzle-orm/pg-core'` and `import { sql } from 'drizzle-orm'` import additions documented.
    - .planning/phases/06-performance/06-CONTEXT.md §"Index Migration (PERF-A4)" — D-12 (plain CREATE INDEX, not CONCURRENTLY), D-13 (the 5+1 index list), and §"Out of scope" — "Removing the archivedAt IS NULL filter from contacts dedup — the existing behavior (dedup only against active contacts, allowing re-import of archived ones) is the documented intent." This invariant DICTATES the partial UNIQUE predicate below.
  </read_first>
  <action>
    Modify both schema files per D-12 / D-13. NO column definitions change — only the third callback is added.

    In `drizzle/schema/contacts.ts`:
    - Extend the `drizzle-orm/pg-core` import to include `index` and `uniqueIndex`.
    - Add `import { sql } from 'drizzle-orm';` (required for the partial-index WHERE predicate on the UNIQUE index per D-08).
    - Add the third-callback to `pgTable('contacts', { ... }, (table) => [ ... ])` returning an array with exactly 4 declarations:
      1. `index('contacts_archived_at_idx').on(table.archivedAt)` — supports the ubiquitous `WHERE archived_at IS NULL` filter (D-13 #1).
      2. `uniqueIndex('contacts_linkedin_url_unique_idx').on(table.linkedinUrl).where(sql\`${table.linkedinUrl} IS NOT NULL AND ${table.archivedAt} IS NULL\`)` — D-08 partial UNIQUE; doubles as the linkedin_url btree from PERF-A4 #2 (one index, two purposes per D-13 #2). **The `AND archived_at IS NULL` half of the predicate is REQUIRED to preserve the CONTEXT §Out of scope invariant** ("the existing behavior — dedup only against active contacts, allowing re-import of archived ones — is the documented intent"). Without this clause, a previously-archived contact with linkedin_url='X' would block re-import of a NEW active contact with the same URL via ON CONFLICT DO NOTHING — a silent behavioral regression. The partial UNIQUE predicate scopes the index to active rows only.
      3. `index('contacts_company_id_idx').on(table.companyId)` — JOIN key (D-13 #3).
      4. `index('contacts_linkedin_connection_date_idx').on(table.linkedinConnectionDate)` — triage ordering (D-13 #4).

    In `drizzle/schema/companies.ts`:
    - Extend the `drizzle-orm/pg-core` import to include `index`.
    - Add the third-callback returning `[ index('companies_name_idx').on(table.name) ]` (D-13 #5).

    Naming convention: `<table>_<column>_idx` (matches Drizzle's default emission convention). Explicit names are mandatory because the regression test in Task 3 queries `pg_indexes.indexname` by string match.

    Do NOT change any column definition, default, nullability, or reference. Do NOT touch any other schema file. Do NOT add indexes beyond the 5+1 locked by D-13 — `pg_trgm` GIN indexes are deferred per CONTEXT §Deferred. Do NOT use a predicate of `linkedin_url IS NOT NULL` ALONE — the conjunctive `AND archived_at IS NULL` is mandatory.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "drizzle/schema/(contacts|companies)\.ts" || echo "Schema files compile clean"</automated>
  </verify>
  <acceptance_criteria>
    - `drizzle/schema/contacts.ts` imports `index` and `uniqueIndex` from `drizzle-orm/pg-core` and `sql` from `drizzle-orm`.
    - `drizzle/schema/contacts.ts` third-callback returns an array with exactly 4 elements: one `index('contacts_archived_at_idx')`, one `uniqueIndex('contacts_linkedin_url_unique_idx').where(sql\`... IS NOT NULL AND ... IS NULL\`)` (BOTH predicates conjoined), one `index('contacts_company_id_idx')`, one `index('contacts_linkedin_connection_date_idx')`.
    - The `uniqueIndex(...).where(...)` predicate references BOTH `table.linkedinUrl` (with `IS NOT NULL`) AND `table.archivedAt` (with `IS NULL`), connected by `AND` — verifiable by grep on the schema source.
    - `drizzle/schema/companies.ts` imports `index` from `drizzle-orm/pg-core` and the third-callback returns `[index('companies_name_idx').on(table.name)]`.
    - All existing column definitions in both files are unchanged (verify via diff: only import lines and the closing `)` lines are modified).
    - `npx tsc --noEmit` exits 0 with no errors mentioning the two schema files.
  </acceptance_criteria>
  <done>Both schema files declare the indexes per D-13 with the partial UNIQUE scoped to active rows only; types still compile.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Generate Drizzle migration 0008 and apply to local Neon dev branch</name>
  <files>drizzle/migrations/0008_phase6_indexes.sql, drizzle/migrations/meta/_journal.json, drizzle/migrations/meta/0008_snapshot.json</files>
  <read_first>
    - drizzle/migrations/0007_add_queued_failed_status_and_error_columns.sql (format reference: `--> statement-breakpoint` markers between DDL statements, double-quoted identifiers).
    - drizzle/migrations/meta/_journal.json (auto-edited by drizzle-kit; do NOT hand-edit — `npm run db:generate` updates it).
    - drizzle.config.ts (confirms dialect: postgresql, schema: ./drizzle/schema/index.ts, out: ./drizzle/migrations).
    - .planning/phases/06-performance/06-CONTEXT.md §"Migration filename + format" (D-14) — accepts the random-slug filename `0008_<slug>.sql` from drizzle-kit, but the planner-preferred descriptive slug is `0008_phase6_indexes.sql`. Rename post-generation if drizzle-kit emits a random slug.
    - .planning/phases/06-performance/06-PATTERNS.md §"drizzle/migrations/0008_*.sql" — expected DDL content (5 CREATE INDEX + 1 CREATE UNIQUE INDEX, plain — NOT CONCURRENTLY per D-12).
  </read_first>
  <action>
    This task is BLOCKING: Wave 2 cannot pass verification without these indexes applied. TS types come from schema files (not the live DB), so build + type-check would pass without the migration — a false-positive. This task closes that gap.

    Step 1 — Generate the migration:
    - Run `npm run db:generate`. This invokes `drizzle-kit generate` against `drizzle.config.ts`, reads the modified `drizzle/schema/contacts.ts` and `drizzle/schema/companies.ts` from Task 1, diffs against the most recent snapshot in `drizzle/migrations/meta/`, and writes a new `drizzle/migrations/0008_<slug>.sql` file plus an updated `_journal.json` and a new `0008_snapshot.json`.
    - drizzle-kit emits a random slug by default. After generation, rename the SQL file to `0008_phase6_indexes.sql` (`git mv drizzle/migrations/0008_<random>.sql drizzle/migrations/0008_phase6_indexes.sql`). Update `_journal.json` to reference the renamed tag (drizzle-kit stores the tag without the `.sql` extension — find the `"tag": "0008_<random>"` entry in the journal entries array and rename it to `"0008_phase6_indexes"`).

    Step 2 — Verify the SQL content:
    - The generated SQL MUST contain all 6 DDL statements in this exact set (order may differ; drizzle-kit chooses):
      1. `CREATE INDEX "contacts_archived_at_idx" ON "contacts" USING btree ("archived_at");`
      2. `CREATE UNIQUE INDEX "contacts_linkedin_url_unique_idx" ON "contacts" USING btree ("linkedin_url") WHERE "linkedin_url" IS NOT NULL AND "archived_at" IS NULL;` **Both halves of the predicate MUST appear** — if drizzle-kit emits only `WHERE "linkedin_url" IS NOT NULL`, Task 1's schema edit was insufficient; STOP and fix Task 1 before re-running generate.
      3. `CREATE INDEX "contacts_company_id_idx" ON "contacts" USING btree ("company_id");`
      4. `CREATE INDEX "contacts_linkedin_connection_date_idx" ON "contacts" USING btree ("linkedin_connection_date");`
      5. `CREATE INDEX "companies_name_idx" ON "companies" USING btree ("name");`
    - Statements 1-4 must be separated by `--> statement-breakpoint` markers (matches 0007 format).
    - There must be NO `CONCURRENTLY` keyword (D-12: plain CREATE INDEX; Drizzle's migration runner wraps DDL in transactions which conflicts with CONCURRENTLY).
    - If any statement is missing or extra DDL is present (e.g., column type changes, drops), STOP — Task 1's schema edits diverged from D-13. Fix Task 1 and re-generate.

    Step 3 — Apply to the local Neon dev branch:
    - Confirm `DATABASE_URL` in `.env.local` points to the local Neon dev branch (NOT production).
    - Run `npm run db:migrate`. drizzle-kit will apply 0008 (and skip 0000-0007 which are already applied). Exit code must be 0.
    - If the migration fails because the partial UNIQUE would be violated by existing data (a contact with a duplicate linkedin_url AMONG ACTIVE rows already in the live DB), report the duplicates back to the user and STOP. The conflict query for the NEW predicate is: `SELECT linkedin_url, COUNT(*) FROM contacts WHERE linkedin_url IS NOT NULL AND archived_at IS NULL GROUP BY 1 HAVING COUNT(*) > 1`. Do NOT delete or merge contacts to force the migration through — that is a data decision, not a migration decision.

    Do NOT run `npm run db:push` (dev-only schema sync that bypasses the migration history). Do NOT skip the regeneration of `_journal.json`. Do NOT hand-edit the generated SQL except to rename the slug — drizzle-kit's output is the canonical migration content.
  </action>
  <verify>
    <automated>test -f drizzle/migrations/0008_phase6_indexes.sql && grep -c "CREATE INDEX\|CREATE UNIQUE INDEX" drizzle/migrations/0008_phase6_indexes.sql | grep -q "^6$" && grep -q "WHERE \"linkedin_url\" IS NOT NULL AND \"archived_at\" IS NULL" drizzle/migrations/0008_phase6_indexes.sql && ! grep -qi "CONCURRENTLY" drizzle/migrations/0008_phase6_indexes.sql && echo "Migration file shape OK with active-rows-only partial predicate"</automated>
  </verify>
  <acceptance_criteria>
    - `drizzle/migrations/0008_phase6_indexes.sql` exists on disk.
    - The file contains exactly 6 DDL statements (5 `CREATE INDEX` + 1 `CREATE UNIQUE INDEX ... WHERE ... IS NOT NULL AND ... IS NULL`); no other DDL.
    - The `CREATE UNIQUE INDEX` statement's WHERE clause contains BOTH `"linkedin_url" IS NOT NULL` AND `"archived_at" IS NULL`, conjoined with `AND` — verifiable by the grep in `<verify>`.
    - No `CONCURRENTLY` keyword appears anywhere in the file.
    - `drizzle/migrations/meta/_journal.json` includes a journal entry whose `tag` is `0008_phase6_indexes` and `breakpoints` matches the other entries.
    - `drizzle/migrations/meta/0008_snapshot.json` exists.
    - `npm run db:migrate` against the local Neon dev branch exits 0 (verify `echo $?` is 0 immediately after).
    - All previous migrations 0000-0007 remain unchanged (verify via `git diff --stat drizzle/migrations/` — only new files added, no existing files modified except `_journal.json`).
  </acceptance_criteria>
  <done>Migration 0008 is on disk with correct DDL (including the active-rows-only partial predicate), journal is updated, and the migration is applied to the local dev DB.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add pg_indexes regression test pinning all 6 indexes and the active-rows-only partial predicate (D-20)</name>
  <files>src/lib/db/__phase6_indexes__.test.ts</files>
  <read_first>
    - src/test-utils/pglite.ts (full file, 30 lines) — `createTestDb()` replays all `drizzle/migrations/*.sql` files in sort order; 0008 from Task 2 is picked up automatically with no harness change.
    - src/app/api/applications/[id]/status/route.test.ts (top of file, ~10 lines) — canonical `vi.hoisted + Proxy` pattern; this test does NOT need that pattern because it talks to PGlite directly via `createTestDb()` (no route handler under test, no module-level `db` to mock).
    - .planning/phases/06-performance/06-PATTERNS.md §"Index-presence regression test (D-20) — NEW pattern" — sketch + the 6 expected indexname strings.
    - .planning/phases/06-performance/06-CONTEXT.md D-20 — "queries pg_indexes on the PGlite instance after migrations run, asserting all six indexes from D-13 exist on the expected columns."
    - .planning/phases/06-performance/06-CONTEXT.md §"Out of scope" — the re-import-of-archived invariant Test 2's expanded predicate assertion locks in.
  </read_first>
  <behavior>
    - Test 1 (D-20 happy path): createTestDb() → query `SELECT indexname FROM pg_indexes WHERE schemaname='public' AND tablename IN ('contacts','companies')` → result includes all 5 named indexes (`contacts_archived_at_idx`, `contacts_linkedin_url_unique_idx`, `contacts_company_id_idx`, `contacts_linkedin_connection_date_idx`, `companies_name_idx`).
    - Test 2 (partial-index predicate, active-rows-only): the row for `contacts_linkedin_url_unique_idx` has an `indexdef` string containing `UNIQUE`, `WHERE`, `linkedin_url IS NOT NULL`, AND `archived_at IS NULL` — pinning the conjunctive predicate that preserves the re-import-of-archived invariant. If `archived_at IS NULL` is missing from the predicate, Task 1's schema edit reverted to the buggy version; the test must fail loudly.
    - Naming-drift guard: assertion is `expect(indexNames).toEqual(expect.arrayContaining([...all 5...]))` — order-independent, but missing any one of the 5 fails the test.
  </behavior>
  <action>
    Create `src/lib/db/__phase6_indexes__.test.ts` (a new test file in `src/lib/db/` to keep it adjacent to the db client; the path is intentionally non-`route.test.ts` because there's no route under test).

    Imports:
    - `createTestDb` from `@/test-utils/pglite`
    - `sql` from `drizzle-orm`
    - vitest globals `describe`, `it`, `expect` (vitest's `globals: true` config means no `import { describe, it, expect } from 'vitest'` is needed; confirm via existing tests in `src/app/api/**/route.test.ts` which also rely on globals).

    Structure:
    ```typescript
    describe('Phase 6 schema indexes regression (D-20)', () => {
      it('migrations create the 5 hot-path indexes from D-13', async () => {
        const db = await createTestDb();
        const result = await db.execute(sql`
          SELECT indexname, tablename, indexdef
          FROM pg_indexes
          WHERE schemaname = 'public'
            AND tablename IN ('contacts', 'companies')
        `);
        const rows = result.rows as Array<{ indexname: string; tablename: string; indexdef: string }>;
        const indexNames = rows.map((r) => r.indexname);
        expect(indexNames).toEqual(expect.arrayContaining([
          'contacts_archived_at_idx',
          'contacts_linkedin_url_unique_idx',
          'contacts_company_id_idx',
          'contacts_linkedin_connection_date_idx',
          'companies_name_idx'
        ]));
      });

      it('contacts_linkedin_url_unique_idx is a partial UNIQUE scoped to ACTIVE rows only (D-08 + Out-of-scope invariant)', async () => {
        const db = await createTestDb();
        const result = await db.execute(sql`
          SELECT indexdef
          FROM pg_indexes
          WHERE schemaname = 'public'
            AND tablename = 'contacts'
            AND indexname = 'contacts_linkedin_url_unique_idx'
        `);
        const rows = result.rows as Array<{ indexdef: string }>;
        expect(rows).toHaveLength(1);
        const def = rows[0].indexdef.toUpperCase();
        // Pins the UNIQUE constraint.
        expect(def).toContain('UNIQUE');
        // Pins the partial predicate exists.
        expect(def).toContain('WHERE');
        // Pins BOTH halves of the conjunctive predicate. The `AND archived_at IS NULL` half
        // is the CONTEXT §Out-of-scope invariant — "the existing behavior (dedup only against
        // active contacts, allowing re-import of archived ones) is the documented intent."
        // Without this clause, re-importing a previously-archived linkedin_url silently
        // no-ops via ON CONFLICT DO NOTHING in Plan 4 — a behavioral regression.
        expect(def).toContain('LINKEDIN_URL IS NOT NULL');
        expect(def).toContain('ARCHIVED_AT IS NULL');
      });
    });
    ```

    Type note: `db.execute()` against the pglite drizzle adapter returns `{ rows: unknown[] }`. The `as Array<...>` cast is the standard pattern (see how `recommendations/route.ts` and `metrics/dashboard/route.ts` shape executed-SQL result rows).

    Run the test once locally to confirm both `it` blocks pass green BEFORE proceeding. If either fails:
    - "expected array containing 5 strings, got 4" → Task 1 schema edit is missing one of the index() calls.
    - "expected 1 row, got 0" → drizzle-kit didn't emit the partial UNIQUE; verify `uniqueIndex(...).where(sql\`...\`)` syntax in Task 1.
    - "expected def to contain ARCHIVED_AT IS NULL" → Task 1 used the old single-predicate version; fix the `.where(sql\`... IS NOT NULL AND ... IS NULL\`)` conjunction and regenerate.
  </action>
  <verify>
    <automated>npm run test:run -- src/lib/db/__phase6_indexes__.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/db/__phase6_indexes__.test.ts` exists.
    - Running `npm run test:run -- src/lib/db/__phase6_indexes__.test.ts` exits 0 with both `it` blocks reported passing.
    - Test 2 asserts `def` contains BOTH `'LINKEDIN_URL IS NOT NULL'` and `'ARCHIVED_AT IS NULL'` — pinning the active-rows-only partial predicate.
    - The test file does NOT use the `vi.hoisted + Proxy` db-mock pattern (no route handler under test).
    - The test queries `pg_indexes` (the real Postgres system catalog, exposed by PGlite) — NOT `information_schema.statistics` or other non-portable catalogs.
    - The test runs in under 2 seconds (the 10s pre-push gate from CD-01 must continue to be met — verify via the time reported in vitest's output).
  </acceptance_criteria>
  <done>Test file exists, both assertions pass against the live migration (including the active-rows-only predicate guard), and the suite is fast enough to not jeopardize CD-01.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer machine → local Neon dev branch | DDL execution; bounded by `DATABASE_URL` pointing at dev branch |
| developer machine → production Neon branch | Out of scope for this task — production deploy happens via Vercel's auto-apply hook, not this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | T (tampering) | drizzle/migrations/0008_phase6_indexes.sql | mitigate | Migration is generated by drizzle-kit from declarative schema, not hand-written. Task 2 verification asserts the file contains exactly 6 DDL statements via grep gate; any extra/missing statement fails verification and blocks Wave 2. |
| T-06-02 | I (information disclosure) | DDL execution against production by accident | mitigate | Task 2 acceptance criteria require `DATABASE_URL` to point at the local Neon dev branch before `npm run db:migrate`. Production application is gated by Vercel's deploy hook, which is out of scope for this plan. |
| T-06-03 | D (denial of service) | UNIQUE partial index violation on production data | accept | Single-user app; the developer running the migration owns the DB and can resolve duplicates before re-running. Mitigation already specified in Task 2 Step 3: query `SELECT linkedin_url, COUNT(*) FROM contacts WHERE linkedin_url IS NOT NULL AND archived_at IS NULL GROUP BY 1 HAVING COUNT(*) > 1` first and STOP on conflict — the developer decides whether to merge. The narrowed predicate (active rows only) means archived-row URL duplicates do NOT block the migration. |
| T-06-04 | E (elevation of privilege) | index() / uniqueIndex() Drizzle API | accept | New API surface to this repo but well-documented in Drizzle's official docs; no new attack surface introduced. The partial-index `WHERE` predicate is a Drizzle-managed `sql` template, not string-concatenated user input. |
</threat_model>

<verification>
1. `npx tsc --noEmit` exits 0.
2. `npm run db:migrate` against the local dev branch exits 0 (idempotent — running twice exits 0 the second time too, as drizzle-kit's `__drizzle_migrations` ledger skips applied migrations).
3. `npm run test:run -- src/lib/db/__phase6_indexes__.test.ts` exits 0; both `it` blocks pass.
4. `npm run test:run` (full suite) exits 0 — no regression in existing tests from the schema additions.
5. `git status` shows: 2 modified schema files, 1 new SQL migration, 2 modified/new meta files, 1 new test file. No other files touched.
</verification>

<success_criteria>
- All 6 indexes from D-13 exist as named indexes on PGlite (verified by Task 3) and on the local Neon dev branch (verified by Task 2 `npm run db:migrate` exit 0 + the fact that subsequent route tests in Plans 2-5 will run their PGlite seed → query → assert pattern through these indexes without issue).
- `contacts_linkedin_url_unique_idx` is a partial UNIQUE index whose predicate is `WHERE linkedin_url IS NOT NULL AND archived_at IS NULL` (verified by Task 3's second `it` block) — this is the Plan 4 prerequisite for `ON CONFLICT DO NOTHING` per D-08 AND the CONTEXT §Out-of-scope invariant that re-importing an archived contact creates a fresh active row (not silently skipped).
- Wave 2 (Plans 2-5) is unblocked.
</success_criteria>

<output>
After completion, create `.planning/phases/06-performance/06-01-SUMMARY.md` capturing:
- The 6 index names emitted by drizzle-kit (confirm they match D-13's specification verbatim).
- The renamed migration filename (`0008_phase6_indexes.sql`).
- The exact partial UNIQUE predicate as emitted by drizzle-kit (confirm `WHERE "linkedin_url" IS NOT NULL AND "archived_at" IS NULL`).
- The pg_indexes regression test path and the run time reported by vitest.
- Any duplicate `linkedin_url` rows found among ACTIVE contacts during Step 3 of Task 2 (likely zero; surface for awareness).
</output>
</content>
</invoke>