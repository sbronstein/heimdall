---
phase: 02-test-infrastructure
plan: 04
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/components/layout/app-sidebar.ssr.test.tsx
  - src/components/layout/app-sidebar.hydration.test.tsx
autonomous: true
requirements:
  - TEST-A3
tags:
  - vitest
  - bug-01
  - regression
  - hydration
  - clerk-mock
must_haves:
  truths:
    - "The SSR test asserts no `<div>` descendant appears inside any `<button>` element in the rendered `<AppSidebar />` output"
    - "The SSR test asserts the `UserAvatarProfile` outer `<span class='flex items-center gap-2'>` is present in the rendered output (and that the mocked user's `fullName` and `emailAddresses[0].emailAddress` strings appear in the SSR HTML, proving the component rendered unconditionally — no `{user && ...}` gating)"
    - "The hydration test asserts `console.error` was NOT called with any argument matching `/hydrat/i` or `/did not match/i` during `hydrateRoot` of `<AppSidebar />` in jsdom"
    - "Both regression files pass with the current (fixed) source under `npm run test:run`"
  artifacts:
    - path: "src/components/layout/app-sidebar.ssr.test.tsx"
      provides: "SSR structural assertions for BUG-01 (node environment): no `<div>` in `<button>`, `UserAvatarProfile` markup present"
      contains: "renderToString"
    - path: "src/components/layout/app-sidebar.hydration.test.tsx"
      provides: "Hydration mount with console.error spy (jsdom environment): no hydration warnings"
      contains: "@vitest-environment jsdom"
  key_links:
    - from: "src/components/layout/app-sidebar.ssr.test.tsx"
      to: "src/components/layout/app-sidebar.tsx"
      via: "renderToString(<AppSidebar />) → parse output for structural assertions"
      pattern: "renderToString"
    - from: "src/components/layout/app-sidebar.hydration.test.tsx"
      to: "src/components/layout/app-sidebar.tsx"
      via: "hydrateRoot(container, <AppSidebar />) inside jsdom; spy on console.error"
      pattern: "hydrateRoot"
    - from: "Both test files"
      to: "@clerk/nextjs"
      via: "vi.mock('@clerk/nextjs', ...) returning useUser + SignOutButton stubs (D-16)"
      pattern: "vi\\.mock\\('@clerk/nextjs'"
---

<objective>
Pin the BUG-01 fix with two regression tests per D-14: one SSR-only structural test (default `node` env) that catches re-introduction of `<div>` inside `<button>` and `{user && ...}` gating of `UserAvatarProfile`, and one hydration-mount test (jsdom env via per-file pragma) that catches re-introduction of hydration warnings via a `console.error` spy.

Purpose: Phase 1 fixed BUG-01 in `src/components/layout/app-sidebar.tsx` and `src/components/user-avatar-profile.tsx`. Without these regression tests, a future refactor could re-introduce the crash. The two-file split honors the pattern-mapper caveat: `@vitest-environment` pragmas are file-scope only, so SSR (node) and hydration (jsdom) cannot share one file. CONTEXT.md D-14 originally said "Two tests, one file"; the deviation rationale is captured in D-14's implementation note in 02-CONTEXT.md.

Output: Two `.test.tsx` files colocated with `app-sidebar.tsx`. Both mock `@clerk/nextjs` and `next/navigation`. Both run under a single `npm run test:run` invocation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-test-infrastructure/02-CONTEXT.md
@.planning/phases/02-test-infrastructure/02-PATTERNS.md
@CLAUDE.md
@bug.md
@src/components/layout/app-sidebar.tsx
@src/components/user-avatar-profile.tsx
@.planning/phases/01-critical-bug-fix/01-CONTEXT.md

<interfaces>
<!-- Component shapes and external dependencies the tests must mock. -->

From src/components/layout/app-sidebar.tsx:
```
'use client';
// Imports:
//   useUser, SignOutButton from '@clerk/nextjs'
//   usePathname, useRouter from 'next/navigation'
// Default export: function AppSidebar()
// Renders <UserAvatarProfile user={user ?? null} /> at line 148 (inside SidebarMenuButton → <button>)
// Renders <UserAvatarProfile user={user ?? null} /> at line 164 (inside DropdownMenuLabel → not a <button>)
```

From src/components/user-avatar-profile.tsx:
```
// Named export: UserAvatarProfile
// Outer element: <span className='flex items-center gap-2'>
// Inner: <Avatar>, conditional <span className='grid flex-1 ...'> with fullName + emailAddresses[0]?.emailAddress ?? ''
```

Clerk dependency to mock (D-16):
```
vi.mock('@clerk/nextjs', () => ({
  useUser: () => ({
    user: {
      fullName: 'Steve Bronstein',
      imageUrl: 'https://img.clerk.com/test',
      emailAddresses: [{ emailAddress: 'steve@bronstein.org' }]
    }
  }),
  SignOutButton: ({ children }: { children?: React.ReactNode }) => children ?? null
}));
```

next/navigation dependency to mock:
```
vi.mock('next/navigation', () => ({
  usePathname: () => '/dashboard/overview',
  useRouter: () => ({ push: vi.fn() })
}));
```

Other components rendered by AppSidebar that may need light handling:
- `@/components/ui/sidebar` (Sidebar, SidebarMenuButton, etc.) — these are real shadcn components built on Radix UI; they should render in SSR without a problem because Radix primitives are SSR-safe in this codebase (the app SSRs in production).
- `@/hooks/use-media-query` `useMediaQuery` — returns `{ isOpen: boolean }`. In SSR/jsdom there is no real `window.matchMedia`. Either:
  * Mock the hook: `vi.mock('@/hooks/use-media-query', () => ({ useMediaQuery: () => ({ isOpen: true }) }));`
  * Or stub `window.matchMedia` in the jsdom test only.
  Prefer mocking the hook so the SSR test (node env, no window at all) also works.
- `@/hooks/use-nav` `useFilteredNavItems` — returns the filtered nav items array. Mock to return `navItems` unchanged: `vi.mock('@/hooks/use-nav', () => ({ useFilteredNavItems: (items: unknown) => items }));`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SSR structural regression test (BUG-01) in node environment</name>
  <files>src/components/layout/app-sidebar.ssr.test.tsx</files>
  <read_first>
    - src/components/layout/app-sidebar.tsx (full file, 199 lines) — confirm line 148 (UserAvatarProfile inside SidebarMenuButton) and the `user ?? null` pattern
    - src/components/user-avatar-profile.tsx (full file, 39 lines) — confirm outer `<span className='flex items-center gap-2'>`
    - bug.md — root cause description
    - .planning/phases/02-test-infrastructure/02-CONTEXT.md (D-14 bullet 1 + implementation note, D-16)
    - .planning/phases/02-test-infrastructure/02-PATTERNS.md ("`src/components/layout/app-sidebar.test.tsx`" section, specifically the SSR assertions block)
  </read_first>
  <behavior>
    - `renderToString(<AppSidebar />)` produces a non-empty HTML string with no thrown error.
    - No `<button>` element in the output has a `<div>` descendant. Implementation: parse the HTML via `linkedom`-style parsing or, more reliably, use `jsdom` directly (jsdom is available as a transitive dep of vitest, OR install it explicitly in 02-01 if needed — see Action below for how to detect and handle). For each `<button>`, `button.querySelector('div')` must be `null`.
      * NOTE: This assertion runs in the default `node` environment, where there is no `document` global. Either import the JSDOM API directly (`import { JSDOM } from 'jsdom'`) and parse the SSR string, OR use a regex assertion as a fallback. Prefer JSDOM parsing — it's deterministic and catches nested cases the regex would miss.
    - The SSR HTML contains the string `'flex items-center gap-2'` (the outer `<span>` className of `UserAvatarProfile`). This catches re-introduction of `{user && <UserAvatarProfile />}` gating because `useUser` is mocked to return a user, so the component must render unconditionally for `flex items-center gap-2` to appear.
    - The SSR HTML contains `'Steve Bronstein'` and `'steve@bronstein.org'` (from the mocked Clerk user — confirms `UserAvatarProfile` rendered with the user prop, not as a gated-out empty branch).
  </behavior>
  <action>
    Create `src/components/layout/app-sidebar.ssr.test.tsx`. No pragma — runs in default `node` env per D-09.

    1. Add `jsdom` to devDependencies if not already present. Verify with `node -e "require.resolve('jsdom')"` first — vitest typically pulls it in as a transitive optional dep, but in a node-default config it may not be auto-installed. If `require.resolve` fails, run `npm install --save-dev jsdom` as part of this task. (One-line check + conditional install. Document in summary which path was taken.)

    2. Mock order — these all go at the top of the file, BEFORE the route module import:
       ```
       vi.mock('@clerk/nextjs', () => ({
         useUser: () => ({
           user: {
             fullName: 'Steve Bronstein',
             imageUrl: 'https://img.clerk.com/test',
             emailAddresses: [{ emailAddress: 'steve@bronstein.org' }]
           }
         }),
         SignOutButton: ({ children }) => children ?? null
       }));
       vi.mock('next/navigation', () => ({
         usePathname: () => '/dashboard/overview',
         useRouter: () => ({ push: vi.fn() })
       }));
       vi.mock('@/hooks/use-media-query', () => ({ useMediaQuery: () => ({ isOpen: true }) }));
       vi.mock('@/hooks/use-nav', () => ({ useFilteredNavItems: (items: unknown) => items }));
       ```

    3. Imports:
       - `import { renderToString } from 'react-dom/server';`
       - `import React from 'react';`
       - `import { JSDOM } from 'jsdom';`
       - `import AppSidebar from '@/components/layout/app-sidebar';` (default export per source line 49)

    4. Single `describe('AppSidebar SSR structural (BUG-01 regression)', ...)` with these `it()` blocks:
       - `it('SSR renders without throwing')` — `expect(() => renderToString(<AppSidebar />)).not.toThrow();`
       - `it('contains no <div> inside any <button>')` — parse via JSDOM, iterate `document.querySelectorAll('button')`, assert `btn.querySelector('div') === null` for every one. The failure message must include the offending button's outerHTML (truncated) for debuggability.
       - `it('renders UserAvatarProfile markup (no {user && ...} gating)')` — assert the SSR string includes `'flex items-center gap-2'`.
       - `it('renders the mocked user fullName and email')` — assert the SSR string includes both `'Steve Bronstein'` and `'steve@bronstein.org'`.

    5. Wrap the SSR call in a single `beforeAll`:
       ```
       let html: string;
       let dom: JSDOM;
       beforeAll(() => {
         html = renderToString(<AppSidebar />);
         dom = new JSDOM(html);
       });
       ```
       Each `it()` consumes the shared `html` / `dom`.

    Style: single quotes, 2-space indent, semicolons. Use the file extension `.test.tsx` so JSX is recognized.
  </action>
  <verify>
    <automated>npx vitest run src/components/layout/app-sidebar.ssr.test.tsx --reporter=basic</automated>
  </verify>
  <done>
    File exists. All 4 `it()` blocks pass. `jsdom` is available (either pre-existing transitive dep or newly installed). The structural assertion uses JSDOM parsing, not a regex, so deeply-nested cases are caught. No hydration logic appears in this file (that's Task 2).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Hydration mount regression test (BUG-01) in jsdom environment</name>
  <files>src/components/layout/app-sidebar.hydration.test.tsx</files>
  <read_first>
    - src/components/layout/app-sidebar.tsx (already read in Task 1 — confirm `'use client'` directive on line 1)
    - .planning/phases/02-test-infrastructure/02-CONTEXT.md (D-14 bullet 2 + implementation note, D-15, D-16)
    - .planning/phases/02-test-infrastructure/02-PATTERNS.md ("Hydration mount assertions" subsection)
    - The newly-created `src/components/layout/app-sidebar.ssr.test.tsx` (created in Task 1) — copy the mock blocks verbatim
  </read_first>
  <behavior>
    - The very first line of the file is exactly `// @vitest-environment jsdom`. Vitest only honors this pragma when it appears as a comment at the top of the file (D-09).
    - SSR-rendering `<AppSidebar />` then hydrating into a jsdom container does NOT trigger any `console.error` call whose first argument matches `/hydrat/i` or `/did not match/i`.
    - Non-hydration console.error calls (e.g. an unrelated Radix UI warning) do NOT fail the test — the assertion filters specifically for hydration patterns.
    - The hydration assertion gates on React having actually committed: the test awaits a microtask flush (`await new Promise((r) => setTimeout(r, 0))`) before reading the spy.
  </behavior>
  <action>
    Create `src/components/layout/app-sidebar.hydration.test.tsx`.

    1. **First line of the file MUST be:**
       ```
       // @vitest-environment jsdom
       ```
       No blank lines before this. The pragma is file-scope; Vitest will refuse to honor it if anything precedes it.

    2. Mock blocks — copy verbatim from Task 1 (same Clerk, next/navigation, use-media-query, use-nav mocks). These belong below the pragma but above all `import` statements that exercise the mocked modules.

    3. Imports:
       - `import { renderToString } from 'react-dom/server';`
       - `import { hydrateRoot } from 'react-dom/client';`
       - `import React from 'react';`
       - `import AppSidebar from '@/components/layout/app-sidebar';`

    4. Single `describe('AppSidebar hydration mount (BUG-01 regression)', ...)` with one `it('hydrates without React hydration warnings')`:
       ```
       const html = renderToString(<AppSidebar />);
       const container = document.createElement('div');
       container.innerHTML = html;
       document.body.appendChild(container);

       const errorSpy = vi.spyOn(console, 'error').mockImplementation(() => {});

       hydrateRoot(container, <AppSidebar />);
       // Flush so React commits and any hydration mismatch surfaces.
       await new Promise((resolve) => setTimeout(resolve, 0));

       const hydrationCalls = errorSpy.mock.calls.filter(([firstArg]) =>
         typeof firstArg === 'string' && (/hydrat/i.test(firstArg) || /did not match/i.test(firstArg))
       );

       expect(hydrationCalls, `Unexpected hydration warnings:\n${hydrationCalls.map((c) => c.join(' ')).join('\n')}`).toEqual([]);

       errorSpy.mockRestore();
       ```

    5. The custom failure message (second arg to `expect`) MUST include the offending error messages so a regression failure shows what React complained about, not just "expected [] but got [...]".

    6. Add an `afterEach` that calls `vi.restoreAllMocks()` and removes the container: `document.body.innerHTML = '';` — defensive isolation in case this file ever grows more `it()` blocks.
  </action>
  <verify>
    <automated>npx vitest run src/components/layout/app-sidebar.hydration.test.tsx --reporter=basic && head -1 src/components/layout/app-sidebar.hydration.test.tsx | grep -q "// @vitest-environment jsdom"</automated>
  </verify>
  <done>
    File exists with the `// @vitest-environment jsdom` pragma as the literal first line. The hydration test passes against the current (fixed) source. The failure message of the assertion includes the offending error strings so future regression diagnoses are one-glance. `afterEach` restores mocks and cleans the document body.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test-runtime → @clerk/nextjs | Clerk is fully mocked. The real Clerk runtime (`useUser`, `SignOutButton` components) is never imported, so no Clerk publishable key, no fetch to clerk.com, no auth state. |
| test-runtime → react | React render output is asserted via string + DOM parsing only. No external network. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-09 | Information disclosure | `vi.mock('@clerk/nextjs')` | mitigate | Mock returns hardcoded `'steve@bronstein.org'` — this is the project owner's known address and is already a public single-user lock per `src/proxy.ts`; the project README documents the address. No additional secret exposure. |
| T-02-10 | Information disclosure | JSDOM parsing of SSR HTML | accept | JSDOM is in-process and does not load external resources by default. No `<script>` execution, no resource fetching. Low risk. |
| T-02-11 | Tampering | `console.error` spy in hydration test | mitigate | Spy is restored via `mockRestore()` and `vi.restoreAllMocks()` in `afterEach`. Cross-test leakage structurally prevented. |
</threat_model>

<verification>
1. `npm run test:run` exits 0 with both new files passing.
2. `head -1 src/components/layout/app-sidebar.hydration.test.tsx` returns exactly `// @vitest-environment jsdom`.
3. `head -1 src/components/layout/app-sidebar.ssr.test.tsx` does NOT contain `@vitest-environment` (file should run in default node env per D-09).
4. Sanity regression check (executor performs manually after both tests pass):
   - Temporarily edit `src/components/user-avatar-profile.tsx` line 19 to use a `<div>` instead of `<span>` as the outer element. Run `npx vitest run src/components/layout/app-sidebar.ssr.test.tsx` — it should FAIL on the "no <div> inside any <button>" assertion. Revert the edit. Document the result in 02-04-SUMMARY.md.
</verification>

<success_criteria>
- [ ] `src/components/layout/app-sidebar.ssr.test.tsx` exists; runs in node env; has 4 `it()` blocks covering: no-throw render, no-div-in-button, UserAvatarProfile markup present, mocked user fullName/email present.
- [ ] `src/components/layout/app-sidebar.hydration.test.tsx` exists; first line is the `// @vitest-environment jsdom` pragma; has 1 `it()` covering hydration with the filtered `console.error` spy.
- [ ] Both files mock `@clerk/nextjs`, `next/navigation`, `@/hooks/use-media-query`, `@/hooks/use-nav`.
- [ ] The structural assertion uses JSDOM parsing (not a regex) for nested-element correctness.
- [ ] The hydration assertion filters for `/hydrat/i` and `/did not match/i` patterns; other console.error calls do not fail the test (per D-15: only React's hydration warnings are gated).
- [ ] `npm run test:run` exits 0.
</success_criteria>

<output>
After completion, create `.planning/phases/02-test-infrastructure/02-04-SUMMARY.md` capturing:
- Whether `jsdom` was newly installed or pre-existing
- The regression sanity-check result (introduce-a-`<div>`, see SSR test fail, revert)
- Any unexpected console.error noise the hydration test surfaced (Radix UI warnings, etc.) and confirmation that the filter correctly ignored them
- Confirmation that no real Clerk runtime was imported (grep for `@clerk/themes` or actual Clerk imports outside `vi.mock`)
</output>
</output>
