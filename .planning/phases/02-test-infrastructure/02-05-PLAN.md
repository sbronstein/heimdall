---
phase: 02-test-infrastructure
plan: 05
type: execute
wave: 3
depends_on:
  - 02-02
  - 02-03
  - 02-04
files_modified:
  - .husky/pre-push
autonomous: false
requirements:
  - TEST-A1
tags:
  - husky
  - pre-push
  - ci-local
must_haves:
  truths:
    - "Running `git push` (which fires `.husky/pre-push`) executes both `bun run build` AND `npm run test:run`, in that order"
    - "If any test fails, the push is blocked (exit code propagates)"
    - "If the test suite takes longer than ~10s, the executor leaves the hook unchanged and documents the decision in the summary (per CD-01)"
  artifacts:
    - path: ".husky/pre-push"
      provides: "Local-only quality gate running both build and test:run before every push"
      contains: "npm run test:run"
  key_links:
    - from: ".husky/pre-push"
      to: "package.json test:run script"
      via: "npm run test:run invocation appended after the existing bun run build"
      pattern: "npm run test:run"
---

<objective>
Per CD-01, integrate `npm run test:run` into the existing Husky pre-push hook IF AND ONLY IF the full Phase 2 test suite executes in under ~10 seconds on the developer's machine. Per CD-01 the recommended default is yes; the executor's first task is to measure, then decide.

Purpose: Closes the CI-side half of TEST-A1's success criterion ("`npm test` runs a Vitest suite ... and exits 0 on a clean checkout"). The hook ensures the suite actually runs before every push, not just when the developer remembers.

Output: Either an updated `.husky/pre-push` containing both build and test invocations, OR an unchanged hook plus a summary note explaining why integration was skipped.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-test-infrastructure/02-CONTEXT.md
@.planning/codebase/TESTING.md
@.husky/pre-push
@package.json

<interfaces>
<!-- Current state of the pre-push hook -->

From .husky/pre-push (single line, full file):
```
bun run build
```

From package.json scripts (after 02-01):
```
"test": "vitest"
"test:run": "vitest run"
"build": "next build"
```

Husky v9 convention: `.husky/pre-push` is a plain shell script. The shebang `#!/usr/bin/env sh` is NOT required in v9 (Husky now wraps execution itself). Multi-line commands are just newline-separated shell statements; if any line exits non-zero, the hook fails (default `sh` behavior with `set -e` semantics applied by Husky's wrapper).
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Measure suite runtime and decide on hook integration</name>
  <read_first>
    - .husky/pre-push (full file, 1 line)
    - .planning/phases/02-test-infrastructure/02-CONTEXT.md (CD-01 — "recommended default: yes, but only if the full suite stays under ~10s")
  </read_first>
  <action>
    Run `npm run test:run` from the repo root, time it with `time` or by reading Vitest's reporter footer (`Duration: Xs`).

    Record the actual duration. Then decide based on this threshold:
    - **Suite runs in ≤ 10 seconds (wall-clock):** Proceed to Task 2 — integrate `npm run test:run` into the pre-push hook.
    - **Suite runs in > 10 seconds:** Skip Task 2. Document the timing and decision in the SUMMARY. The hook stays as `bun run build` only.

    This is a checkpoint:decision rather than a checkpoint:human-action because the rule is mechanical — if the timing is under 10s, integrate; otherwise don't. The executor reports the timing and the chosen path; the user can override if they want CI-only enforcement regardless.
  </action>
  <decision>
    Should the pre-push hook run `npm run test:run` in addition to `bun run build`?
  </decision>
  <context>
    Trade-off: faster pre-push feedback at the cost of slower push command. Heimdall is a solo developer's personal CRM; per CLAUDE.md the user values catching regressions fast. The 10s ceiling is a developer-flow heuristic — beyond that, friction starts to dominate value.
  </context>
  <options>
    <option id="integrate">
      <name>Integrate (suite is fast)</name>
      <pros>Every push runs the full test suite. BUG-01 regression cannot slip past local. TEST-A1's "exits 0 on a clean checkout" is enforced automatically.</pros>
      <cons>Adds ~suite-runtime seconds to every `git push`.</cons>
    </option>
    <option id="skip">
      <name>Skip (suite is slow)</name>
      <pros>Push remains fast. Tests still runnable via `npm test`.</pros>
      <cons>Developer must remember to run tests before pushing. CI catches it later, but feedback loop is longer.</cons>
    </option>
  </options>
  <resume-signal>Report timing + chosen path; if 'integrate', proceed to Task 2; if 'skip', emit a brief summary and finish.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Append npm run test:run to .husky/pre-push (only if Task 1 decided 'integrate')</name>
  <files>.husky/pre-push</files>
  <read_first>
    - .husky/pre-push (current contents — single line `bun run build`)
    - .planning/phases/02-test-infrastructure/02-CONTEXT.md (CD-01)
    - .planning/phases/02-test-infrastructure/02-01-SUMMARY.md (confirm `test:run` script exists in package.json)
  </read_first>
  <action>
    Skip this task entirely if Task 1's decision was 'skip' — record the skip in the summary and finish.

    Otherwise, edit `.husky/pre-push` to add `npm run test:run` on its own line AFTER the existing `bun run build` line. Final file content:
    ```
    bun run build
    npm run test:run
    ```

    Do NOT add a shebang (Husky v9 doesn't need it — confirm by checking that `.husky/pre-commit` also has no shebang). Do NOT add `set -e` — Husky's wrapper handles failure propagation. Do NOT add comments. Two lines, exactly.

    Order matters: `bun run build` runs first because it's the existing gate and its failure mode (type errors) is cheaper to surface than test failures. If the build is broken, tests would also fail in a noisier way.

    File permissions: Husky manages the executable bit. Use `chmod +x .husky/pre-push` after editing if needed (Husky v9 doesn't strictly require the executable bit when invoked via its wrapper, but it's harmless to set).
  </action>
  <verify>
    <automated>cat .husky/pre-push | grep -c "npm run test:run" | grep -q '^1$' && cat .husky/pre-push | grep -c "bun run build" | grep -q '^1$' && echo OK</automated>
  </verify>
  <done>
    `.husky/pre-push` contains both `bun run build` (line 1) and `npm run test:run` (line 2). No shebang, no comments, no extra commands. A manual `bash .husky/pre-push` invocation from the repo root succeeds when the suite is green and fails when a test is broken.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local-git → remote | The pre-push hook is local-machine-only. It runs in the developer's shell, has access to the working tree, and gates the push. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-12 | Denial of service | `.husky/pre-push` | accept | A slow test suite would slow pushes. Task 1 explicitly measures and gates on a 10s ceiling per CD-01. Low risk — solo developer can bypass with `git push --no-verify` if ever needed. |
| T-02-13 | Information disclosure | Test run during pre-push | mitigate | `npm run test:run` invokes `vitest run` which uses PGlite — no network, no real DB. Confirmed by 02-01 / 02-03 threat models. |
</threat_model>

<verification>
1. (If integrated) `bash .husky/pre-push` exits 0 when the working tree is clean — both build and tests pass.
2. (If integrated) Intentional test break sanity-check: temporarily edit a passing test to `expect(true).toBe(false)`, run `bash .husky/pre-push`, observe non-zero exit, revert. Document in summary.
3. (If skipped) The .husky/pre-push file is unchanged (`bun run build` only) and the summary explains the timing measurement.
</verification>

<success_criteria>
- [ ] Task 1 records the measured suite runtime in the summary.
- [ ] If suite ≤ 10s: `.husky/pre-push` contains both `bun run build` and `npm run test:run`, and the manual run + intentional-break sanity check both pass.
- [ ] If suite > 10s: `.husky/pre-push` is unchanged and the summary explains the decision.
- [ ] No new dependencies added. No package.json changes (the `test:run` script was added in 02-01).
</success_criteria>

<output>
After completion, create `.planning/phases/02-test-infrastructure/02-05-SUMMARY.md` capturing:
- The measured runtime of `npm run test:run` (wall-clock)
- The decision (integrated or skipped)
- If integrated: the intentional-break sanity check result
- If skipped: the rationale and a one-line note for future re-evaluation when the suite grows / shrinks
</output>
