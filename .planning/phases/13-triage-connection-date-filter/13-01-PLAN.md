---
phase: 13-triage-connection-date-filter
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/contacts/lib/connection-year.ts
  - src/features/contacts/lib/connection-year.test.ts
  - src/features/contacts/components/triage/connection-year-filter.tsx
  - src/features/contacts/components/triage/triage-workflow.tsx
autonomous: true
requirements: [TRGE-01]

must_haves:
  truths:
    - "D-03: Triage shows a row of year buttons derived from the actual linkedinConnectionDate values of the loaded untriaged contacts"
    - "D-01: Clicking a single year narrows the triage queue to that year; clicking a second year sets an inclusive two-year range and the queue shows only contacts in that range"
    - "D-02: The year control is keyboard-navigable (arrow keys move, Enter selects), matching the sibling triage button-bars"
    - "D-04: Filtering is client-side over the already-fetched untriaged set; the triage RSC page.tsx and its DB query are unchanged"
    - "D-05: The selected year/range is written to the URL via nuqs (connectionYearStart/connectionYearEnd) and survives a page reload"
    - "D-07: A live 'N contacts' count reflects the active filter; an empty-state message shows when nothing matches"
    - "D-06: Clicking an already-selected single year deselects it; an explicit Clear / 'All years' control restores the full untriaged queue"
    - "D-08: The year/range control is a standalone reusable component owning the nuqs URL state, ready for Phase 14 reuse"
  artifacts:
    - path: "src/features/contacts/lib/connection-year.ts"
      provides: "Pure year-derivation + range-filter + param-clamp helpers (reusable, testable)"
      exports: ["deriveConnectionYears", "filterByConnectionYearRange", "clampConnectionYear", "MIN_CONNECTION_YEAR"]
    - path: "src/features/contacts/lib/connection-year.test.ts"
      provides: "Vitest coverage of derivation, range filtering, and param clamping"
      contains: "deriveConnectionYears"
    - path: "src/features/contacts/components/triage/connection-year-filter.tsx"
      provides: "Button-bar control owning the connectionYearStart/connectionYearEnd nuqs URL state with click-to-range + clear"
      exports: ["ConnectionYearFilter", "ConnectionYearFilterHandle"]
      min_lines: 60
    - path: "src/features/contacts/components/triage/triage-workflow.tsx"
      provides: "In-memory queue narrowing driven by the nuqs range, live count, empty state"
      contains: "ConnectionYearFilter"
  key_links:
    - from: "src/features/contacts/components/triage/triage-workflow.tsx"
      to: "src/features/contacts/components/triage/connection-year-filter.tsx"
      via: "import + render above TriageCard"
      pattern: "ConnectionYearFilter"
    - from: "src/features/contacts/components/triage/connection-year-filter.tsx"
      to: "nuqs URL params connectionYearStart/connectionYearEnd"
      via: "useQueryStates with parseAsInteger"
      pattern: "useQueryStates"
    - from: "src/features/contacts/components/triage/triage-workflow.tsx"
      to: "src/features/contacts/lib/connection-year.ts"
      via: "deriveConnectionYears + filterByConnectionYearRange"
      pattern: "filterByConnectionYearRange"
---

<objective>
Add an additive connection-year / two-year-range filter to the existing triage workflow so the owner can narrow the untriaged queue to a cohort of LinkedIn connections by connection year (the driving use case: surface "ID.me colleagues from 2021–2022" together). Filtering is client-side over the already-fetched untriaged set (D-04); the selection lives in the URL via nuqs so it survives reload (D-05); the year control is extracted as a reusable component so Phase 14's campaign builder can consume it (D-08).

Purpose: Delivers TRGE-01 — the owner triages by cohort instead of one undifferentiated stream.
Output: A tested pure-logic helper module, a reusable `ConnectionYearFilter` button-bar control, and the wiring in `triage-workflow.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-triage-connection-date-filter/13-CONTEXT.md
@.planning/phases/13-triage-connection-date-filter/13-PATTERNS.md

<interfaces>
<!-- Contracts the executor needs. No codebase exploration required. -->

From src/lib/domain/types.ts:
  export type Contact = typeof contacts.$inferSelect
  // Contact.linkedinConnectionDate is `Date | null`
  // Contact.id is string; Contact.triagedAt, lastContactDate, howMet already used in triage-workflow

From drizzle/schema/contacts.ts:
  linkedinConnectionDate: timestamp('linkedin_connection_date')  // indexed

Existing cross-phase param contract — src/app/api/contacts/route.ts (lines 67-68, 107-122):
  connectionYearStart  -> gte(linkedinConnectionDate, new Date(`${start}-01-01`))
  connectionYearEnd    -> lte(linkedinConnectionDate, new Date(`${end}-12-31T23:59:59`))
  // Triage filters CLIENT-SIDE and does NOT call this API, but MUST reuse these exact
  // param names so Phase 14 (D-08) can drive GET /api/contacts with the identical control.

nuqs (already a project pattern; NuqsAdapter mounted app-wide at src/app/layout.tsx line 59):
  import { parseAsInteger, useQueryStates } from 'nuqs'
  const [{ connectionYearStart, connectionYearEnd }, setRange] = useQueryStates({
    connectionYearStart: parseAsInteger,
    connectionYearEnd: parseAsInteger,
  })
  // setRange({ connectionYearStart: null, connectionYearEnd: null }) clears both from the URL

Sibling button-bar control contract (match for consistency) — last-contact-year.tsx:
  'use client' at top; forwardRef<Handle, Props>; Handle = { focus: () => void }
  useImperativeHandle(ref, () => ({ focus: () => containerRef.current?.focus() }))
  Container tabIndex={0} + onKeyDown; child buttons tabIndex={-1}; named PascalCase export, no default export
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure connection-year helpers + Vitest test (TRGE-01)</name>
  <files>src/features/contacts/lib/connection-year.ts, src/features/contacts/lib/connection-year.test.ts</files>
  <read_first>
    - src/features/contacts/components/triage/triage-workflow.tsx (consumer of these helpers — confirms Contact[] shape, currentIndex/total usage at lines 45-46)
    - src/lib/domain/types.ts (the Contact type; linkedinConnectionDate is Date | null)
    - src/lib/api/filters.test.ts (existing Vitest style in this repo — import shape, describe/it/expect)
    - drizzle/schema/contacts.ts (linkedinConnectionDate column)
    - .planning/phases/13-triage-connection-date-filter/13-PATTERNS.md (year-derivation + range snippets, lines 91-104)
  </read_first>
  <behavior>
    - deriveConnectionYears: given contacts, returns the DISTINCT set of `linkedinConnectionDate` calendar years (via new Date(d).getFullYear()), nulls dropped, sorted DESCENDING (newest first). All-null or empty input → [].
    - filterByConnectionYearRange(contacts, start, end): start==null && end==null → returns the input array unchanged (full queue). Either bound set → keep contacts whose connection year is within the inclusive [min(lo,hi), max(lo,hi)] window where lo = start ?? end and hi = end ?? start (so start-only behaves as a single-year filter). Contacts with null linkedinConnectionDate are always excluded when any bound is set.
    - clampConnectionYear(value): returns null for null/NaN/non-finite or out-of-bounds values (< MIN_CONNECTION_YEAR or > currentYear+1); otherwise returns the integer year. MIN_CONNECTION_YEAR = 1990.
    - Test cases: distinct+sorted-desc derivation; null/empty → []; single-year filter (start set, end null) keeps only that year; two-year inclusive range keeps both years and excludes adjacent years; both-null returns all incl. null-date contacts; null-date contacts excluded when filtering; clamp rejects 99999999 and 1900 (→ null) but accepts a real year; clamp passes null through as null.
  </behavior>
  <action>Create `src/features/contacts/lib/connection-year.ts` exporting `MIN_CONNECTION_YEAR` (const 1990), `deriveConnectionYears`, `filterByConnectionYearRange`, and `clampConnectionYear`. Use generic constraint `<T extends { linkedinConnectionDate: Date | null }>` on both data functions so they work with `Contact` without importing it at runtime (use `import type` only if referencing Contact in tests). Pure functions, no React, no DB, no I/O. Match repo style: single quotes, 2-space indent, no semicolon omission, no trailing commas, named exports. Then write `connection-year.test.ts` covering every case listed in <behavior> using the same Vitest `describe/it/expect` style as `src/lib/api/filters.test.ts`. Build a small fixture array of `{ id, linkedinConnectionDate }` objects (Dates for 2019/2021/2021/2022 and one null) — no DB.</action>
  <verify>
    <automated>npx vitest run src/features/contacts/lib/connection-year.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/features/contacts/lib/connection-year.test.ts` passes with all cases from <behavior> green.
    - Source asserts: `connection-year.ts` exports `deriveConnectionYears`, `filterByConnectionYearRange`, `clampConnectionYear`, `MIN_CONNECTION_YEAR` (grep -E 'export (function|const) (deriveConnectionYears|filterByConnectionYearRange|clampConnectionYear)' and 'MIN_CONNECTION_YEAR').
    - Behavior: filterByConnectionYearRange(fixture, 2021, 2022) returns exactly the 2021+2021+2022 entries (3), excludes 2019 and the null-date entry (maps to success criteria #1 single-year and #2 two-year-range).
    - Behavior: filterByConnectionYearRange(fixture, null, null) returns the full fixture including the null-date entry (maps to criterion #4 clear-restores-all).
    - Behavior: clampConnectionYear(99999999) === null and clampConnectionYear(1900) === null (URL-param hardening, T-13-01).
    - No fenced code / no DB import in `connection-year.ts`.
  </acceptance_criteria>
  <done>Helper module + passing Vitest test exist; range/single/clear/clamp behaviors verified by automated test.</done>
</task>

<task type="auto">
  <name>Task 2: ConnectionYearFilter button-bar control with nuqs URL state + click-to-range (D-01, D-02, D-05, D-06, D-08)</name>
  <files>src/features/contacts/components/triage/connection-year-filter.tsx</files>
  <read_first>
    - src/features/contacts/components/triage/last-contact-year.tsx (PRIMARY analog — forwardRef handle, keyboard nav, button-row + selected styling, lines 1-118)
    - src/features/contacts/components/contact-table/index.tsx (minimal useQueryState usage)
    - src/hooks/use-data-table.ts (useQueryStates + parseAsInteger pattern)
    - .planning/phases/13-triage-connection-date-filter/13-PATTERNS.md (nuqs + click-to-range guidance, lines 21-81)
    - src/lib/utils.ts (cn helper)
  </read_first>
  <action>Create `src/features/contacts/components/triage/connection-year-filter.tsx` ('use client'). Export `ConnectionYearFilter` (named PascalCase, no default) and `ConnectionYearFilterHandle` = `{ focus: () => void }`. Props: `ConnectionYearFilterProps = { years: number[] }` (the data-derived year list from the parent — never hardcode or fetch years, contrast last-contact-year's hardcoded array). The control OWNS the URL state via `useQueryStates({ connectionYearStart: parseAsInteger, connectionYearEnd: parseAsInteger })` from 'nuqs' (exact param names — cross-phase contract for D-08). Implement click-to-range per D-01/D-06 with this state machine in an `onYearClick(year)` handler: (a) no start set → set `{ connectionYearStart: year, connectionYearEnd: null }`; (b) start set, no end → if year === start clear both (deselect, D-06) else set `{ connectionYearStart: min(start,year), connectionYearEnd: max(start,year) }`; (c) range already set (both non-null) → begin a fresh single selection `{ connectionYearStart: year, connectionYearEnd: null }`. Render the year buttons (newest-first, as provided) plus a trailing "All years" / Clear button (analogous to last-contact-year's EARLIER_SENTINEL extra option) that calls `setRange({ connectionYearStart: null, connectionYearEnd: null })`. `isSelected(year)` is a range-membership test using lo = start ?? end, hi = end ?? start: `start == null && end == null ? false : year >= min && year <= max`. Reuse last-contact-year's container/button classNames verbatim (container `flex gap-2 rounded-md outline-none focus-visible:ring-2 ... p-1`; per-button selected vs not + focus-ring via `cn`). Implement keyboard nav (ArrowRight/Down next, ArrowLeft/Up prev, Enter selects focused year through `onYearClick`) and forwardRef `focus()` via `useImperativeHandle` to match the three sibling controls. Wrap with a `<label className='text-sm font-medium'>Connection Year</label>` like the siblings. No fenced code in this plan body — implement from the analog.</action>
  <verify>
    <automated>npx tsc --noEmit && npx eslint src/features/contacts/components/triage/connection-year-filter.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` passes (no type errors introduced).
    - `npx eslint src/features/contacts/components/triage/connection-year-filter.tsx` reports no errors.
    - Source asserts: file begins with `'use client'`; grep `export const ConnectionYearFilter`, `export interface ConnectionYearFilterHandle`, `useQueryStates`, `connectionYearStart`, `connectionYearEnd`, `parseAsInteger`.
    - Source asserts: a clear/reset affordance exists that sets both params to null (grep for `connectionYearStart: null` in the same setRange call) — backs criterion #4.
    - Source asserts: years come from the `years` prop, NOT a hardcoded literal array of years (no `const years = [20` literal in this file).
    - No default export.
  </acceptance_criteria>
  <done>Reusable `ConnectionYearFilter` compiles, lints clean, owns the connectionYearStart/connectionYearEnd URL state, implements click-to-range + clear, and matches the sibling button-bar contract.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the filter into triage-workflow — derive years, narrow queue, live count + empty state (D-03, D-04, D-07)</name>
  <files>src/features/contacts/components/triage/triage-workflow.tsx</files>
  <read_first>
    - src/features/contacts/components/triage/triage-workflow.tsx (the file being modified — current total/current at lines 45-46, currentIndex reset useEffect lines 49-56, completion branch lines 191-223, render block lines 225-284)
    - src/features/contacts/components/triage/connection-year-filter.tsx (the control from Task 2 — its props + the nuqs keys it owns)
    - src/features/contacts/lib/connection-year.ts (helpers from Task 1)
    - .planning/phases/13-triage-connection-date-filter/13-PATTERNS.md (wiring guidance lines 85-111)
  </read_first>
  <action>Modify `src/features/contacts/components/triage/triage-workflow.tsx`. Import `ConnectionYearFilter` (+ its Handle type) from `./connection-year-filter` and `deriveConnectionYears`, `filterByConnectionYearRange`, `clampConnectionYear` from `@/features/contacts/lib/connection-year`. Import `parseAsInteger, useQueryStates` from 'nuqs'. Read the same URL keys the control writes: `const [{ connectionYearStart, connectionYearEnd }, ] = useQueryStates({ connectionYearStart: parseAsInteger, connectionYearEnd: parseAsInteger })` (nuqs keeps the two hooks in sync — pattern (a) from PATTERNS line 111; the control owns the write, the workflow reads). Add `const connectionYears = useMemo(() => deriveConnectionYears(contacts), [contacts])`. Add `const filteredContacts = useMemo(() => filterByConnectionYearRange(contacts, clampConnectionYear(connectionYearStart), clampConnectionYear(connectionYearEnd)), [contacts, connectionYearStart, connectionYearEnd])`. Replace `const total = contacts.length` and `const current = contacts[currentIndex]` (lines 45-46) to read from `filteredContacts` instead of `contacts`. Add a `useEffect` that resets `currentIndex` to 0 when `connectionYearStart`/`connectionYearEnd` change (mirror the existing reset-on-index effect style; key on the two params). Render `<ConnectionYearFilter years={connectionYears} />` near the top of the returned card — ABOVE `<TriageCard>` (line 237), since it filters the queue rather than editing a per-contact field — with a sibling line showing the live match count `{filteredContacts.length} contacts` (D-07). Add a NEW render branch BEFORE the existing completion branch (line 191): when `total === 0 && (connectionYearStart != null || connectionYearEnd != null)`, render a centered Card (reuse the lines 197-219 layout) with the empty-state message "No connections from {start}{end ? `–${end}` : ''} — clear the filter" plus the `ConnectionYearFilter` (so Clear is reachable) — do NOT show "Triage Complete" for an empty FILTER (only for a genuinely drained queue with no filter active). Keep `page.tsx` untouched (D-04 — no DB query change). Preserve all existing keyboard shortcuts, undo/skip, and submit logic.</action>
  <verify>
    <automated>npx tsc --noEmit && npx eslint src/features/contacts/components/triage/triage-workflow.tsx && npx vitest run src/features/contacts/lib/connection-year.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` passes; `npx eslint src/features/contacts/components/triage/triage-workflow.tsx` reports no errors; Task 1 test still green.
    - Source asserts: grep `ConnectionYearFilter`, `deriveConnectionYears`, `filterByConnectionYearRange`, `clampConnectionYear`, `useQueryStates` in triage-workflow.tsx.
    - Source asserts: `total` and `current` are derived from `filteredContacts`, not the raw `contacts` prop (grep `filteredContacts.length` and `filteredContacts[currentIndex]`).
    - Source asserts: a live count string `contacts` is rendered next to/under the control (D-07) and an empty-filter branch keyed on `connectionYearStart != null || connectionYearEnd != null` exists.
    - Behavior (verified in browser via 13-02): selecting a year narrows the queue (criterion #1), a two-year range narrows to that range (#2), reload preserves the selection (#3 — nuqs URL), Clear restores the full queue (#4).
    - `src/app/dashboard/contacts/triage/page.tsx` is NOT modified (D-04).
  </acceptance_criteria>
  <done>Triage queue is narrowed in-memory by the URL-driven connection-year selection, shows a live count and a filter-specific empty state, and the year buttons are data-derived — with no change to the RSC data fetch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL query params → client filter | `connectionYearStart` / `connectionYearEnd` are user-controllable (URL/bookmark/hand-edited). They are read client-side and applied to an in-memory list; they never reach the database in this phase. |
| (unchanged) Clerk middleware → triage RSC | Existing single-user auth gate on `/dashboard/*` (src/proxy.ts). This phase adds no new route and no new data fetch. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Tampering | `connectionYearStart`/`connectionYearEnd` URL params consumed by triage-workflow | mitigate | Parse via nuqs `parseAsInteger` (coerces to int or null) AND pass through `clampConnectionYear()` which bounds to [1990, currentYear+1] and returns null otherwise. A hostile value (e.g. `99999999`, `-1`, `abc`) yields null/empty filter, never an exception or unbounded behavior. No string is interpolated into any query. |
| T-13-02 | Information Disclosure | Triage data set | accept | No new read surface or endpoint is introduced; the control filters data already fetched for the authenticated single user by the existing RSC. No additional data is exposed. |
| T-13-03 | Tampering (supply chain) | npm dependencies | accept | No new packages installed — `nuqs` is already a project dependency. No `## Package Legitimacy Audit` needed because there are zero new installs. |

No HIGH-severity (ASVS L1) threats exist: the phase introduces no mutation, no new endpoint, no auth change, and no untrusted input that reaches a query or the DOM as markup. The single user-controlled input (year params) is integer-parsed and range-clamped before use.
</threat_model>

<verification>
- `npx vitest run src/features/contacts/lib/connection-year.test.ts` green (logic).
- `npx tsc --noEmit` clean across the repo.
- `npx eslint` clean on the two changed/new components.
- `src/app/dashboard/contacts/triage/page.tsx` unchanged (D-04).
- Interactive success criteria (#1–#4) are confirmed by plan 13-02 (human-verify).
</verification>

<success_criteria>
1. Year buttons in triage filter the queue to contacts whose `linkedinConnectionDate` falls within the selected year (criterion #1).
2. A two-year range (e.g. 2021–2022) shows only matching contacts (criterion #2).
3. The filter survives reload via nuqs `connectionYearStart`/`connectionYearEnd` URL params (criterion #3).
4. Clearing the filter restores the full triage queue (criterion #4).
5. The year control is a standalone reusable component owning the URL state, ready for Phase 14 reuse (D-08).
</success_criteria>

<output>
Create `.planning/phases/13-triage-connection-date-filter/13-01-SUMMARY.md` when done.
</output>
