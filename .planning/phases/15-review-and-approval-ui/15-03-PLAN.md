---
phase: 15-review-and-approval-ui
plan: 03
type: execute
wave: 2
depends_on: ['15-01']
files_modified:
  - src/features/outreach/components/campaign-review-page.tsx
autonomous: true
requirements: [REV-01, REV-04, REV-06]

must_haves:
  truths:
    - "The campaign review page lists every email in the campaign, each rendered as an EmailReviewCard"
    - "The campaign header shows progress counts: approved / total"
    - "Editing, approving, or regenerating one card updates the page's progress counts without a full reload (optimistic local state)"
    - "Pending emails (before generation) render as cards in pending state; an empty campaign shows the existing empty state"
  artifacts:
    - path: "src/features/outreach/components/campaign-review-page.tsx"
      provides: "Client review-page container: progress header + EmailReviewCard list + optimistic state"
      exports: ["CampaignReviewPage"]
      contains: "EmailReviewCard"
  key_links:
    - from: "src/features/outreach/components/campaign-review-page.tsx"
      to: "src/features/outreach/components/email-review-card.tsx"
      via: "renders one EmailReviewCard per email, passes onEmailUpdated"
      pattern: "EmailReviewCard"
    - from: "src/features/outreach/components/campaign-review-page.tsx"
      to: "src/features/outreach/lib/review-helpers.ts"
      via: "approvedCount for the progress header"
      pattern: "approvedCount"
---

<objective>
Replace the Phase 14 placeholder `CampaignReviewPage` with a `'use client'` container that renders the campaign header with approved/total progress and a list of `EmailReviewCard`s, holding the email list in local state so per-card edit/approve/regenerate actions update the header counts optimistically.

Purpose: This is the assembly step that turns the leaf card (15-01) into the full review experience and delivers success criteria 1 and 5 (list + progress header). It depends on EmailReviewCard's `onEmailUpdated` contract.
Output: rewritten `campaign-review-page.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
EmailReviewCard (src/features/outreach/components/email-review-card.tsx — built in 15-01):
  props: { campaignId: string; email: OutreachEmail; contact: Contact | null; onEmailUpdated: (updated: OutreachEmail) => void }

review-helpers (src/features/outreach/lib/review-helpers.ts — built in 15-01):
  approvedCount(emails: OutreachEmail[]): number

RSC page (src/app/dashboard/outreach/[id]/page.tsx — UNCHANGED) already passes:
  campaign: OutreachCampaign
  emails: { email: OutreachEmail; contact: Contact | null }[]
  (the leftJoin selects the FULL contact row, so contact.archivedAt and contact.email are present — no extra query needed; performance criterion: one query loads all emails)

Existing placeholder file already imports Card/CardHeader/CardTitle/CardContent + Badge and computes statusCounts; reuse that header scaffold.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Client review-page container with progress header</name>
  <files>src/features/outreach/components/campaign-review-page.tsx</files>
  <read_first>
    - src/features/outreach/components/campaign-review-page.tsx (current placeholder — keep CampaignReviewPageProps shape, header Card, empty-state block)
    - src/features/outreach/components/email-review-card.tsx (15-01 — the child component + onEmailUpdated contract)
    - src/features/outreach/lib/review-helpers.ts (15-01 — approvedCount)
    - src/features/outreach/components/campaign-builder.tsx (useState/useCallback client-container pattern in this feature)
    - src/app/dashboard/outreach/[id]/page.tsx (the RSC that renders this component — confirm the props shape is unchanged)
  </read_first>
  <action>
    Convert `campaign-review-page.tsx` to a `'use client'` component. Keep the exported name `CampaignReviewPage` and the `CampaignReviewPageProps` interface (`campaign: OutreachCampaign`, `emails: { email: OutreachEmail; contact: Contact | null }[]`). Hold the email rows in local state seeded from the `emails` prop (e.g. `useState`), keyed by email id. Provide an `onEmailUpdated(updated)` callback that replaces the matching row's `email` in state (match on `updated.id`), so progress recomputes after any card action.
    Header (REV-06 / criterion 5): render the campaign name + goalInstruction (reuse existing Card markup) and a progress line showing `approvedCount(emails) / emails.length` approved (e.g. "3 / 12 approved"); keep the per-status Badge summary already present.
    List (REV-01): map the local rows to `<EmailReviewCard campaignId={campaign.id} email={row.email} contact={row.contact} onEmailUpdated={...} />`. Preserve the existing empty-state Card when there are no emails. Maintain the `mx-auto max-w-3xl space-y-6` layout. The RSC page (page.tsx) is NOT modified — it already passes the joined contact rows in one query (no N+1).
    Single quotes, 2-space indent, no omitted semicolons.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -i "campaign-review-page" || echo "campaign-review-page typechecks"</automated>
  </verify>
  <acceptance_criteria>
    - File begins with `'use client'` and still exports `CampaignReviewPage` with the same props shape (page.tsx compiles unchanged)
    - Renders one `EmailReviewCard` per email row, passing `campaignId={campaign.id}` and an `onEmailUpdated` handler (grep: `EmailReviewCard` and `onEmailUpdated`)
    - Header progress uses `approvedCount` over the local email list (grep: `approvedCount`)
    - `onEmailUpdated` replaces the matching row by `updated.id` so counts recompute (state update keyed on id)
    - Empty-state branch for zero emails is preserved
    - `npx tsc --noEmit` reports no errors in campaign-review-page.tsx or the RSC page
  </acceptance_criteria>
  <done>The review page lists all emails as cards, shows approved/total in the header, and recomputes progress when any card reports an update — pending emails render as pending cards before Phase 16 runs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RSC → client container | Server-fetched campaign + joined email/contact rows serialize into the client component; no new trust boundary introduced (read-only props) |
| client container → API | All mutations are delegated to EmailReviewCard, which calls the Phase 12 routes; the container performs no fetch of its own |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-06 | Information Disclosure | campaign data rendered client-side | accept | Single-user app; Clerk middleware gates `/dashboard(.*)` and `/api/(.*)` to the locked owner. No cross-tenant exposure possible |
| T-15-07 | Tampering | optimistic local state diverging from server | mitigate | onEmailUpdated only stores the server's returned OutreachEmail (the route's authoritative row), never a client-fabricated value — state mirrors persisted truth |
| T-15-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages; reuses shadcn/ui already in the tree. No legitimacy checkpoint required |
</threat_model>

<verification>
- `npx tsc --noEmit` reports no new errors; the unchanged RSC page still compiles against the props shape
- Trace: header count derives from approvedCount over the live local list; map renders one EmailReviewCard per email
</verification>

<success_criteria>
- The review page shows every email as a card with its status and (when generated) subject/body
- The header shows approved/total progress and updates after approve/edit/regenerate without a reload
- The empty campaign and pending-email states render cleanly
</success_criteria>

<output>
Create `.planning/phases/15-review-and-approval-ui/15-03-SUMMARY.md` when done
</output>
