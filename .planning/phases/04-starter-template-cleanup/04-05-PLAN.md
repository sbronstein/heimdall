---
phase: 04-starter-template-cleanup
plan: 05
type: execute
wave: 2
depends_on:
  - 04-01
  - 04-02
  - 04-03
  - 04-04
files_modified:
  - __CLEANUP__/cleanup.md
  - __CLEANUP__/clerk
  - __CLEANUP__/kanban
  - __CLEANUP__/sentry
  - __CLEANUP__/scripts
  - src/__cleanup__.test.ts
autonomous: true
requirements:
  - DEBT-A4
tags:
  - cleanup
  - starter-template
  - deletion
  - verification

must_haves:
  truths:
    - "__CLEANUP__/ directory no longer exists at repo root (SC #2)"
    - "A filesystem-existence Vitest test file exists at src/__cleanup__.test.ts asserting every Phase 4 deletion target is absent (D-16)"
    - "The test asserts the string `computeBridgeScore` is not present in src/app/api/job-leads/[id]/search/route.ts (D-16, SC #4)"
    - "npm run test:run exits 0 (test passes against the post-cleanup tree)"
    - "npm run build exits 0 against the fully-cleaned tree"
    - "package.json contains no `cleanup` script entry tied to __CLEANUP__/scripts/cleanup.js (CD-05 check)"
    - "D-13: __CLEANUP__/ directory removed entirely (clerk/, kanban/, sentry/, scripts/, cleanup.md all gone)"
    - "D-14: __CLEANUP__/ removal sequenced LAST (Wave 2 depends_on [04-01, 04-02, 04-03, 04-04]) — prevents accidental cleanup-script invocation against half-cleaned state"
    - "D-16: filesystem-existence Vitest test at src/__cleanup__.test.ts pins all Phase 4 deletions; runs in node env without PGlite"
    - "D-17: no runtime HTTP 404 smoke test — Next.js's route resolver IS the contract"
    - "D-18: plan grouping (4 disjoint Wave-1 plans + 1 verification Wave-2 plan) followed"
    - "D-19: atomic commit per DEBT-Ax preserved (one commit for DEBT-A4 plus a test+verify commit)"
  artifacts:
    - path: "src/__cleanup__.test.ts"
      provides: "Filesystem-existence assertion test that pins all Phase 4 deletions (D-16)"
    - path: ".planning/phases/04-starter-template-cleanup/04-starter-template-cleanup-05-SUMMARY.md"
      provides: "Plan completion record + end-of-phase verification report"
  key_links:
    - from: "src/__cleanup__.test.ts"
      to: "all Phase 4 deletion targets"
      via: "fs.existsSync(path) === false assertion"
      pattern: "test runs in Vitest node environment with no PGlite (D-16)"
    - from: "__CLEANUP__/"
      to: "(absent)"
      via: "rm -rf"
      pattern: "fs.existsSync('__CLEANUP__') === false"
---

<objective>
Execute DEBT-A4 (delete `__CLEANUP__/` directory) AND ship the end-of-phase verification test. This plan is sequenced as Wave 2 (depends on plans 04-01, 04-02, 04-03, 04-04) for two reasons:

1. **Safety (D-14):** `__CLEANUP__/scripts/cleanup.js` can overwrite real source files with template versions when invoked (e.g., `node __CLEANUP__/scripts/cleanup.js kanban` would overwrite the now-deleted kanban files with template versions). Sequencing `rm -rf __CLEANUP__/` LAST makes "accidentally run the cleanup script against a half-cleaned tree" structurally impossible.
2. **Verification correctness (D-16):** The filesystem-existence assertion test must run against the FINAL post-cleanup tree. Putting it in any earlier wave would have it pass against an intermediate state and miss later regressions.

Per D-16 in CONTEXT.md, verification has two layers:
- A Vitest filesystem-existence test asserting all Phase 4 deletion targets are absent + a string assertion that `computeBridgeScore` is not present in the search route.
- A clean `npm run build` exit. (Existing Phase 2 husky pre-push hook already runs `npm run build` + `npm run test:run`; this plan ships the new test that the pre-push hook will then run on every push.)

Per D-17, do NOT add a runtime HTTP test that spins up a dev server and curls the deleted routes — filesystem existence + clean build IS the contract Next.js's framework-default 404 already guarantees.

Output: One deleted top-level directory (`__CLEANUP__/`), one new test file (`src/__cleanup__.test.ts`), one atomic commit. After this commit, ROADMAP SC #1–4 are all directly verifiable by running `npm run test:run && npm run build`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-starter-template-cleanup/04-CONTEXT.md
@.planning/codebase/CONCERNS.md
@.planning/phases/02-test-infrastructure/02-CONTEXT.md
@.planning/phases/04-starter-template-cleanup/04-01-PLAN.md
@.planning/phases/04-starter-template-cleanup/04-02-PLAN.md
@.planning/phases/04-starter-template-cleanup/04-03-PLAN.md
@.planning/phases/04-starter-template-cleanup/04-04-PLAN.md
@vitest.config.ts
@__CLEANUP__/cleanup.md
@CLAUDE.md

<interfaces>
<!-- __CLEANUP__/ directory contents at repo root (entire tree gets `rm -rf`'d): -->
__CLEANUP__/
  cleanup.md        — Owner intent doc; line 33 says "Once you've finished cleaning up features you don't need, delete the __CLEANUP__ folder."
  clerk/            — Template "after Clerk removal" versions of providers, nav, etc. (NEVER USED)
  kanban/           — Template "after kanban removal" versions of providers, store, etc. (NEVER USED)
  sentry/           — Template "after Sentry removal" instrumentation. (NEVER USED)
  scripts/          — Cleanup-script implementation (cleanup.js + config). Can overwrite real source.

<!-- Vitest config (Phase 2 — no edits needed for this plan, just reuse): -->
vitest.config.ts:
  - environment: 'node'
  - globals: true
  - include: ['src/**/*.test.{ts,tsx}']  <-- new test file MUST live under src/
  - passWithNoTests: true

<!-- Phase 2 established the colocated *.test.ts pattern. This new test is a single-file Phase-4-phase artifact, not colocated with any one module — D-16 recommends `src/__cleanup__.test.ts` (top-level under src/) to be discoverable as a phase artifact. -->

<!-- Deletion targets the test must assert (all from CONTEXT.md D-16): -->
PATHS that must NOT exist post-Phase-4:
  src/features/products              (plan 04-01)
  src/app/dashboard/product          (plan 04-01)
  src/constants/mock-api.ts          (plan 04-01)
  src/app/dashboard/exclusive        (plan 04-02)
  src/app/dashboard/workspaces       (plan 04-02)
  src/app/dashboard/billing          (plan 04-02)
  src/components/ui/infobar.tsx      (plan 04-02)
  src/components/ui/info-button.tsx  (plan 04-02)
  src/components/layout/info-sidebar.tsx  (plan 04-02)
  src/config/infoconfig.ts           (plan 04-02)
  src/app/dashboard/kanban           (plan 04-03)
  src/features/kanban                (plan 04-03)
  __CLEANUP__                        (this plan)

SOURCE STRING that must NOT appear (per D-16, plan 04-04):
  `computeBridgeScore` in src/app/api/job-leads/[id]/search/route.ts
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight check + delete __CLEANUP__/ directory (DEBT-A4)</name>
  <files>
    __CLEANUP__/cleanup.md,
    __CLEANUP__/clerk,
    __CLEANUP__/kanban,
    __CLEANUP__/sentry,
    __CLEANUP__/scripts
  </files>
  <action>
    Per D-13, D-14, CD-05 in CONTEXT.md:

    1. Pre-flight CD-05 check: confirm `package.json` has no `cleanup` script tied to `__CLEANUP__/scripts/cleanup.js`:
       `grep -c "__CLEANUP__\|cleanup\.js" package.json` should return `0`. The scout-confirmed scripts in `package.json` are: dev, build, start, lint, lint:fix, lint:strict, format, format:check, prepare, db:generate, db:migrate, db:push, db:studio, db:seed, test, test:run. None reference the cleanup script. If a `cleanup` script is found, REMOVE that line from `package.json` and include it in the same commit.
    2. Verify no source file references the cleanup directory:
       `grep -rn "__CLEANUP__" src/` MUST return zero hits.
    3. Confirm Wave 1 prerequisite plans are landed — `git log` should show four commits with subjects containing `DEBT-A1`, `DEBT-A2`, `DEBT-A3`, `DEBT-A5` (in any order). If any are missing, STOP — this plan is Wave 2 and requires Wave 1 completion.
    4. `rm -rf __CLEANUP__/` — entire top-level directory removed in one operation. Use `git rm -r __CLEANUP__/` for clean staging.
    5. Verify post-deletion:
       - `test ! -d __CLEANUP__` succeeds.
       - `git status` shows only the expected deletions (and any pending edits from Task 2).
       - `npm run build` exits 0.

    Do NOT touch `scripts/` at the repo root — per CONTEXT.md `<deferred>`, that's Heimdall tooling (`generate-import-data.py`, `parse-paste.py`), not the cleanup script. Only `__CLEANUP__/scripts/` (the cleanup-script implementation) is deleted, and it goes with its parent directory.
  </action>
  <verify>
    <automated>test ! -d __CLEANUP__ &amp;&amp; ! grep -rn "__CLEANUP__" src/ &amp;&amp; npm run build</automated>
  </verify>
  <done>
    `__CLEANUP__/` does not exist. No source file references the directory. `package.json` has no `cleanup` script entry. `npm run build` exits 0.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author filesystem-existence verification test (D-16)</name>
  <files>
    src/__cleanup__.test.ts
  </files>
  <behavior>
    - Test 1 (forEach loop per CD-03): every path in the deleted-paths array returns `false` from `fs.existsSync(path)`. Path list is the 13 entries listed in `<interfaces>` "PATHS that must NOT exist post-Phase-4".
    - Test 2: reading `src/app/api/job-leads/[id]/search/route.ts` as UTF-8 yields a string that does NOT match the regex `/computeBridgeScore/` (asserts SC #4 the structural way, complementing the build-warning gate).
    - Both tests pass against the current (post-Wave-1, post-Task-1) tree. The whole test file runs in <50ms (D-16 — fast deterministic check).
    - The test catches any future regression that re-introduces a deleted starter-template path or re-imports `computeBridgeScore` into the search route.
  </behavior>
  <action>
    Per D-16, PD-05, CD-03 in CONTEXT.md and the Phase 2 Vitest harness (`vitest.config.ts` already includes `src/**/*.test.{ts,tsx}` — no config change needed):

    Create `src/__cleanup__.test.ts` with the following structure (use `import { describe, it, expect } from 'vitest'` + `node:fs` + `node:path`):

    ```ts
    import { describe, it, expect } from 'vitest';
    import { existsSync, readFileSync } from 'node:fs';
    import { resolve } from 'node:path';

    // Phase 4 deletion targets. Asserts the post-cleanup tree state per
    // .planning/phases/04-starter-template-cleanup/04-CONTEXT.md D-16.
    const deletedPaths = [
      'src/features/products',
      'src/app/dashboard/product',
      'src/constants/mock-api.ts',
      'src/app/dashboard/exclusive',
      'src/app/dashboard/workspaces',
      'src/app/dashboard/billing',
      'src/components/ui/infobar.tsx',
      'src/components/ui/info-button.tsx',
      'src/components/layout/info-sidebar.tsx',
      'src/config/infoconfig.ts',
      'src/app/dashboard/kanban',
      'src/features/kanban',
      '__CLEANUP__'
    ];

    describe('Phase 4 starter-template cleanup', () => {
      it.each(deletedPaths)('removes %s', (relPath) => {
        expect(existsSync(resolve(process.cwd(), relPath))).toBe(false);
      });

      it('removes unused computeBridgeScore import from job-leads search route', () => {
        const file = resolve(
          process.cwd(),
          'src/app/api/job-leads/[id]/search/route.ts'
        );
        const content = readFileSync(file, 'utf-8');
        expect(content).not.toMatch(/computeBridgeScore/);
      });
    });
    ```

    Notes on implementation choices:
    - **`it.each(deletedPaths)`** (CD-03 recommended `forEach`; Vitest's `it.each` is the canonical Vitest equivalent and gives one named test per path so failures point at the specific regression). Either `it.each` or a single `it()` with a `forEach` is acceptable per CD-03; `it.each` produces clearer test output.
    - **`process.cwd()`** as the root — Vitest runs from the repo root by default (the Phase 2 tests use the same pattern in PGlite migrations replay). No need for `__dirname`/`fileURLToPath` boilerplate here since the test is at top of `src/` and paths are relative to the project.
    - **`readFileSync` not async** — these are filesystem assertions; sync is fine and matches the test's sub-50ms target.
    - Do NOT import from Drizzle, PGlite, the route under test, or any application code. This is a pure filesystem test (per D-16 — node environment, no PGlite).

    After writing, run `npm run test:run` and confirm:
    - The full Phase 2 + new Phase 4 suite exits 0.
    - The new test file shows 14 passing tests (13 forEach + 1 string assertion).
    - Total suite runtime stays under the Phase 2 CD-01 10-second threshold (the new test contributes <100ms).

    If `npm run test:run` fails on any of the 14 assertions, STOP — that means a Wave 1 deletion was incomplete. Re-run the failing plan before proceeding.
  </action>
  <verify>
    <automated>test -f src/__cleanup__.test.ts &amp;&amp; npm run test:run</automated>
  </verify>
  <done>
    `src/__cleanup__.test.ts` exists, contains the 13-path forEach assertion + the source-string assertion, and `npm run test:run` exits 0. The full suite still completes under 10 seconds.
  </done>
</task>

<task type="auto">
  <name>Task 3: Final build verification + atomic commit (DEBT-A4 + verification artifact)</name>
  <files>
    .planning/phases/04-starter-template-cleanup/04-starter-template-cleanup-05-SUMMARY.md
  </files>
  <action>
    1. Run the full Phase 4 verification suite locally:
       - `npm run test:run` — MUST exit 0 (includes the new filesystem-existence test from Task 2).
       - `npm run build` — MUST exit 0, no warnings naming `computeBridgeScore`, `Infobar`, `Product`, `SaleUser`, `recentSalesData`, or any of the deleted route paths. This satisfies ROADMAP SC #4 (and confirms SC #1, SC #2, SC #3 because the build would fail on dangling imports if any deletion was incomplete).
       - Optional: `git diff --stat HEAD~4 HEAD --` to confirm the four Wave 1 commits landed cleanly. The summary should report the net file count delta (expect ~25-30 files deleted, ~5 files edited, 1 new test file added).
    2. Commit DEBT-A4 + the verification artifact atomically per D-19. The summary file gets staged with the other artifacts to make the phase-completion commit self-contained:
       ```
       git add __CLEANUP__ src/__cleanup__.test.ts .planning/phases/04-starter-template-cleanup/04-starter-template-cleanup-05-SUMMARY.md
       # (also add package.json if Task 1 found and stripped a cleanup script)
       git commit -m "feat(04): DEBT-A4 — rm __CLEANUP__/ + add filesystem-existence verification test"
       ```
       (Match Phase 3's commit style. The pre-push hook will run `npm run build` + `npm run test:run` per Phase 2's pre-push gate — this is the final gate.)
    3. End-of-phase report — write `.planning/phases/04-starter-template-cleanup/04-starter-template-cleanup-05-SUMMARY.md` covering:
       - Net diff stats (files deleted / edited / added).
       - Confirmation of all five DEBT-Ax commits in `git log` order.
       - Test suite size delta (Phase 2 baseline + 14 new assertions in `__cleanup__.test.ts`).
       - Build runtime delta (expect a small decrease — 805-line `infobar.tsx` no longer bundles).
       - Confirmation of ROADMAP SC #1–4 satisfaction.
       - Any CD-* decisions exercised (e.g., did CD-01 delete `data.ts`? did CD-05 strip a `cleanup` script from package.json?).
       - Any deviations from CONTEXT.md (expected: none).
  </action>
  <verify>
    <automated>npm run test:run &amp;&amp; npm run build &amp;&amp; git log -5 --oneline | grep -q "DEBT-A4" &amp;&amp; test -f .planning/phases/04-starter-template-cleanup/04-starter-template-cleanup-05-SUMMARY.md</automated>
  </verify>
  <done>
    `npm run test:run` and `npm run build` both exit 0 against the final tree. `git log` contains five atomic commits, one per DEBT-Ax. The phase-completion summary file exists. Phase 4 is structurally complete.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (n/a) | No new trust boundary. The cleanup-script directory is the only mutating capability removed; all four Wave 1 plans were pure deletions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-11 | Tampering | `__CLEANUP__/scripts/cleanup.js` (filesystem-mutating Node script committed at repo root) | mitigate | Reduces attack surface by removing a committed script with `--force`-bypassable safety checks (per `__CLEANUP__/cleanup.md` line 22: "Use `--force` to skip this check"). After deletion, no committed script in the repo can overwrite real source from template files. Wave 2 sequencing (D-14) ensures the script is gone before any future developer or AI could accidentally invoke it against a partially-cleaned tree. |
| T-04-12 | Repudiation | Phase 4 deletion set (cross-plan) | mitigate | The new `src/__cleanup__.test.ts` provides a permanent, version-controlled assertion that the Phase 4 deletions stayed deleted. Any future commit that re-introduces a starter-template path will fail the test in the husky pre-push gate (Phase 2 CD-01). This is durable verification, not a one-time check. |
| T-04-13 | Information Disclosure | `__CLEANUP__/clerk/`, `__CLEANUP__/sentry/` (template files) | mitigate | Removes committed reference copies of "what the app looks like with auth/observability stripped" — these were instructive for the starter template but are zero-value-and-misleading for Heimdall. Smaller repo = smaller surface for misreading historical context as current. |
</threat_model>

<verification>
After all three tasks (and end-of-phase):
- `test ! -d __CLEANUP__` succeeds.
- `src/__cleanup__.test.ts` exists and asserts 13 deleted paths + 1 source-string assertion.
- `npm run test:run` exits 0; the new test file reports 14 passing assertions.
- `npm run build` exits 0 with no warnings matching `computeBridgeScore`, `Infobar`, `Product`, `SaleUser`, `recentSalesData`, or any deleted route path.
- `git log -5 --oneline` shows five commits, one each for DEBT-A1, A2, A3, A4, A5 (atomic per D-19).
- `.planning/phases/04-starter-template-cleanup/04-starter-template-cleanup-05-SUMMARY.md` exists and reports phase-completion state.

End-of-phase, ROADMAP SC #1–4 are ALL directly verifiable:
- SC #1: `fs.existsSync('src/app/dashboard/product') === false` (asserted by `__cleanup__.test.ts`); deleted routes return 404 by Next.js framework default.
- SC #2: `fs.existsSync('src/features/products') === false`, `fs.existsSync('src/components/ui/infobar.tsx') === false`, `fs.existsSync('__CLEANUP__') === false` (all asserted by `__cleanup__.test.ts`).
- SC #3: `grep -q "Removed in Phase 4" .planning/PROJECT.md` succeeds (recorded by plan 04-03); kanban directories deleted.
- SC #4: `grep "computeBridgeScore" src/app/api/job-leads/[id]/search/route.ts` returns nothing (asserted by `__cleanup__.test.ts`); `npm run build` exits 0 with no `computeBridgeScore` warning.
</verification>

<success_criteria>
Plan is complete when:
1. `__CLEANUP__/` does not exist at the repo root.
2. `src/__cleanup__.test.ts` exists, runs in Vitest node environment, and asserts all 13 deletion targets + the `computeBridgeScore` absence.
3. `npm run test:run` exits 0; new test produces 14 passing assertions.
4. `npm run build` exits 0 against the final post-cleanup tree.
5. `package.json` contains no `cleanup` script tied to `__CLEANUP__/scripts/cleanup.js` (CD-05).
6. One atomic git commit with `DEBT-A4` in the subject.
7. `.planning/phases/04-starter-template-cleanup/04-starter-template-cleanup-05-SUMMARY.md` exists and reports satisfaction of ROADMAP SC #1–4.
</success_criteria>

<output>
After completion, the SUMMARY file (`.planning/phases/04-starter-template-cleanup/04-starter-template-cleanup-05-SUMMARY.md`) serves as the end-of-phase verification report. It should reference the four Wave 1 SUMMARY files and confirm:
- All five DEBT-Ax requirements addressed.
- All four ROADMAP SC #1–4 verifiable on a clean checkout.
- The new `__cleanup__.test.ts` enters the husky pre-push gate as a permanent regression guard.
- Any CD-* decisions exercised, documented for the milestone retrospective.
</output>
