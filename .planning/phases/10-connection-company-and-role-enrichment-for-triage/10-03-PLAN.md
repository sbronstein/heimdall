---
phase: 10-connection-company-and-role-enrichment-for-triage
plan: 03
type: execute
wave: 3
depends_on: [10-01, 10-02]
files_modified:
  - src/features/job-leads/components/recommendation-card.tsx
  - src/features/job-leads/components/recommendation-list.tsx
  - src/app/api/job-leads/[id]/recommendations/route.ts
autonomous: true
requirements: [ENR-05, ENR-06]
must_haves:
  truths:
    - "The triage recommendation view renders each connection's company + role-at-connection alongside the existing fields"
    - "When triaging a company's shared connections, mutual connections still missing company/role are flagged/enriched just-in-time without requiring the full backlog processed first"
    - "The recommendations route stays a pure READ — it surfaces enrichment need and triggers it through the REST PATCH endpoint, never writing the contacts table inline"
  artifacts:
    - path: "src/features/job-leads/components/recommendation-card.tsx"
      provides: "Render of companyAtConnection + roleAtConnection per card"
      contains: "companyAtConnection"
    - path: "src/features/job-leads/components/recommendation-list.tsx"
      provides: "Passes the new fields from rec.contact into the card props"
      contains: "companyAtConnection"
    - path: "src/app/api/job-leads/[id]/recommendations/route.ts"
      provides: "JIT detection of contacts missing at-connection fields; meta.pendingEnrichment"
      contains: "pendingEnrichment"
  key_links:
    - from: "src/features/job-leads/components/recommendation-list.tsx"
      to: "src/features/job-leads/components/recommendation-card.tsx"
      via: "companyAtConnection / roleAtConnection props from rec.contact"
      pattern: "companyAtConnection"
    - from: "src/app/api/job-leads/[id]/recommendations/route.ts"
      to: "PATCH /api/contacts/[id]/enrichment"
      via: "JIT trigger routes the WRITE through the REST endpoint, not inline db.update"
      pattern: "pendingEnrichment|enrichment"
---

<objective>
Render the at-connection company + role in the triage recommendation view, and add the
just-in-time enrichment path: when triaging a company's shared connections, any mutual
connection still missing company/role is surfaced (and triggered) for enrichment at that
moment — without requiring the full backlog processed first.

Purpose: Success criterion #1 (triage view renders company + role-at-connection) and #5 (JIT
enrichment on the company-triage path). The recommendations route must remain a pure READ for
the recommendation computation; the JIT WRITE goes through `PATCH /api/contacts/[id]/enrichment`
(Plan 02) — the no-inline-DB-write invariant flagged in CONTEXT.md.

Output: Card + list render the new fields; the recommendations route reports
`meta.pendingEnrichment` and triggers JIT enrichment via the REST endpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-connection-company-and-role-enrichment-for-triage/10-CONTEXT.md
@.planning/phases/10-connection-company-and-role-enrichment-for-triage/10-PATTERNS.md
@.planning/phases/10-connection-company-and-role-enrichment-for-triage/10-01-SUMMARY.md
@.planning/phases/10-connection-company-and-role-enrichment-for-triage/10-02-SUMMARY.md

<interfaces>
<!-- PrioritizedRecommendation.contact is the full Contact Drizzle type (gains the new columns
     once Plan 01 lands — NO prioritization.ts change). From src/features/job-leads/lib/prioritization.ts -->
  export type PrioritizedRecommendation = {
    contact: Contact;                 // Contact now includes companyAtConnection, roleAtConnection, enrichmentStatus
    score: number;
    prospects: Array<{ prospect: Prospect; bridgeScore: number }>;
  };

<!-- Card prop-passing point. From src/features/job-leads/components/recommendation-list.tsx lines 75-87 -->
  <RecommendationCard
    contactName={`${rec.contact.firstName} ${rec.contact.lastName}`}
    closeness={rec.contact.closeness}
    lastContactDate={rec.contact.lastContactDate}
    score={rec.score}
    prospects={...}
    onRequestIntro={...}
  />

<!-- Card render points. From src/features/job-leads/components/recommendation-card.tsx -->
  // RecommendationCardProps interface lines 23-35 (add two optional `string | null` props)
  // "Last contact" muted subline lines 60-64 — model the new line on this
  // <Badge variant='outline' ...> in-component badge convention lines 51-58 / 93-98

<!-- Recommendations route read+meta shape to preserve. From src/app/api/job-leads/[id]/recommendations/route.ts -->
  const rows = await db.select({ prospect, bridge, contact }).from(prospectBridges)
    .innerJoin(prospects, ...).innerJoin(contacts, ...).where(eq(prospects.jobLeadId, id));   // line 31-40
  const recommendations = buildRecommendations(rows);                                          // line 42
  return success({ recommendations, meta: { totalProspects, totalBridges, totalContacts } });  // line 44-52

<!-- The JIT write endpoint (Plan 02). PATCH /api/contacts/[id]/enrichment { companyAtConnection, roleAtConnection } -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Render at-connection company + role on the recommendation card and list</name>
  <files>src/features/job-leads/components/recommendation-card.tsx, src/features/job-leads/components/recommendation-list.tsx</files>
  <read_first>
    - src/features/job-leads/components/recommendation-card.tsx (the file being modified — props interface 23-35, last-contact subline 60-64, Badge convention 51-58)
    - src/features/job-leads/components/recommendation-list.tsx (the file being modified — card prop-passing 75-87)
    - src/features/job-leads/lib/prioritization.ts (PrioritizedRecommendation.contact is full Contact — confirms the new fields are available, no change here)
  </read_first>
  <action>
    In `recommendation-card.tsx`: add `companyAtConnection?: string | null` and `roleAtConnection?: string | null` to `RecommendationCardProps`. Destructure them in the component signature. Below the existing "Last contact" muted subline (the `lastContactDate &&` block), add a sibling muted line that renders the company + role-at-connection when present — e.g. `{(companyAtConnection || roleAtConnection) && (<p className='text-muted-foreground mt-0.5 text-xs'>{[roleAtConnection, companyAtConnection].filter(Boolean).join(' @ ')}</p>)}`. Use the same `text-muted-foreground mt-0.5 text-xs` styling as the last-contact line for visual consistency.

    In `recommendation-list.tsx`: in the `<RecommendationCard>` mapping, pass `companyAtConnection={rec.contact.companyAtConnection}` and `roleAtConnection={rec.contact.roleAtConnection}` from the full Contact object.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `recommendation-card.tsx` `RecommendationCardProps` contains `companyAtConnection` and `roleAtConnection` as optional `string | null`
    - The card renders a muted subline showing role/company-at-connection when either field is present
    - `recommendation-list.tsx` passes `companyAtConnection={rec.contact.companyAtConnection}` and `roleAtConnection={rec.contact.roleAtConnection}`
    - `npx tsc --noEmit` exits 0 and `npm run build` succeeds
  </acceptance_criteria>
  <done>The triage recommendation view shows each connection's company + role-at-connection alongside the existing fields.</done>
</task>

<task type="auto">
  <name>Task 2: Add just-in-time enrichment detection + REST-triggered enrichment to the recommendations route</name>
  <files>src/app/api/job-leads/[id]/recommendations/route.ts</files>
  <read_first>
    - src/app/api/job-leads/[id]/recommendations/route.ts (the file being modified — join 31-40, buildRecommendations 42, success+meta 44-52, "pure read" comment 28-30)
    - src/app/api/contacts/[id]/enrichment/route.ts (Plan 02 — the PATCH endpoint the JIT path MUST call instead of writing inline)
    - .planning/phases/.../10-PATTERNS.md (§ recommendations/route.ts — the hook point + the no-direct-DB-write invariant)
    - .planning/phases/.../10-CONTEXT.md (§ specifics — JIT path detail)
  </read_first>
  <action>
    After the join produces `rows` (line 40) and before `buildRecommendations` (line 42), compute the set of contacts whose `companyAtConnection` AND `roleAtConnection` are both null and whose `enrichmentStatus !== 'enriched'`. These are the JIT-eligible mutual connections.

    Do NOT write the contacts table inline (architectural invariant — recommendations stays a pure read for the recommendation computation). Instead, surface the need in `meta`: extend the existing `meta` object with `pendingEnrichment: <count>` and `pendingEnrichmentContactIds: <string[]>` (additive — do NOT rename the existing keys `totalProspects`/`totalBridges`/`totalContacts`, which `recommendation-list.tsx:25` reads via `data.recommendations`).

    For the JIT trigger itself: the WRITE must go through `PATCH /api/contacts/[id]/enrichment`. Since the scrape requires the agent-browser skill (not available in a serverless request), the route's JIT responsibility is to (a) flip eligible contacts' `enrichmentStatus` toward `'pending'` via the existing PATCH endpoint by issuing an internal authenticated fetch to `/api/contacts/<id>/enrichment` per pending contact (using the bearer token from the request's own Authorization header, forwarded), and (b) report them in `meta.pendingEnrichment`. The actual company/role backfill happens when the skill drains the queue (Plan 04) — JIT here means the contacts are marked pending the instant they surface during company triage, not after the full backlog. Keep the recommendation computation itself unchanged.

    If forwarding the bearer header is not viable in this context, fall back to ONLY reporting `pendingEnrichment`/`pendingEnrichmentContactIds` in meta (no status flip) and document that the UI/skill consumes the ids — never add an inline `db.update(contacts)` to this route.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - The route detects contacts with both at-connection fields null and `enrichmentStatus !== 'enriched'`
    - `meta` gains `pendingEnrichment` (count) and `pendingEnrichmentContactIds` (string[]); existing `totalProspects`/`totalBridges`/`totalContacts` keys are unchanged
    - There is NO `db.update(contacts)` or any direct contacts-table write in this route file (grep: route file contains no `db.update(contacts)` and no `db.insert(contacts)`)
    - The recommendation computation (`buildRecommendations(rows)`) is unchanged
    - `npx tsc --noEmit` exits 0 and `npm run build` succeeds
    - `curl -H "Authorization: Bearer $(cat ~/.heimdall/api-token)" http://localhost:4000/api/job-leads/<id>/recommendations` returns 200 with `meta.pendingEnrichment` present
  </acceptance_criteria>
  <done>Triaging a company's shared connections surfaces (and marks pending) mutual connections still missing company/role, just-in-time, with the write routed through the REST enrichment endpoint and no inline DB write.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser (triage UI) → recommendations route | authed read; renders contact PII into the DOM |
| recommendations route → PATCH /enrichment | server-to-server authenticated fetch forwarding the bearer header |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-07 | Elevation of Privilege | JIT path writing to contacts | mitigate | The recommendations route never calls `db.update(contacts)`; all writes route through the audited PATCH /enrichment endpoint (Zod + logTimeline + envelope). Acceptance criteria assert no inline write |
| T-10-08 | Information Disclosure | bearer token forwarded server-to-server for JIT | mitigate | Token is read from the incoming request's Authorization header and forwarded only to the same-origin localhost endpoint; never logged (no console.log of the header); falls back to meta-only reporting if forwarding is not viable |
| T-10-09 | Tampering | rendering scraped company/role into the DOM | accept | React escapes interpolated strings by default; values were already length-capped at write time (T-10-03); no `dangerouslySetInnerHTML` |
| T-10-SC | Tampering | npm installs | accept | Zero new packages added in this plan — RESEARCH Package Legitimacy gate N/A |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0 and `npm run build` succeeds.
- The recommendation card visibly renders role/company-at-connection.
- The recommendations route reports `meta.pendingEnrichment` and contains no inline `db.update(contacts)`/`db.insert(contacts)`.
- Existing meta keys (`totalProspects`/`totalBridges`/`totalContacts`) and `data.recommendations` shape are unchanged.
</verification>

<success_criteria>
The triage view renders each connection's company + role-at-connection (criterion #1), and the
company-triage path surfaces + marks-pending mutual connections still missing the fields
just-in-time (criterion #5) — with every write routed through the REST enrichment endpoint and
no inline DB write in the recommendations route.
</success_criteria>

<output>
Create `.planning/phases/10-connection-company-and-role-enrichment-for-triage/10-03-SUMMARY.md` when done.
</output>
