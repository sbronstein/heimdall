---
phase: 08-skill-input-parsing-navigation-branching-drain
plan: 03
type: execute
wave: 2
depends_on:
  - 08-01
  - 08-02
files_modified:
  - .claude/skills/scrape-linkedin-connections/SKILL.md
  - .claude/skills/scrape-linkedin-connections/references/heimdall-api.md
  - .claude/skills/scrape-linkedin-connections/references/troubleshooting.md
autonomous: true
requirements:
  - JL-C6
  - JL-C7
must_haves:
  truths:
    - "Drain mode loop in SKILL.md is a single for-each-lead with an inline branch (D-11) keyed off the structural discriminator lead.linkedinJobUrl === null (D-12): null → company-scope branch (navigate to lead.companyLinkedinUrl + '/people/'); non-null → existing job-URL branch — both lead types drain from the same GET /api/job-leads?status=queued endpoint in a single loop (JL-C7)"
    - "When a company-scope queued lead has companyLinkedinUrl === null, the drain loop runs the bare-name search + disambiguation flow inline using lead.companyName, then PUTs /api/companies/<lead.companyId> with { linkedinUrl: <picked> } to backfill (D-14)"
    - "Drain mode emits a one-line confirmation per company-scope lead: Lead <id>: company-scope (<companyName>) — navigating to <companyLinkedinUrl>/people/... (D-15)"
    - "heimdall-api.md documents the new companyLinkedinUrl field on GET /api/job-leads response (D-13) and the discriminated POST /api/job-leads body shape from Phase 7 (used by the company-URL and bare-name flows from Plan 08-02)"
    - "heimdall-api.md documents PUT /api/companies/[id] (not PATCH) with the linkedinUrl body shape used by the D-14 backfill — actual route handler is PUT per src/app/api/companies/[id]/route.ts:55"
    - "troubleshooting.md folds three new failure modes (name-extraction fallback, zero-match on bare-name search, mid-drain disambiguation) into existing categories — no new top-level category"
  artifacts:
    - path: .claude/skills/scrape-linkedin-connections/SKILL.md
      provides: "Drain mode loop rewritten with linkedinJobUrl null-branch + D-14 mid-drain fallback + D-15 per-lead confirmation; rest of SKILL.md unchanged"
      contains: "lead.linkedinJobUrl"
    - path: .claude/skills/scrape-linkedin-connections/references/heimdall-api.md
      provides: "Updated GET /api/job-leads response shape (companyLinkedinUrl field), new POST /api/job-leads section (Phase 7 discriminated body), new PUT /api/companies/[id] section (D-14 backfill)"
      contains: "companyLinkedinUrl"
    - path: .claude/skills/scrape-linkedin-connections/references/troubleshooting.md
      provides: "Three new failure-mode bullets folded into existing categories"
      contains: "Could not extract company name"
  key_links:
    - from: ".claude/skills/scrape-linkedin-connections/SKILL.md (Drain mode loop)"
      to: "GET /api/job-leads (response now carries companyLinkedinUrl per Plan 08-01)"
      via: "curl -H Authorization fetch + per-lead linkedinJobUrl null check"
      pattern: "lead.linkedinJobUrl"
    - from: ".claude/skills/scrape-linkedin-connections/SKILL.md (D-14 fallback)"
      to: "PUT /api/companies/[id] (existing route, accepts linkedinUrl)"
      via: "curl -X PUT body { linkedinUrl }"
      pattern: "PUT /api/companies"
---

<objective>
Integrate the new input shapes (from Plan 08-02) with the drain queue: rewrite SKILL.md's `## Drain mode (no arg)` loop to branch on `lead.linkedinJobUrl`, consume the new `companyLinkedinUrl` field from the GET response (Plan 08-01 shipped this), and run the D-14 mid-drain fallback (bare-name disambiguate + PUT backfill) when `companyLinkedinUrl` is null. Document the three API touch-points the skill now relies on in `heimdall-api.md`, and fold the three new failure modes into `troubleshooting.md`.

This is the Wave 2 plan — it depends on Plan 08-01 (GET projection ships `companyLinkedinUrl`) and Plan 08-02 (bare-name + Company-URL paths exist in `linkedin-navigation.md` for the loop to reference).

Purpose: Implements user decisions D-11 (single drain loop with inline branch on `linkedinJobUrl`), D-12 (structural discriminator is `lead.linkedinJobUrl === null` everywhere), D-13 (consume the new GET field), D-14 (drain-mode fallback: bare-name flow + PUT backfill), D-15 (per-lead confirmation line). Documentation updates per CD-01 / D-13 cross-references.

Output:
- SKILL.md `## Drain mode (no arg)` section: replaces the existing 6-step list with the D-11/D-14/D-15 branched loop
- `references/heimdall-api.md`: extends GET /api/job-leads response shape; adds POST /api/job-leads section (Phase 7) and PUT /api/companies/[id] section (D-14 backfill)
- `references/troubleshooting.md`: appends three new bullets under existing categories — name-extraction fallback, zero-match on bare-name search, mid-drain disambiguation
- No code changes — markdown-only plan
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-skill-input-parsing-navigation-branching-drain/08-CONTEXT.md
@.planning/phases/08-skill-input-parsing-navigation-branching-drain/08-PATTERNS.md
@.planning/phases/08-skill-input-parsing-navigation-branching-drain/08-01-PLAN.md
@.planning/phases/08-skill-input-parsing-navigation-branching-drain/08-02-PLAN.md
@.planning/phases/07-schema-api-for-company-scope-leads/07-CONTEXT.md

<interfaces>
<!-- API shapes the drain loop relies on. Plan 08-01 already shipped the GET extension. -->

GET /api/job-leads?status=queued (post-08-01):
- Response per row: { id, linkedinJobUrl, roleTitle, companyName, companyId, applicationId, status, scrapedData, prospectCount, lastError, lastErrorAt, createdAt, updatedAt, archivedAt, companyLinkedinUrl }
- companyLinkedinUrl is null OR string

POST /api/job-leads (Phase 7, src/app/api/job-leads/route.ts):
- Body shape B (company-scope): { companyName: string, linkedinCompanyUrl?: string-url }
- Returns 201 newly-created OR 200 idempotent dedup; both envelope { success: true, data: <lead-row> }

PATCH /api/job-leads/<id>/status (existing, unchanged in Phase 8):
- Claim: body { status: 'searching' }
- Failure write: body { status: 'failed', lastError: '<Category>: <detail>' }

POST /api/job-leads/<id>/prospects (existing, unchanged in Phase 8):
- Body { prospects: ScrapedProspect[] }
- Side effects: insert + flip 'searching' → 'found' + emit timeline event

PUT /api/companies/[id] (existing route — VERIFIED via src/app/api/companies/[id]/route.ts:55):
- Body: { linkedinUrl?: string-url, ...other optional fields }
- updateCompanySchema (line 18) already accepts linkedinUrl
- Returns { success: true, data: <updated company row> }
- HARD CONSTRAINT: this is PUT, NOT PATCH. CONTEXT.md uses "PATCH" colloquially but the route handler exports PUT.

D-14 backfill flow (must appear in SKILL.md drain loop + linkedin-navigation.md Bare-name Step 5 + heimdall-api.md PUT section):
```
url = await runBareNameFlow(lead.companyName)   // interactive: presents 3–5 matches, waits for user pick
PUT /api/companies/<lead.companyId> { linkedinUrl: url }
navigate(`${url.endsWith('/') ? url : url + '/'}people/`)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite SKILL.md Drain mode loop with linkedinJobUrl branch, D-14 mid-drain fallback, D-15 per-lead confirmation</name>
  <files>.claude/skills/scrape-linkedin-connections/SKILL.md</files>
  <read_first>
    - .claude/skills/scrape-linkedin-connections/SKILL.md (FULL — current `## Drain mode (no arg)` at lines 44–55 is the 6-step list being rewritten; the rest of the file — Setup / Argument parsing (already rewritten by 08-02) / Single-lead mode / Error handling / Constraints — stays verbatim)
    - .planning/phases/08-skill-input-parsing-navigation-branching-drain/08-PATTERNS.md (§ .claude/skills/scrape-linkedin-connections/SKILL.md — "Phase 8 rewrite — drain-mode loop" has the full target block)
    - .planning/phases/08-skill-input-parsing-navigation-branching-drain/08-CONTEXT.md (§ decisions D-11, D-12, D-13, D-14, D-15; § specifics "Drain-mode loop sketch")
    - .planning/phases/08-skill-input-parsing-navigation-branching-drain/08-01-PLAN.md (confirms `companyLinkedinUrl` is on every GET row after Wave 1 — required prerequisite for this loop)
    - src/app/api/companies/[id]/route.ts (FULL — confirms PUT handler at line 55; confirms updateCompanySchema accepts linkedinUrl at line 18 — used by D-14 backfill)
  </read_first>
  <action>
    Rewrite the `## Drain mode (no arg)` section of SKILL.md. The rest of the file is NOT touched by this task — `## Setup`, `## Argument parsing` (already rewritten by Plan 08-02), `## Single-lead mode` (already partially extended by Plan 08-02 for Step 3 sub-routes), `## Error handling`, `## Constraints` all remain byte-identical to their post-08-02 state.

    Replace the entire `## Drain mode (no arg)` block (currently 6 steps; lines 43–55) with the following 6-step block. Use the existing markdown numbered-list style (1. / 2. / 3. ...) and fenced code blocks for the curl + pseudo-code:

    1. **Fetch the queue.** Use the existing curl, but add the note about the new field:
       ```bash
       TOKEN=$(cat ~/.heimdall/api-token)
       curl -s -H "Authorization: Bearer $TOKEN" \
         'http://localhost:4000/api/job-leads?status=queued&limit=50'
       ```
       Each row in `data[]` now includes `companyLinkedinUrl` (D-13) — string when the linked company has a non-null `linkedinUrl`, null otherwise. Job-URL leads also carry `companyLinkedinUrl` (joined from the same `companies` row) but the drain loop ignores it for them — they navigate via `linkedinJobUrl` instead.

    2. **Render `data` as a markdown table** with these columns: `id`, scope (`linkedinJobUrl ? 'job-URL' : 'company-scope'`), `companyName`, `roleTitle`, queued-since (from `updatedAt`). The scope column makes the queue mix visible to the user at a glance.

    3. **Ask the user**: "Process all N? Process the first then ask again? Skip and exit?" (unchanged from today)

    4. **For each approved lead, branch on `lead.linkedinJobUrl`** — this is the single-loop with inline branching per D-11 / D-12 / JL-C7. Insert this pseudo-code block (keep as markdown-fenced `text` block, NOT as a bash script — it is executor guidance, not literal shell):
       ```text
       print(`Lead ${lead.id}: ${lead.linkedinJobUrl ? 'job-URL' : 'company-scope'} (${lead.companyName})`)   // D-15

       if (lead.linkedinJobUrl == null) {
         // Company-scope branch (D-11, D-12, D-15)
         let url = lead.companyLinkedinUrl
         if (url == null) {
           // D-14 mid-drain fallback: bare-name search → disambiguate → backfill
           //   1. Follow references/linkedin-navigation.md § Bare-name path using lead.companyName as the keyword.
           //   2. User picks → derive url from the picked card.
           //   3. Backfill the persisted companies row so subsequent drains find a non-null companyLinkedinUrl.
           url = runBareNameFlow(lead.companyName)
           PUT /api/companies/<lead.companyId> body { linkedinUrl: url }     // Note: PUT (not PATCH). See heimdall-api.md.
         }
         print(`Lead ${lead.id}: company-scope (${lead.companyName}) — navigating to ${url}/people/...`)   // D-15
         navigate(url.endsWith('/') ? `${url}people/` : `${url}/people/`)
         // Per references/linkedin-navigation.md § Company-URL path: do NOT re-POST /api/job-leads — the lead already exists.
         // Best-effort name extraction is unnecessary here (the lead already has companyName).
       } else {
         // Job-URL branch (unchanged from Phase 5)
         // Follow references/linkedin-navigation.md § Job-URL path Steps 1–3:
         //   open lead.linkedinJobUrl → click company name link → click "X employees" link.
         navigateJobUrlBranch(lead.linkedinJobUrl)
       }

       // Both branches converge at references/linkedin-navigation.md § Shared:
       // Apply the 2nd-degree filter (Shared Step 4), paginate + extract (Shared Step 5), POST /api/job-leads/<id>/prospects.
       claimAndScrape(lead.id)
       ```

    5. **On failure: write the categorized error and CONTINUE to the next lead — do NOT abort the batch.** (Same as today.) New: if the D-14 mid-drain disambiguation step is presented and the user cancels (no pick / Ctrl-C), write the failure as `LinkedIn navigation failed: user cancelled disambiguation for <companyName>` and continue to the next lead.

    6. **End with a summary** (unchanged): `N processed, M succeeded, K failed (categories: Timeout: x, LinkedIn navigation failed: y, ...)`.

    HARD CONSTRAINTS:
    - Use PUT, not PATCH, on `/api/companies/<id>` everywhere in this section. The pattern mapper verified that the actual route handler is PUT.
    - Do NOT touch any other section of SKILL.md. This task owns the drain loop only.
    - The D-15 confirmation line ("Lead <id>: company-scope (<companyName>) — navigating to ...") must be present verbatim in the pseudo-code block (the executor narrates this when running the loop).
    - The branch discriminator MUST be `lead.linkedinJobUrl == null` (per D-12). Do NOT use `roleTitle === 'Company-wide scrape'` as a control-flow predicate anywhere.
  </action>
  <verify>
    <automated>test -f .claude/skills/scrape-linkedin-connections/SKILL.md</automated>
    <automated>grep -q "lead.linkedinJobUrl" .claude/skills/scrape-linkedin-connections/SKILL.md</automated>
    <automated>grep -q "companyLinkedinUrl" .claude/skills/scrape-linkedin-connections/SKILL.md</automated>
    <automated>grep -q "PUT /api/companies" .claude/skills/scrape-linkedin-connections/SKILL.md</automated>
    <automated>grep -v '^#' .claude/skills/scrape-linkedin-connections/SKILL.md | grep -c "PATCH /api/companies" | grep -q "^0$"</automated>
    <automated>grep -q "D-14" .claude/skills/scrape-linkedin-connections/SKILL.md || grep -q "mid-drain fallback" .claude/skills/scrape-linkedin-connections/SKILL.md</automated>
    <automated>grep -q "company-scope (" .claude/skills/scrape-linkedin-connections/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - Doc: `grep -q "lead.linkedinJobUrl" .claude/skills/scrape-linkedin-connections/SKILL.md` exits 0 (branch discriminator present per D-12)
    - Doc: `grep -q "companyLinkedinUrl" .claude/skills/scrape-linkedin-connections/SKILL.md` exits 0 (drain loop consumes the new field per D-13)
    - Doc: `grep -q "PUT /api/companies" .claude/skills/scrape-linkedin-connections/SKILL.md` exits 0 (D-14 backfill uses PUT)
    - Doc: NO PATCH /api/companies anywhere — `grep -v '^#' .claude/skills/scrape-linkedin-connections/SKILL.md | grep -c "PATCH /api/companies"` outputs `0`
    - Doc: D-15 per-lead confirmation message present — `grep -q "company-scope (" .claude/skills/scrape-linkedin-connections/SKILL.md` exits 0
    - Doc: D-14 fallback path documented — `grep -q "mid-drain" .claude/skills/scrape-linkedin-connections/SKILL.md || grep -q "bare-name search" .claude/skills/scrape-linkedin-connections/SKILL.md` exits 0
    - Doc: roleTitle NOT used as a control-flow discriminator — `grep -E "roleTitle.*===|=== .*'Company-wide scrape'" .claude/skills/scrape-linkedin-connections/SKILL.md` exits non-zero (no such control-flow comparison)
    - Doc: existing Setup / Single-lead mode / Error handling / Constraints sections preserved — `grep -q "User-side prerequisites" .claude/skills/scrape-linkedin-connections/SKILL.md && grep -q "Do NOT mock or skip the API write" .claude/skills/scrape-linkedin-connections/SKILL.md && grep -q "^## Single-lead mode" .claude/skills/scrape-linkedin-connections/SKILL.md` exits 0
    - Doc: argument parser from Plan 08-02 preserved — `grep -q "first match wins" .claude/skills/scrape-linkedin-connections/SKILL.md` exits 0 (08-02 added this; 08-03 must not delete it)
    - Doc: the drain loop's old "For each approved lead, run the single-lead flow" sentence is REPLACED — `grep -q "For each approved lead, branch on" .claude/skills/scrape-linkedin-connections/SKILL.md` exits 0 (new wording present)
    - Doc: single drain endpoint (JL-C7) — `grep -q "status=queued" .claude/skills/scrape-linkedin-connections/SKILL.md` exits 0
  </acceptance_criteria>
  <done>Drain mode is a single loop reading `GET /api/job-leads?status=queued` (already extended with `companyLinkedinUrl` by Plan 08-01); it branches on `lead.linkedinJobUrl` per D-11/D-12; null leads use `companyLinkedinUrl` (or the D-14 fallback) to navigate to `/people/`; non-null leads run the unchanged job-URL path; per-lead D-15 confirmation is emitted; PUT (not PATCH) is the only verb used for /api/companies.</done>
</task>

<task type="auto">
  <name>Task 2: Update heimdall-api.md — extend GET response shape, add POST /api/job-leads section, add PUT /api/companies/[id] section</name>
  <files>.claude/skills/scrape-linkedin-connections/references/heimdall-api.md</files>
  <read_first>
    - .claude/skills/scrape-linkedin-connections/references/heimdall-api.md (FULL — currently documents Auth, Response envelope, and four endpoints: GET /api/job-leads, PATCH /api/job-leads/[id]/status, POST /api/job-leads/[id]/prospects, POST /api/job-leads/[id]/search)
    - .planning/phases/08-skill-input-parsing-navigation-branching-drain/08-PATTERNS.md (§ heimdall-api.md — has the full target JSON shape for the extended GET response, the new POST /api/job-leads section, and the new PUT /api/companies/[id] section)
    - src/app/api/job-leads/route.ts (POST handler at lines 73–225 — the discriminated union body the new section documents)
    - src/app/api/companies/[id]/route.ts (PUT handler at line 55 + updateCompanySchema at line 18 — the route the new section documents)
    - .planning/phases/07-schema-api-for-company-scope-leads/07-CONTEXT.md (§ D-13 idempotent dedup behavior — 200 vs 201)
  </read_first>
  <action>
    Make three surgical edits to `references/heimdall-api.md`. Do NOT touch the Auth section, Response envelope section, status-code table, or the existing PATCH /status / POST /prospects / POST /search sections — they stay verbatim.

    **Edit 1: Extend the GET /api/job-leads response-shape block.**

    Locate the existing JSON block (lines 83–98 in the current doc — the one starting `{ "success": true, "data": [...] }`). Replace it with:

    ```json
    {
      "success": true,
      "data": [
        {
          "id": "uuid",
          "linkedinJobUrl": "https://www.linkedin.com/jobs/view/..." | null,
          "roleTitle": "VP Data" | "Company-wide scrape" | null,
          "companyName": "Example Co",
          "companyLinkedinUrl": "https://www.linkedin.com/company/example" | null,
          "status": "queued",
          "lastError": null,
          "updatedAt": "2026-05-14T08:00:00.000Z"
        }
      ],
      "meta": { "cursor": "2026-05-14T08:00:00.000Z", "hasMore": false }
    }
    ```

    Immediately below the JSON block, add this paragraph (verbatim):

    ```
    **Note (Phase 8 D-13):** `companyLinkedinUrl` is left-joined from `companies.linkedinUrl`.
    It is `null` when the lead has no `companyId`, OR when the linked company row has no
    `linkedinUrl` set. The drain skill uses this field to navigate directly to
    `<companyLinkedinUrl>/people/` for company-scope leads (`linkedinJobUrl === null`),
    skipping the job → company link-clicking dance.

    **Note (Phase 7 D-12 + Phase 8 D-12):** The discriminator for "is this a company-scope
    lead?" is `linkedinJobUrl === null`, not `roleTitle === 'Company-wide scrape'`. The
    sentinel role title is informational only.
    ```

    **Edit 2: Add a new "5. POST /api/job-leads" section.**

    Insert immediately AFTER section "4. POST /api/job-leads/[id]/search" and BEFORE "Error envelopes the skill must handle". Renumber as section 5 (note: the existing doc already has sections 1–4 numbered in `### N. Endpoint` style; insert the new section consistent with that style). Content per 08-PATTERNS.md § heimdall-api.md "New section to add — POST /api/job-leads (creation, not just status)":

    ```markdown
    ### 5. `POST /api/job-leads` (Phase 7 + 8)

    **Used by:** company-URL input, bare-name input pick. NOT used by drain mode (drain only PATCHes status on existing leads — the leads are already in the queue).

    **Body — discriminated union (Phase 7 D-01):** two shapes; first-match-wins via Zod `z.union`.

    Shape A — job-URL (existing job-URL flow; UI uses this; skill uses this when Branch 4 of the argument parser fires):
    ```json
    { "linkedinJobUrl": "https://www.linkedin.com/jobs/view/..." }
    ```

    Shape B — company-scope (NEW in Phase 7, used by Phase 8 skill from the company-URL and bare-name flows):
    ```json
    {
      "companyName": "OpenAI",
      "linkedinCompanyUrl": "https://www.linkedin.com/company/openai/"
    }
    ```
    (`linkedinCompanyUrl` is optional.)

    **Side effects** (handled by the route — do NOT replicate in the skill):

    1. Looks up `companies` by case-insensitive name match.
    2. On match: backfills `companies.linkedinUrl` if it was null AND the request supplied one. Never overwrites a non-null `linkedinUrl` (protects user-curated data).
    3. On no match: auto-creates a stub `companies` row with `name` + optional `linkedinUrl`, plus schema defaults for everything else.
    4. Idempotent dedup (D-13/D-14 in Phase 7 CONTEXT): if an in-flight company-scope lead already exists for this company (status in `queued`/`searching`/`failed`, `archived_at IS NULL`), returns HTTP **200** with the existing row. Otherwise inserts a new lead and returns HTTP **201**.
    5. Emits a `job_lead_created` timeline event with `metadata.scope: 'company'`.

    **Response (both branches):**
    ```json
    {
      "success": true,
      "data": {
        "id": "uuid",
        "linkedinJobUrl": null,
        "roleTitle": "Company-wide scrape",
        "companyName": "OpenAI",
        "companyId": "uuid",
        "status": "queued",
        "...": "..."
      }
    }
    ```

    The skill handles 200 and 201 identically — use the returned lead, claim it via PATCH /status.

    **Curl (company-scope):**
    ```bash
    TOKEN=$(cat ~/.heimdall/api-token)
    curl -s -X POST \
      -H "Authorization: Bearer $TOKEN" \
      -H 'Content-Type: application/json' \
      -d '{"companyName":"OpenAI","linkedinCompanyUrl":"https://www.linkedin.com/company/openai/"}' \
      http://localhost:4000/api/job-leads
    ```
    ```

    **Edit 3: Add a new "6. PUT /api/companies/[id]" section.**

    Insert immediately after the new section 5, BEFORE "Error envelopes the skill must handle". Content per 08-PATTERNS.md:

    ```markdown
    ### 6. `PUT /api/companies/[id]` (Phase 8 D-14 backfill)

    **Used by:** drain-mode fallback when a company-scope lead has `companyLinkedinUrl === null`. After running the bare-name disambiguation flow (per `references/linkedin-navigation.md` § Bare-name path), the skill backfills the URL so subsequent drains don't re-prompt.

    **Note on verb:** The actual route handler is **PUT**, not PATCH. CONTEXT.md refers to it as PATCH but the route exports `PUT` (verified in `src/app/api/companies/[id]/route.ts:55`). Use `-X PUT` in curl.

    **Body (the only field the skill writes):**
    ```json
    { "linkedinUrl": "https://www.linkedin.com/company/openai/" }
    ```

    The route's `updateCompanySchema` (line 18 in the same file) declares `linkedinUrl` as `z.string().url().optional().nullable()` — accepts any valid URL or `null`.

    **Response:** `{ success: true, data: <updated company row> }`.

    **Curl:**
    ```bash
    TOKEN=$(cat ~/.heimdall/api-token)
    curl -s -X PUT \
      -H "Authorization: Bearer $TOKEN" \
      -H 'Content-Type: application/json' \
      -d '{"linkedinUrl":"https://www.linkedin.com/company/openai/"}' \
      "http://localhost:4000/api/companies/$COMPANY_ID"
    ```
    ```

    Sections 1–4 of the original doc (GET /api/job-leads except the JSON block edit, PATCH /api/job-leads/[id]/status, POST /prospects, POST /search) are NOT touched by this task. The "Error envelopes the skill must handle" table at the bottom is also unchanged.

    HARD CONSTRAINTS:
    - PUT, not PATCH. Both the verb header and the curl example MUST use PUT. The note about the discrepancy with CONTEXT.md is explicit.
    - The new POST /api/job-leads section documents BOTH shapes (job-URL + company-scope) but emphasizes that the skill uses Shape B from the company-URL and bare-name flows.
  </action>
  <verify>
    <automated>test -f .claude/skills/scrape-linkedin-connections/references/heimdall-api.md</automated>
    <automated>grep -q "companyLinkedinUrl" .claude/skills/scrape-linkedin-connections/references/heimdall-api.md</automated>
    <automated>grep -q "POST /api/job-leads" .claude/skills/scrape-linkedin-connections/references/heimdall-api.md</automated>
    <automated>grep -q "PUT /api/companies" .claude/skills/scrape-linkedin-connections/references/heimdall-api.md</automated>
    <automated>grep -q "linkedinCompanyUrl" .claude/skills/scrape-linkedin-connections/references/heimdall-api.md</automated>
    <automated>grep -q "Phase 7 D-12" .claude/skills/scrape-linkedin-connections/references/heimdall-api.md || grep -q "discriminator" .claude/skills/scrape-linkedin-connections/references/heimdall-api.md</automated>
    <automated>grep -v '^#' .claude/skills/scrape-linkedin-connections/references/heimdall-api.md | grep -c "PATCH /api/companies" | grep -q "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - Doc: GET response now lists `companyLinkedinUrl` — `grep -q "companyLinkedinUrl" .claude/skills/scrape-linkedin-connections/references/heimdall-api.md` exits 0
    - Doc: GET response shape JSON now includes the new field on the data[0] object — `awk '/^### 1\. /,/^### 2\. /' .claude/skills/scrape-linkedin-connections/references/heimdall-api.md | grep -q "companyLinkedinUrl"` exits 0
    - Doc: D-13 note paragraph present — `grep -q "Phase 8 D-13" .claude/skills/scrape-linkedin-connections/references/heimdall-api.md` exits 0
    - Doc: D-12 discriminator note present — `grep -q "linkedinJobUrl === null" .claude/skills/scrape-linkedin-connections/references/heimdall-api.md` exits 0
    - Doc: new POST /api/job-leads section present — `grep -q "^### 5\. " .claude/skills/scrape-linkedin-connections/references/heimdall-api.md && grep -q "POST /api/job-leads" .claude/skills/scrape-linkedin-connections/references/heimdall-api.md` exits 0
    - Doc: POST section documents BOTH shapes — `grep -q "linkedinJobUrl" .claude/skills/scrape-linkedin-connections/references/heimdall-api.md && grep -q "linkedinCompanyUrl" .claude/skills/scrape-linkedin-connections/references/heimdall-api.md` exits 0
    - Doc: POST section documents idempotent dedup (200 vs 201) — `grep -q "200" .claude/skills/scrape-linkedin-connections/references/heimdall-api.md && grep -q "201" .claude/skills/scrape-linkedin-connections/references/heimdall-api.md && grep -q "dedup" .claude/skills/scrape-linkedin-connections/references/heimdall-api.md` exits 0
    - Doc: new PUT /api/companies section present and uses PUT — `grep -q "^### 6\. " .claude/skills/scrape-linkedin-connections/references/heimdall-api.md && grep -q "PUT /api/companies" .claude/skills/scrape-linkedin-connections/references/heimdall-api.md` exits 0
    - Doc: PUT section curl example uses -X PUT — `awk '/^### 6\. /,/^## /' .claude/skills/scrape-linkedin-connections/references/heimdall-api.md | grep -q "\-X PUT"` exits 0
    - Doc: PUT section notes the PATCH-vs-PUT discrepancy with CONTEXT.md — `awk '/^### 6\. /,/^## /' .claude/skills/scrape-linkedin-connections/references/heimdall-api.md | grep -qi "PUT.*not PATCH\|PATCH.*but the route\|verb"` exits 0
    - Doc: NO PATCH /api/companies in non-comment lines — `grep -v '^#' .claude/skills/scrape-linkedin-connections/references/heimdall-api.md | grep -c "PATCH /api/companies"` outputs `0`
    - Doc: existing sections preserved — `grep -q "^## Auth" .claude/skills/scrape-linkedin-connections/references/heimdall-api.md && grep -q "^### 2\. " .claude/skills/scrape-linkedin-connections/references/heimdall-api.md && grep -q "^### 3\. " .claude/skills/scrape-linkedin-connections/references/heimdall-api.md && grep -q "^### 4\. " .claude/skills/scrape-linkedin-connections/references/heimdall-api.md && grep -q "^## Error envelopes" .claude/skills/scrape-linkedin-connections/references/heimdall-api.md` exits 0
  </acceptance_criteria>
  <done>heimdall-api.md's GET section reflects the new `companyLinkedinUrl` field; two new sections (POST /api/job-leads + PUT /api/companies/[id]) document the routes the skill now calls; PUT is the only verb used for /api/companies; sections 1–4 + Error envelopes table are unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Add three new failure-mode bullets to troubleshooting.md under existing categories</name>
  <files>.claude/skills/scrape-linkedin-connections/references/troubleshooting.md</files>
  <read_first>
    - .claude/skills/scrape-linkedin-connections/references/troubleshooting.md (FULL — currently has 5 categories: Timeout, LinkedIn navigation failed, No prospects found, Browser unavailable, Unknown error; ends with Manual recovery and "What the skill does NOT handle (yet)")
    - .planning/phases/08-skill-input-parsing-navigation-branching-drain/08-PATTERNS.md (§ troubleshooting.md — has the verbatim text of the three new bullets and where they go)
    - .planning/phases/08-skill-input-parsing-navigation-branching-drain/08-CONTEXT.md (§ decisions D-06, D-09, D-14)
  </read_first>
  <action>
    Append three new bullets to existing sections in `references/troubleshooting.md`. Do NOT add new top-level categories (the doc's own guidance: "the bar for adding a sixth category is high"). All three modes map into the existing `## LinkedIn navigation failed` category.

    **Edit 1: Append to `## LinkedIn navigation failed` → "Common triggers and remediation"** list.

    After the existing fifth bullet ("DOM change.") add these three bullets (verbatim from 08-PATTERNS.md):

    ```markdown
    - **Company-name-extraction failure (Phase 8 D-05/D-06).** When the skill navigates to
      `/company/<slug>/people/`, it extracts the company name from the page H1 / heading-role
      element. If that extraction returns null/empty (DOM shift, sign-in wall, captcha during
      load), this is NOT a hard failure: the skill falls back to using the slug as the
      `companyName` for the POST `/api/job-leads` call, logs the warning `Could not extract
      company name from <url>; using slug "<slug>" as fallback. Rename in the companies UI if
      needed.`, and proceeds. Remediation is post-hoc curation in the companies UI; no skill
      retry needed.

    - **Zero matches on bare-name LinkedIn search (Phase 8 D-09).** When the user passes a bare
      company name and `https://www.linkedin.com/search/results/companies/?keywords=<name>`
      returns no result cards, the skill writes `No companies found for "<name>". Try a more
      specific name or pass a LinkedIn company URL.` and exits cleanly. This is BEFORE any
      Heimdall row is created — no `job_leads` row exists to mark as `failed`; the failure is
      a user-facing message only. Distinguishable from `No prospects found` (which is
      post-navigation, after the lead is created and claimed).

    - **Mid-drain disambiguation (Phase 8 D-14).** When draining a company-scope lead whose
      `companyLinkedinUrl IS NULL`, the skill pauses and runs the bare-name disambiguation flow
      inline using `lead.companyName`. The user picks; the skill PUTs
      `/api/companies/<lead.companyId>` to backfill the URL (verb is PUT, not PATCH — per
      `heimdall-api.md` § 6), then resumes navigation. If the user cancels (no pick / Ctrl-C),
      the skill writes `failed` with `lastError: "LinkedIn navigation failed: user cancelled
      disambiguation for <companyName>"` and continues to the next lead.
    ```

    **Edit 2: Append to "What the skill does NOT handle (yet)"** (the bottom section).

    Add two new bullets immediately after the existing four (Rate-limit backoff / Pagination beyond page 10 / Captcha auto-solve / Multi-instance drain):

    ```markdown
    - **Auto-pick disambiguation single matches** (Phase 8 D-08 declined). Even when LinkedIn
      returns exactly one result for a bare-name search, the skill confirms with the user
      before proceeding. The cost is one keystroke; the benefit is never silently scraping the
      wrong company on a fuzzy match.

    - **Retry-with-broader-query on zero matches** (Phase 8 D-09 declined). The skill does NOT
      auto-strip suffixes like "Inc"/"LLC" or fall back to looser searches. Fail-loudly is the
      v1 policy.
    ```

    Do NOT touch the other category sections (`## Timeout`, `## No prospects found`, `## Browser unavailable`, `## Unknown error`, `## Manual recovery`). Do NOT add a sixth top-level `## Category` heading.

    HARD CONSTRAINT: the mid-drain disambiguation bullet's PUT reference must explicitly say PUT (not PATCH).
  </action>
  <verify>
    <automated>test -f .claude/skills/scrape-linkedin-connections/references/troubleshooting.md</automated>
    <automated>grep -q "Could not extract company name" .claude/skills/scrape-linkedin-connections/references/troubleshooting.md</automated>
    <automated>grep -q "No companies found for" .claude/skills/scrape-linkedin-connections/references/troubleshooting.md</automated>
    <automated>grep -q "Mid-drain disambiguation" .claude/skills/scrape-linkedin-connections/references/troubleshooting.md</automated>
    <automated>grep -q "Auto-pick disambiguation" .claude/skills/scrape-linkedin-connections/references/troubleshooting.md</automated>
    <automated>grep -q "Retry-with-broader-query" .claude/skills/scrape-linkedin-connections/references/troubleshooting.md</automated>
    <automated>grep -c "^## " .claude/skills/scrape-linkedin-connections/references/troubleshooting.md | grep -qE "^[78]$"</automated>
    <automated>grep -v '^#' .claude/skills/scrape-linkedin-connections/references/troubleshooting.md | grep -c "PATCH /api/companies" | grep -q "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - Doc: name-extraction fallback bullet present — `grep -q "Could not extract company name" .claude/skills/scrape-linkedin-connections/references/troubleshooting.md` exits 0
    - Doc: zero-match bullet present — `grep -q "No companies found for" .claude/skills/scrape-linkedin-connections/references/troubleshooting.md` exits 0
    - Doc: mid-drain disambiguation bullet present — `grep -q "Mid-drain disambiguation" .claude/skills/scrape-linkedin-connections/references/troubleshooting.md` exits 0
    - Doc: D-14 backfill in the new bullet uses PUT — `awk '/Mid-drain disambiguation/,/^- \*\*|^## /' .claude/skills/scrape-linkedin-connections/references/troubleshooting.md | grep -q "PUT"` exits 0
    - Doc: NO PATCH /api/companies anywhere in non-comment lines — `grep -v '^#' .claude/skills/scrape-linkedin-connections/references/troubleshooting.md | grep -c "PATCH /api/companies"` outputs `0`
    - Doc: "What the skill does NOT handle (yet)" section appended with the two new D-08 / D-09 decline bullets — `grep -q "Auto-pick disambiguation" .claude/skills/scrape-linkedin-connections/references/troubleshooting.md && grep -q "Retry-with-broader-query" .claude/skills/scrape-linkedin-connections/references/troubleshooting.md` exits 0
    - Doc: no new top-level category was added — `grep -c "^## " .claude/skills/scrape-linkedin-connections/references/troubleshooting.md` outputs a number that equals the original top-level section count (the doc has these `## ` headings today: Timeout, LinkedIn navigation failed, No prospects found, Browser unavailable, Unknown error, Manual recovery, What the skill does NOT handle (yet) = 7 sections; count must be exactly 7 after this edit)
    - Doc: the five top-level categories present and unchanged — `for h in "Timeout" "LinkedIn navigation failed" "No prospects found" "Browser unavailable" "Unknown error"; do grep -q "^## $h" .claude/skills/scrape-linkedin-connections/references/troubleshooting.md || echo "MISSING: $h"; done` produces no output
  </acceptance_criteria>
  <done>troubleshooting.md gains three new failure-mode bullets under `## LinkedIn navigation failed` (name-extraction fallback, zero-match, mid-drain disambiguation) and two new D-08/D-09 decline bullets under "What the skill does NOT handle (yet)"; no new top-level category; PUT (not PATCH) is the verb used in the mid-drain bullet.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Skill → GET /api/job-leads | Consuming the new `companyLinkedinUrl` field from Plan 08-01's projection |
| Skill → PUT /api/companies/[id] | New caller of an existing authenticated route (D-14 mid-drain backfill) |
| Skill → user (interactive) | Disambiguation prompts during drain (D-14) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-09 | Tampering | D-14 PUT body | mitigate | `updateCompanySchema` (`src/app/api/companies/[id]/route.ts:18`) declares `linkedinUrl: z.string().url().optional().nullable()`. Invalid URLs return 400; the skill surfaces validation errors verbatim per its existing error-envelope handling. |
| T-08-10 | Repudiation | D-14 backfill silently mutates user-curated data | mitigate | Phase 7 D-09 already gated backfill on null-only (never overwrites an existing `linkedinUrl`). The PUT path through `updateCompanySchema` always replaces `linkedinUrl` when present — the skill only invokes PUT when `companyLinkedinUrl IS NULL` per the drain loop's branch, so this code path is also null-only in practice. Documented in heimdall-api.md § 6 and troubleshooting.md mid-drain bullet. |
| T-08-11 | Denial of Service | Mid-drain interactive prompt blocks drain progress | accept | This is intentional — the skill is single-threaded and synchronous per-lead. D-14 trades drain throughput for not silently scraping the wrong company (D-08 / D-09 rationale). User can cancel and resume; failed-leads stay in `'failed'` for later retry via UI. |
| T-08-12 | Information Disclosure | New troubleshooting / heimdall-api docs surface route paths + bearer-token pattern | accept | These docs already exist with the same disclosure surface; this plan adds 3 new endpoint-doc paragraphs and 3 troubleshooting bullets. No new credential, no new secret. The bearer-token pattern is documented per Phase 5 D-04 (token-in-file at `~/.heimdall/api-token`, chmod 600). |

This plan touches no code, no package installs. Package Legitimacy Gate does not apply.
</threat_model>

<verification>
1. SKILL.md's drain loop branches on `lead.linkedinJobUrl`, references `companyLinkedinUrl`, and uses PUT for the D-14 backfill
2. heimdall-api.md has 6 numbered endpoint sections (was 4): GET /api/job-leads (extended) / PATCH /status / POST /prospects / POST /search / **NEW** POST /api/job-leads / **NEW** PUT /api/companies/[id]
3. troubleshooting.md `## LinkedIn navigation failed` section has 3 new bullets at the end of "Common triggers and remediation"; "What the skill does NOT handle (yet)" has 2 new bullets at the end
4. PUT (not PATCH) is the only verb used for /api/companies in any of the three files
5. No new top-level category in troubleshooting.md (exactly 7 `## ` headings total)
6. SKILL.md sections OTHER than drain mode are byte-identical to their post-08-02 state (Setup, Argument parsing, Single-lead mode, Error handling, Constraints)
</verification>

<success_criteria>
- 3 files modified (SKILL.md drain section, heimdall-api.md, troubleshooting.md)
- Drain mode loop: single loop, branches on `lead.linkedinJobUrl`, consumes `companyLinkedinUrl` from GET response (shipped by 08-01)
- Mid-drain D-14 fallback wired: bare-name search → user picks → PUT /api/companies/[id] backfill → resume navigation
- heimdall-api.md: 6 endpoint sections (existing GET extended + 5 unchanged + 2 new)
- troubleshooting.md: 3 new bullets folded into `## LinkedIn navigation failed`; 2 new bullets in "What the skill does NOT handle (yet)"; no new top-level category
- PUT (not PATCH) for /api/companies in all three files
- Requirement coverage: JL-C6 (drain-side branching documented in SKILL.md), JL-C7 (single queue, single loop, no separate status enum — drain processes both lead types via `?status=queued`)
</success_criteria>

<output>
Create `.planning/phases/08-skill-input-parsing-navigation-branching-drain/08-03-SUMMARY.md` when done. Include: files modified, line-count delta per file, before/after `## ` heading counts for heimdall-api.md and troubleshooting.md, and a one-paragraph "phase verification next steps" note (manual dev-server smoke test using `/scrape-linkedin-connections https://linkedin.com/company/openai/` and `/scrape-linkedin-connections OpenAI` to exercise the two new branches end-to-end — agent-browser-driven flows are not unit-testable per Phase 5 D-21, so the verification is human-driven).
</output>
